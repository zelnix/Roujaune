# ROUJAUNE — Cycling Coaching Tablet App

## Problem Statement
Premium, tablet-first (landscape) React Native/Expo app for ROUJAUNE (Harmony Wellness Group):
deep charcoal/near-black theme, glass cards, red/yellow accents, French/Italian cycling heritage.
Recreate supplied high-fidelity references as real, reusable, data-driven components (not screenshots).

## Architecture
- **Frontend:** Expo Router (SDK 54). Landscape-locked via `app.json` + runtime `expo-screen-orientation`.
- **State:** Local React state; mock data. **No backend/auth yet.** Live workout uses a simulated telemetry loop (setInterval) designed to be swapped for a WebSocket/sensor stream.
- **Libs:** expo-image, expo-linear-gradient, expo-blur, react-native-svg, @expo/vector-icons, dayjs, expo-haptics, expo-screen-orientation.
- **Theme/tokens:** `src/theme.ts`. Shared UI primitives: `src/components/ui.tsx` (Touchable w/ containerStyle, Card/GlassCard, PrimaryButton, SecondaryButton, YellowButton, CircularProgress, ClimbBars, LineChart, ReadinessScale, GlassPill, SectionLabel).
- **Assets:** `assets/images/` — `logo_glyph_t.png` (bg-removed), `hero_cyclist_b2.jpg` (brightened rider), `coach_alberto_b2.jpg`. Wordmark rendered as 3D-extruded text/image in `BrandHeader`.

## Screens / Routes
- `/` **Home dashboard** — sidebar, hero (3D wordmark + Alberto card + weather), metric strip, Training Plan / Featured Route / Calendar+Today, bottom cards (Progress/Community/Wellness/Achievements). Responsive tablet + phone-landscape (compact).
- `/training` **Today's Training** — reuses sidebar; header (brand/title/date), Alberto coach card, Main Workout (interval profile + metrics), Route/Weather, right sidebar (Readiness/TrainingLoad/WhatToExpect+Start/Equipment), bottom row (Before You Ride/FB50/MPC). Responsive compact layout.
- `/workout` **Live Workout** — full-screen (no sidebar): top bar (live elapsed, route progress, finish, flame), left live cards (Power/HeartRate/Cadence w/ zone visuals), workout timeline (active-interval highlight), rider route viewport (image placeholder for future 3D avatar) + elevation profile overlay, right column (Climb/RouteMap/Wearable), ride summary strip + time-in-zones, control bar (ERG mode, ERG intensity ±, Controls panel, Pause/Resume, End Workout), Alberto live cue. Simulated live telemetry.

## Navigation
Home Alberto "Start Today's Ride" & sidebar Workouts → `/training`. Training "Start Workout" → `/workout`. Workout "End Workout"/menu → `/training`. Sidebar Home → `/`.

## Implemented (2026-06-22)
- All three screens built and verified by testing agent (100% functional pass each).
- **Live telemetry pipeline:** FastAPI WebSocket `/api/ws/telemetry` (~5 Hz trainer/wearable stream, BLE-bridge stand-in) with ERG/pause/resume/dropout control; MongoDB-backed workout sessions (`/api/workouts` start/get/list/end). Frontend `useTelemetry` hook (auto-connect, backoff reconnect, stale→estimated detection) drives Power/HR/Cadence/Speed/Elapsed; connection-status pill (LIVE/RECONNECTING/ESTIMATED/OFFLINE); verified end-to-end over wss through ingress.
- Live workout three-dot **menu overlay** (Reconnect/Settings/Touch Lock/Peaceful Pause/Save & Exit) + Controls panel.
- RN-web deprecation cleanup: `shadow*`→Platform `boxShadow` on web, `pointerEvents` prop→style, `useNativeDriver` disabled on web. (Minor `textShadow*` warning remains — cosmetic.)
- Landscape lock (native), phone-landscape responsive variants for `/` and `/training`.
- Brand images corrected + brightened; 3D wordmark; scrollable sidebar; equal-width bottom row (0px diff).

## Backlog
- **P1:** Real backend (FastAPI + MongoDB) + WebSocket/BLE telemetry for live workout (power/cadence/HR, ERG control, trainer dropout/reconnect states); replace simulated loop.
- **P1:** Build remaining sidebar routes (Virtual Routes, Progress, Community, Wellness, Calendar, Settings).
- **P2:** Real 3D rider avatar (R3F/Three) in the route viewport bound to live physics.
- **P2:** Clean up RN-web deprecation warnings (shadow*/textShadow*/pointerEvents/useNativeDriver); extract shared Toast into ui.tsx; wire workout menu-button to a real menu overlay.
- **P2:** Dedicated photos for Featured Route & Wellness (currently SVG backdrops); gold script font for signatures.

## Next Tasks
1. Decide on backend + live telemetry integration scope.
2. Flesh out the remaining navigation destinations as real screens.

## Workout Complete summary (2026-06-22)
- `/summary` **Workout Complete** screen built to match the supplied high-fidelity reference (real reusable components, no screenshot backgrounds):
  - Header (brand+tagline / "Workout Complete" title w/ checkmark+sparkles / clock+flame+wifi), bespoke wider Sidebar (Overview active, Calendar, Workouts, Routes, Progress, Wellness, Connections, Settings + Alberto coach avatar).
  - Hero Summary Card (rider image + "ALLEZ OOP!!" + "Strong ride / You held your threshold well." + Alberto recap), 10-cell Primary Ride Metrics Grid, Compliance card (overall ring + 4 compliance columns), Charts row (Power bars w/ amber area + dashed target, HR line, Time-in-Zones table), Sync & Export row (visual-only, toast), Right column (Route Summary w/ elevation map, Achievements, Recovery & Next Steps ring+items), Bottom Action Bar (View Full Analysis / Save & Exit→Home / Share Ride / Plan Tomorrow's Ride→/training).
- **Data:** hybrid — backend `POST /api/workouts/summarize` computes real aggregates (avg/NP power, HR, cadence, distance, calories, IF, TSS, time-in-zones, compliance, downsampled charts) from telemetry samples recorded live during `/workout` (`src/lib/ride.ts`); falls back to polished reference dataset when <30 samples (demo). `src/lib/summary.ts` (`useSummary` hook + fallback + static content).
- Live Workout **End Workout** now navigates to `/summary`.
- Sync/Export are visual-only for now; real Strava/Garmin/TrainingPeaks integrations deferred (backlog).
- **Phone-landscape:** `/summary` has a compact variant (icon-only sidebar rail, logo-only header, compact hero/metrics/compliance, vertically-stacked full-width charts, right-column cards wrapped below main content, wrapped Sync row, compact action bar).

## Immersive route video (2026-06-22)
- Live Workout `/workout` now features an **immersive first-person cycling YouTube video** as the center visual focus (replaces the static rider image viewport).
  - Reusable cross-platform `RouteVideo` (`src/components/RouteVideo.tsx`) + platform player cores: native `src/components/video/RouteVideoPlayer.tsx` (`react-native-youtube-iframe` over `react-native-webview`, official IFrame API, no key) and web `RouteVideoPlayer.web.tsx` (real DOM `<iframe>` controlled via postMessage; picked automatically by Metro `.web.tsx` resolution — no babel alias / `react-native-web-webview` needed).
  - States: loading (with YouTube poster), invalid URL, video-unavailable + Retry, 16:9 responsive, accessible labelled controls. `getYouTubeId`/`posterFor` in `src/lib/youtube.ts`.
  - **Sync:** `playing = !paused` — video plays while the workout runs, pauses on Pause, resumes on Resume, stops on End (unmount → navigate to `/summary`). Always **muted** (no sound), muted autoplay.
  - Config-driven source in `src/data.ts` (`routeVideo.url = https://youtu.be/XlwjMjyU410`, admin-replaceable). Added `NextUpStrip` (next interval preview: label/time/power target/RPE) + `SafetyNote` under the video. All existing workout metrics/controls preserved.
  - **NOTE:** YouTube playback CANNOT be validated in the automated preview/screenshot tool — YouTube blocks embed playback in headless/automation browsers ("Video unavailable"), confirmed even for a universally-embeddable control video. It plays correctly on real browsers and on iOS/Android via Expo Go. Future: route selection, cadence/power-matched playback, interval-linked chapters, locally-hosted licensed videos.
- **Immersive expand + live-data HUD:** the route video has an expand control (top-right) opening a full-screen immersive overlay (`ImmersiveHud`) with translucent live-data feeds overlaid on the video — elapsed + connection status (LIVE/ESTIMATED/…) + route progress (top-left), Alberto cue (top-center), metric chips Power(+W/kg)/Heart Rate/Cadence/Speed (bottom-left), Pause/Resume + End controls (bottom-right); collapse control restores the inline layout (`VideoPlaceholder` holds the inline slot while expanded). `RouteVideo` now measures its own container (onLayout) and supports `fill` + a `children` overlay slot.
- **Route picker:** `ROUTES` button on both the inline video (bottom-left) and the immersive HUD (top-right) opens `RoutePicker` — a modal grid of selectable routes (thumbnail, tag chip, place, distance/elevation) driven by `routeVideos` in `src/data.ts` (5 verified-embeddable POV rides: Alpe d'Huez, Lake Garda, Alpine Descent, Bavaria Autumn, Countryside Roads; admin-replaceable). Selecting swaps the active video (`routeIdx`) live, in both inline and immersive modes.
  - **Auto-match to workout:** on load the route is auto-selected to match the workout type (`currentWorkout.recommendedTag`, e.g. Climb → Mountain → Alpe d'Huez); matching routes show a "Great for your workout" pill, the active auto card shows a ✨ badge, and the inline title reads "· Auto-matched". "Auto-match to workout" button re-applies it.
  - **Surprise me:** shuffle button picks a random different route.
  - **Last ride memory:** the chosen route is persisted with AsyncStorage (`src/lib/prefs.ts`) and restored on the next session (title shows "· Last ride", picker marks it "Your last ride", with a resume toast); choosing Auto-match clears it so it re-matches by workout type.
  - **Route library (15 routes, 2 curated sections):** `routeVideos` now carries a `level` field and the picker groups into **"Legendary race climbs & stages"** (Alpe d'Huez, Mont Ventoux, Passo dello Stelvio, Dolomites Passes, Alpine Ascent, Legendary Climbs, Lake Garda, Alpine Descent) and **"Easy & scenic — casual riders"** (Mincio Riverside, Lake Achensee, Ocean Shores Coast, Spreewald Waterways, German Countryside, Bavaria Autumn, Countryside Roads). All verified embeddable; tag colors encode difficulty. `currentWorkout.recommendedTag = "Climb"`.
- **Route carried into summary + ride history:** the ridden route (name/place/distance/elevation) flows via `rideRecorder` into the `/summary` Route Summary card and the ROUTE metric cell (`useSummary` returns `route`; `RouteSummaryCard`/`MetricsGrid` accept it). Backend `POST /api/workouts/summarize` now accepts the route object and persists a lightweight `ride_history` record (route + key metrics + timestamp); `GET /api/rides/history` lists recent rides.

## Device connection, HUD toggle & audio (2026-06-22)
- **Persistent settings** (`src/lib/settings.ts`, AsyncStorage): `hasTrainer`, `hasWearable`, `hudEnabled`. Opened via the top-bar gear or menu → `SettingsPanel` (Switches).
- **Not-connected indicators:** when `hasTrainer` is off, Power/Cadence cards + Speed show a "Not connected · Connect a smart trainer" state; when `hasWearable` is off, Heart Rate + wearable card show "Connect a wearable". Immersive HUD chips show "—" for the disconnected metrics.
- **Hide HUD (persistent):** `hudEnabled` controls the immersive live-data overlay; an eye toggle in full screen reveals/hides it on the fly; a heads-up toast is shown when entering the workout with the HUD off. Collapse control always remains so the user is never stuck.
- **Cycling music + spoken cues** (`src/hooks/useWorkoutAudio.ts`, `expo-audio` + `expo-speech`): looping instrumental track with a Music & Audio panel (music on/off, volume stepper, Alberto voice on/off). Alberto speaks each in-workout cue aloud in a **deep male voice with a mild French accent** (runtime-selects a male French/fr-CA voice, low pitch 0.78); music **ducks to ~22%** while he speaks, then restores. Floating music button for quick access.
  - **NOTE:** audio playback, TTS voice, and the YouTube video CANNOT be validated in the preview/screenshot tool (headless browser blocks media/voices) — verify on a real device / Expo Go. TTS cannot finely tune accent intensity; voice availability varies by OS.
- **App icon:** set to the ROUJAUNE cyclist logo (`assets/images/icon.png` on dark bg, `adaptive-icon.png` transparent foreground, `favicon.png`, `splash-image.png`).

## Update (latest session)
- **App renamed to "Roujaune"** in `app.json` (name/slug/scheme).
- **Voice selector** (`useWorkoutAudio.ts` + Music panel): lists device voices filtered to Spanish/English/Italian/French (all read English → accent), numbered per accent, tap-to-preview, persisted (`prefs.ts` voiceId). Numbers in cues are spoken as English words to stop the TTS engine handing digits to a fallback (female) voice. Voice-tone slider was added then removed per user.
- **Media bar:** Audio pill + **Cast pill** (bottom-left). Immersive HUD also has a Cast button under the eye toggle.
- **Cast to TV** (`react-native-google-cast` + `expo-build-properties`, config in `app.json`, receiver `CC1AD845`, iOS Bonjour `_googlecast/_airplay`): real device discovery/connect on NATIVE BUILDS ONLY via platform-split `useCast.ts`/`useCast.web.ts`; web/Expo Go falls back to a simulated cast sheet. YouTube+HUD can't be media-cast → sheet guides Screen Mirroring for full ride. NEEDS a dev/prod build to test.
- **Tablet/TV fit-to-screen:** workout renders at fixed `DESIGN_W=1024` and scales to fit (down on tablets, up to 2× on TVs) so all panels show without scrolling; phones (height<620) keep scrolling.
- **Alberto AI coaching cues** (LIVE integration): backend `POST /api/coach/cue` uses `emergentintegrations` LlmChat with **claude-sonnet-4-6** and an Alberto persona (former grand-tour champion, cycling professor, team director, sports psychologist). Frontend (`src/lib/coach.ts`) requests a cue every 60s AND on meaningful power/cadence drift (~25s debounce), with rule-based `buildCue()` fallback on error/timeout. Requires `EMERGENT_LLM_KEY` in backend `.env` (set). Cost accrues per cue.

## "Your Training Plan" screen (2026-06-23)
- New strategic (non-calendar) roadmap screen at **`/plan`** — wired to the **Training Plan** sidebar item (home & training screens route "training" → `/plan`; `/training` remains "Today's Training", reachable via "workouts"/coach card).
- Built as real reusable RN components in `src/components/plan.tsx` (+ screen `app/plan.tsx`): `TrainingPlanSidebar` (wide branded sidebar w/ ROU•JAUNE wordmark, labeled nav, coach card + Message button), `PlanHeader/TopStatus/PlanSelector/PlanTabs`, `PlanHeroCard`, `PlanGoalsCard`, `CurrentPhaseRoadmap`+`PhaseCard`, `WeeklyLoadChart` (13-week TSS bars coloured by phase + "You are here" marker), `KeyWorkoutCard`, `AlbertoAdaptationsCard`, `PlanProgressStrip`+`ProgressRing`, `AlbertoTipFooter`. Charts are memoised `react-native-svg`.
- Mock data lives in the exported `PLAN` object (Build & Climb, 12wk/5days, 4 phases, goals, key workouts, adaptation, progress 25%). Coach persona (Alberto/Adriana) flows in via `useCoach()`.
- Landscape tablet-first: whole canvas (sidebar+content, DESIGN_W 1420) scales on both axes to fill the screen (same technique as workout). Interactions (tabs, plan selector cycle, phase/workout taps, Edit Goals, View All, Message, View Progress, nav) fire toasts. Accessibility roles/labels + 44px targets included. Backend/Alberto/FB50 data-model shapes documented in the request are represented in the component props/`PLAN` shape for later API binding.
- **The 3D rider avatar was abandoned** (deleted `src/avatar3d/`, `/avatar-3d` route, kit prefs, rider overlay/button; three/r3f packages left installed but unused).
- Added a **selectable coach persona**: `src/lib/coach-persona.ts` (`COACHES`, module store + `useCoach()` hook + `getCoach/setCoach`, persisted via `prefs.getCoachId/setCoachId`). Two coaches:
  - **Alberto** — male, default, portrait `assets/images/coach_alberto_v2.png`, uses device **Spanish (English) voice 18**.
  - **Adriana** — female, portrait `assets/images/coach_adriana.png`, uses **Spanish (English) voice 7** (slightly higher TTS pitch).
- The **coach toggle** lives at the top of the workout **Audio** panel (`MusicPanel`, testIDs `coach-alberto`/`coach-adriana`). `useWorkoutAudio` maps the active coach → its voice number (falls back to matching-gender / first Spanish-English voice if the exact number isn't on the device), re-picking whenever the coach changes or the persisted coach loads in async on cold start.
- **Name + portrait propagate everywhere** via `useCoach()`: BrandHeader descriptor, home hero signature + `AlbertoCoachCard`, training `TopStatus`/`AlbertoTrainingCard`/toasts, summary sidebar + recap (`"<name>'s recap"`, avatar, "reviewing…").
- **Backend** (`server.py`): `coach_system(name, gender)` builds the persona prompt; `POST /api/coach/cue` and `/api/coach/debrief` accept `coach_name`/`coach_gender`; debrief is cached per-coach on the ride (`debrief_<name>`), so switching coach regenerates. Verified: cue endpoint returns in-persona Adriana text.
- **NOTE:** voice numbers are per-device — the selector shows "Spanish (English) voice N"; #18/#7 map to what the rider sees on their hardware. In the headless web preview `Speech.getAvailableVoicesAsync()` is empty ("Loading device voices…"), so voices/TTS can only be validated on a real device / Expo Go.

## Training Plan action buttons wired (2026-06-23)
- **Edit Goals** → `EditGoalsModal` (`src/components/plan-modals.tsx`): add/edit/delete goals, toggle complete/incomplete, saved via `PUT /api/plan/goals` (persists to `training_plans.goals`). Screen keeps a `goalsOverride` and passes a merged `displayPlan` into `PlanProvider` so the Goals card updates instantly after save.
- **View Progress** → `ProgressModal`: fetches `GET /api/plan/progress` (progress_pct, summary, fitness CTL/ATL/TSB, 12-wk trend series, key metrics, per-week completion bars). Renders a dual-line CTL/ATL SVG trend chart + weekly-completion bars.
- **View All Adaptations** → `AdaptationsModal`: fetches `GET /api/plan/adaptations` (newest-first history, coach-filtered, seeds first entry from cached/static adaptation). Timeline of coach notes with trigger chip + relative time. Backend now records adaptation history (`_record_adaptation`, capped 20) on manual refresh and after each post-ride debrief.
- New lib fns in `src/lib/plan.ts`: `savePlanGoals`, `fetchPlanProgress`, `fetchAdaptations`.

## Coach Chat + Coach Preferences + dynamic signature (2026-06-23)
- **Coach Chat** (Alberto/Adriana Intelligence): two-way AI chat. Backend `POST /api/coach/chat` (per-coach persisted thread in `coach_chats`, feeds last 10 msgs as context, Claude Sonnet 4.6, `coach_chat_system(name,gender,style)` with FB50 + My Peaceful Companion capabilities), `GET/DELETE /api/coach/chat/history`. Frontend `src/lib/coach-chat.ts` + `src/components/CoachChatModal.tsx` (empty-state greeting + suggestions, optimistic send, typing dots, tap-to-play TTS per coach via `src/hooks/useCoachSpeech.ts`). Launched from "Message {coach}" button in `/plan` header (reusable).
- **Coach Preference (Settings)**: new COACH PREFERENCE card — Coaching Style (Balanced/Performance/Calm/Essential) + Voice Guidance (Full/Essential/Visual/Muted), backed by global stores + AsyncStorage prefs (`get/setCoachStyle`, `get/setVoiceGuidance` in prefs.ts; `useCoachStyle/useVoiceGuidance` in coach-persona.ts). Existing coach select + voice preview retained. Chat respects the selected coaching style.
- **Dynamic coach identity**: `CoachPersona` gained `pronouns` + `signature`; plan.tsx `Signature`, `AlbertoTipFooter` ("{name}'s Tip") and `PlanHeader` ("Adapted by {name}") are now coach-dynamic. Locked side-nav order already compliant (data.ts).

## Coach chat context-awareness (2026-06-23)
- `POST /api/coach/chat` now prepends a `_build_rider_context()` snapshot to the prompt: current plan phase + week X/Y, progress (workouts/time/TSS/CTL/ATL/TSB), open goals, most recent ride (workout/route/duration/distance/avg power/TSS from `ride_history`), and readiness (score/status/sleep/HRV/stress from WELLNESS_DATA). Coach references real numbers naturally; verified (e.g. "Week 4 of your Build Phase… TSB +6… last climb on Alpe d'Huez"). Best-effort, never blocks the reply.

## Chat empty-state latest-ride chip (2026-06-23)
- CoachChatModal empty state now fetches `GET /api/rides/history?limit=1` (`fetchLatestRide`) and shows a highlighted yellow chip "Review my {route.name or workout} ride" that seeds a contextual message, surfacing the coach's ride-awareness immediately. Verified: chip reads "Review my Alpe d'Huez ride".

## Wording + post-ride chat invite (2026-06-23)
- Renamed all "Your Coach"/"your coach" → "Your Companion Coach"/"your companion coach" across the app (coach-persona role, data.ts, plan.tsx, plan-modals, workout, HeroRoute, training, settings label + hint).
- Post-ride debrief invite: `/summary` recap card now shows a "Talk to {coach} about this ride" pill (HeroSummaryCard `onChat`) that opens the reusable CoachChatModal — inviting the rider to debrief with their companion coach while it's fresh. Verified via screenshot.

## Gender-correct coach voices + Home chat launcher (2026-06-23)
- **Voice fix:** New `src/lib/coach-voice.ts` centralises a GENDER-FIRST voice picker — Alberto → Spanish-accented **male** voice, Adriana → Spanish-accented **female** voice, kept distinct from each other, with graceful fallbacks (Spanish+gender → gender → distinct → any). Detects gender from device voice names (Jorge/Diego… vs Mónica/Paulina…) + male/female keywords. Replaced the fragile voice-index logic in `useWorkoutAudio.ts` (pickVoiceForCoach now honors a manual pick first, then gender), rewrote `useCoachSpeech.ts` (chat TTS), and updated `settings.tsx` preview to use `resolveBothCoachVoices`. Shared `COACH_PITCH` (alberto 0.9 / adriana 1.06) as a gentle secondary distinction. Settings coach card now shows "Spanish accent · male/female". NOTE: TTS voices can only be validated on a real device / Expo Go (headless web preview has no voices).
- **Home chat launcher:** AlbertoCoachCard gained a "Message {coach}" button (onMessage) → HeroRoute → Home opens the reusable CoachChatModal, making the companion coach reachable from the first screen.

## Voice fine-tuning in Settings (2026-06-23)
- New "VOICE FINE-TUNING" card: (1) **Speaking speed** segmented control (Slower 0.8 / Normal 0.95 / Faster 1.12) persisted globally (`useCoachRate`/`setCoachRate`, prefs `speechRate`), applied to chat TTS, workout cues and previews. (2) **Per-coach voice picker** — lists installed Spanish voices (`loadSpanishVoices`/`listSpanishVoices`, labelled Male/Female N), tap to select+preview, persisted via existing `setVoiceId(coachId,...)`. Empty-state hint shown in web preview (no device voices). `pickCoachVoice`/`resolveBothCoachVoices` now honor each coach's saved manual pick first; useCoachSpeech + Settings preview honor saved picks + chosen speed. Verified UI via screenshot; actual voices/speed only testable on device / Expo Go.

## Workouts hub screen (2026-06-23)
- New route `app/workouts.tsx` (the "Workouts" nav item now points here everywhere; live session stays `/workout`, reachable from training). Matches the provided reference: wide labelled sidebar (`src/components/WideSidebar.tsx`, reusable) with ROUJAUNE logo+wordmark, locked nav order, dynamic coach portrait card (NO Message button) + coach signature, Settings/Help footer.
- Content: header + 7 tabs (All/By Goal/By Duration/By Intensity/FB50/My Workouts/Favorites), ALL WORKOUT TYPES 4×2 grid of `WorkoutTypeCard`s (icon, purpose, SVG mini power-profile, Best-for, View button, favorite star), right column (Workout Categories with counts, Popular This Week, Quick Actions), bottom "Ask {coach}" recommendation panel (dynamic portrait/quote/signature → opens CoachChatModal), footer "{coach}'s Tip". Data in `src/lib/workouts.ts` (static catalog).
- Interactivity: tabs switch sections; category clicks filter grid / open FB50; favorites persist to Favorites tab; all buttons produce toasts. Fully dynamic coach (Alberto/Adriana). Verified by testing agent (iteration_14, all 11 flows pass). Tablet-first landscape.

## Workout-list (catalog detail) screen (2026-06-23)
- New route `app/workout-list.tsx` — filterable master-detail workout catalog. Left: type filter chips (All + 8 types + FB50), duration bands (<45/45–90/>90), sort (Recommended/Duration/TSS/Intensity), scrollable workout list. Right: detail panel with duration/TSS/IF/difficulty stats, TRAINING ZONES stacked bar + legend (Z1–Z6), description, Start Workout (→ /workout live HUD) + Add to Calendar. WideSidebar (workouts active) + dynamic coach card. Accepts params `type` and `workout`.
- Catalog data in `src/lib/workout-catalog.ts` (~23 workouts across all types + FB50, each with zones/TSS/IF/difficulty; helpers: getWorkout, workoutsByType, sortWorkouts, duration bands, fmtDuration).
- Wired from `/workouts`: type-card "View Workouts" → list filtered by type; category rows → list filtered; Popular cards → list preselected on that workout (workoutId); View All Popular → all; FB50 "View FB50 Sessions" → fb50. Verified: Threshold/VO2 filters + Start Workout launch confirmed via screenshots.

## Segments→HUD, persisted favorites/scheduling, nav audit (2026-06-23)
- **Live HUD mirrors chosen workout**: `/workout` accepts `workoutId` (+`title`); `WorkoutTimelineCard`/`IntervalProfile` now render the selected workout's name + a power profile in its type colour (dynamic mode). Coach context uses the chosen workout title. Launched from workout-list "Start Workout".
- **Persisted favorites**: backend `GET /api/workout-favorites` + `POST /api/workout-favorites/toggle` (Mongo `workout_prefs`). `/workouts` and `/workout-list` load + toggle via `src/lib/workout-prefs.ts` (survives restart). NOTE: `/workouts/favorites` path collided with dynamic `/workouts/{session_id}` GET → renamed to `/workout-favorites`.
- **Add to Calendar persisted**: `POST /api/calendar/schedule`, `GET /api/calendar/scheduled`, `DELETE /api/calendar/scheduled/{id}` (Mongo `scheduled_workouts`). `GET /api/calendar/week` attaches `scheduled[]` per day. Calendar screen shows a new "SCHEDULED BY YOU" grid row (`ScheduledSessionCard`, removable) + empty slots link to /workouts.
- **Nav audit**: added missing **Help** screen (`app/help.tsx`, AppScaffold, active=help) and `help:"/help"` in every ROUTE map (app-scaffold, index, plan, calendar, training, summary, workouts, workout-list). Confirmed every side-nav item routes to a real screen and Workouts opens the new `/workouts` catalog everywhere.

## Live HUD ERG target power + intervals driven by real workout segments (2026-06-24)
- **Segment timeline builder**: `src/lib/workout-catalog.ts` now exposes `buildSegments(workout)` — turns each catalog workout's zone distribution + duration into an ordered warm-up → main-set (repeats + recovery for Z4+, steady blocks for Z2/Z3) → cool-down timeline whose seconds always sum to total. Plus `currentSegment(segs, elapsed)`, `segmentProfile`, `mmss`, `targetWatts(seg, ftp)`.
- **Live HUD is fully workout-driven** (`app/workout.tsx`): removed hardcoded `POWER_TARGET = 251`. PowerCard TARGET/ZONE, WorkoutTimelineCard STEP x/N + TIME LEFT + highlighted interval-profile bar, and NEXT UP now all reflect the current segment of the chosen `workoutId`. Target watts = FTP × zone %FTP midpoint. Coach cue context (`power_target`, `segment`, `zone`) fed live so Alberto/Adriana coach the actual session.
- **FTP source**: user-editable in Settings (`src/lib/settings.ts` now has `ftp` + `ftpAuto`; new "POWER PROFILE" card in `app/settings.tsx` with +/- steppers, disabled while auto is ON) AND auto-syncs from `/api/progress` FTP metric (287 W) when Auto-update toggle is ON. Rider Profile FTP/W-kg stats now reflect `settings.ftp`.
- **Trainer sim tracks the target** (`backend/server.py` TrainerSim): WS `/api/ws/telemetry` accepts `{type:'init', elapsed, distance, watts}` (starts ride at 0 + sets base target) and `{type:'target', watts}` (per-segment). `useTelemetry` gained `sendInit`/`sendTarget`; workout screen sends init on connect and a new target on every segment change. `/api/coach/cue` accepts optional `segment`/`zone`.
- Verified: backend 7/7 pytest (`tests/test_ws_live_hud.py`) + frontend testing agent (iteration_17) — vo2-max-intervals STEP 1/13 TARGET 187 W, endurance-ride STEP 1/3, recovery-spin distinct → confirms segment-driven, not static. Fixed duplicate testID on settings (rider-profile).

## Post-ride per-segment target compliance in Summary + debrief (2026-06-24)
- **Interval scoring**: `src/lib/summary.ts` `computeIntervals()` rebuilds the chosen workout's segments (via `workout-catalog.buildSegments`) with target watts = recorded FTP × zone%, then scores the rider's recorded telemetry against each segment: avg watts + accuracy % (fraction of segment time within ±8% of target). Samples are mapped across the ACTUAL ride `elapsed` (not the planned total) so a ride that ends early only scores the segments it reached — unreached segments show "—" (not a misleading 0%). Overall "TARGET ACCURACY" counts only ridden segments.
- **Ride recorder** (`src/lib/ride.ts`): now carries `workoutId` + `ftp` (seeded in `app/workout.tsx` on mount via `reset`, kept current via `setFtp`).
- **New card**: `IntervalTargetsCard` (`src/components/summary.tsx`, testID `interval-targets-card`) — per-segment rows (zone dot, label, Z/duration, TARGET W, YOURS W, accuracy bar+%), green/yellow/red by accuracy. Direct-open /summary (no ride) shows planned targets + "Planned targets for this session — ride it to score your accuracy". Rendered on `app/summary.tsx` between Compliance and Charts.
- **AI debrief**: `useCoachDebrief` sends `interval_compliance` + a capped `intervals` list (measured segments only) to `/api/coach/debrief`; backend `CoachDebriefRequest` + prompt updated so Alberto/Adriana reference how well the rider held each interval's power target (pacing call-outs).
- Verified: backend 3/3 pytest (`tests/test_intervals_debrief.py`) + testing agent (iteration_18) full ride→summary flow; plus a follow-up fix so partial rides mark unreached segments "—" (warm-up-only 14s ride → 97% warm-up, rest "—", overall 97%).

## Adaptive zone-target engine — execution feeds the plan (2026-06-24)
- **Auto-nudge engine** (`backend/server.py`): after each ride the per-interval execution (target vs actual, grouped by zone, duration-weighted) feeds `_update_adaptive_targets`. It keeps a rolling per-zone ratio (last 5 rides) in the plan's `zone_exec`; once a pattern repeats (≥2 rides): consistent overshoot (>5% over target) nudges that zone's target bias UP, consistent fade (>5% under) nudges it DOWN — in 2% steps, capped at ±8%. Stored as `zone_bias` (e.g. {Z4:+0.04}). Recovery Z1 is never nudged.
- **Coach references it**: `_refresh_adaptation_after_ride` passes the nudges + interval accuracy into `_generate_adaptation`, so the companion coach's adaptation note (and history entry, visible in the "View All Adaptations" modal) explains the target change naturally ("nudged your Z4 up 2%… pulled Z5 back").
- **New endpoint** `GET /api/plan/targets` → `{ zone_bias }`.
- **Frontend applies it**: `src/lib/targets.ts` `fetchZoneBias()`; `workout-catalog.targetWatts(seg, ftp, bias?)` now multiplies by (1+bias[zone]). `app/workout.tsx` fetches the bias on mount, uses it for the live HUD target/next-up + the trainer-sim target, and stores the snapshot in `ride.ts` (`zoneBias`) so `summary.computeIntervals` scores against the exact targets ridden.
- Verified: backend curl (3 rides Z4-overshoot+Z5-fade → zone_bias {Z4:+0.04, Z5:-0.04}, note references it) + frontend screenshot (Z4 bias +5% → HUD/summary show 286 W vs base 273 W). Demo plan reset to neutral bias for a clean start.

## UI standardisation, Profile screen, live-workout wordmark (2026-06-24)
- **Standard sidebar policy**: The home screen's `SideNavigation` (icon rail) is now the standard for ALL top-level nav screens. Swapped `WideSidebar` → `SideNavigation` on `/workouts` and `/workout-list`; every other top-level screen already uses it directly or via `AppScaffold` (which renders `SideNavigation`). `WideSidebar` is now unused. Also removed the "Alberto companion coach" card that used to sit in the workouts sidebar.
- **Home hero overlap fix**: bumped non-compact hero height 432→476 so the 3rd header line ("Personalised cycling training with {coach}.") is no longer covered by the coach card (which grew after the "Message coach" button was added).
- **Profile screen** (`app/profile.tsx`, via `AppScaffold`): rider identity (avatar, name, tier + coached-by chip, Edit→Settings), FTP/Weight/W-kg/VO2 stats (FTP live from settings), "This Season" snapshot, and an achievements grid. The home hero profile avatar (`profile-pill`) now routes here (`HeroRoute` gained an `onProfile` prop).
- **Live workout wordmark**: `BrandWordmark` in `src/components/workout.tsx` now renders the provided `wordmark_t.png` image instead of styled text.
- Verified via screenshots: home (3rd line visible), profile screen, workouts (standard sidebar, coach card gone), live workout (wordmark image), plan Adaptations tab (Adaptive Targets card renders; AI note references interval accuracy).

## Music rotation, profile header declutter (2026-06-23 fork)
- **Random cycling-music rotation** (`src/hooks/useWorkoutAudio.ts`): replaced the single looping track with **10 royalty-free instrumental tracks** (SoundHelix CDN, admin-replaceable in `MUSIC_TRACKS`). A Fisher–Yates `shuffledOrder` play queue rotates tracks with **no repeats until the whole set is played**, then reshuffles. `player.loop = false`; on `status.didJustFinish` the hook advances via `player.replace()` (respects music on/off, volume, and coach ducking). Hook now returns `trackIdx`/`trackCount`. NOTE: audio can only be validated on a real device / Expo Go (headless web preview blocks media).
- **Profile header declutter:** `TopStatus` gained a `minimal` prop (hides the flame streak + notification bell, keeps the avatar); `AppScaffold` passes `minimalStatus`; `app/profile.tsx` sets it — so the profile page's top-right shows only the avatar. Verified via screenshot.
- Re-verified the prior fork's layout fixes via screenshot: live workout coach cue (`AlbertoLiveCue`) sits in-flow ABOVE the top bar (no overlap), and the ROUTE/Climb cards show "time-based" progress mode when no trainer is connected.

## Big fork batch — profile/home/audio/connections (2026-06-23, tested iter19)
- **Music rotation:** 10 named royalty-free tracks (`MUSIC_TRACKS` `{name,uri}`), random shuffle no-repeats. Track skip/name now lives ONLY in the Audio panel (`MusicPanel`); removed the now-playing pill from the media bar and immersive HUD.
- **Profile:** "THIS SEASON" → "YOUR PROGRESS" with horizontally-scrollable period pills (7/30/90 days, 12 months) via reusable `ProgressPanel` (fetches `/api/rider/season?days=N`). FTP now editable in the edit modal (persists to `settings.ftp`, disables `ftpAuto`). Regional details (city/region/country) shown. Avatar+flame+bell fully removed from header. Achievements auto-unlock from `/api/rider/achievements` (empty-state when none). Season + achievements RESET (ride_history cleared).
- **Home:** metric strip is now real weekly data (`/api/rider/season?days=7`): weekly rides, training time, distance km, elevation gained, FTP (settings), readiness (static). Flame → 'Your Progress' modal; bell → `NotificationsModal` (tap-to-read detail, per-row + detail mark read/unread); avatar shows the rider's uploaded avatar (person icon fallback). BrandHeader descriptor already dynamic with coach name.
- **TopStatus removed** from all non-home screens (AppScaffold + plan/calendar/workouts/workout-list/training); home keeps it.
- **Settings:** removed the RIDER PROFILE card (+ orphaned `Stat`). **Connections:** DEVICES now reflect real `settings.hasTrainer`/`hasWearable` with Connect/Disconnect (these drive the live-ride HUD); services updated — added Google Fit + Samsung Health, removed TrainingPeaks.
- Backend: `GET /api/rider/season?days=N` (period filter) and `GET /api/rider/achievements` (badges from ride_history) added. All 9 backend tests pass (`backend/tests/test_iter19_profile_progress.py`).
- OPEN: real OAuth/health linking for connected services (Strava/Google Fit) + native health SDKs (Apple/Samsung/Garmin) NOT implemented — needs API keys + a native build. Service toggles are local-state only for now.

## Structured leveled workout library (2026-06-23)
- Extended `workout-catalog.ts` with a `Level` (Foundation/Development/Performance = Beginner/Intermediate/Advanced) concept + `LEVEL_META`, and an explicit `segmentSpec` (`{label, zoneIdx, minutes, targetPct?}`) on `Workout`. Added a `structured()` factory (duration auto-summed from spec) and target-%FTP presets.
- `buildSegments()` now honours `segmentSpec` (explicit hand-authored timeline) before falling back to the auto-builder — so the Live HUD steps/targets/next-up come straight from the authored session. Verified: launching `aerobic-base-development` shows STEP 1/5, warm-up countdown, NEXT UP "Zone 2 Block 1" 12:00 @189W (=66% of 287 FTP).
- Added 6 routines × 3 levels (18 workouts) per user spec & the 50+ design principles (gradual warm-ups, controlled intensity, cadence/posture notes, seated option): 1) Aerobic Base, 2) Endurance Progression, 3) Aerobic Endurance Intervals, 4) Tempo Endurance Builder, 5) Strength Endurance (low-cadence, joint-care notes), 6) Indoor Endurance Durability (replaces 2–5h formats). All durations within the 25–75 min indoor rule.
- Workouts list (`workout-list.tsx`): added a coloured Level badge on cards, a "Level" stat, and a "SESSION STRUCTURE" breakdown (each segment's duration · zone · %FTP) in the detail pane. Verified via screenshot.
- NOTE: legacy long endurance rides still in catalog (Endurance Ride 1h45m, Long Base Builder 2h30m, Alpine Simulator 2h) VIOLATE the new 25–75 min indoor rule — pending user decision to remove/revise.

## Capability, level filter, outdoor rides, rider image, workout layout (2026-06-23)
- **Rider capability** added to profile (`beginner`/`intermediate`/`advanced`) — backend `RIDER_DEFAULT`+`RiderProfileUpdate`+`_rider_doc` default, frontend `RiderProfile` type + edit-modal chips + identity badge (coloured by level). Verified backend returns `capability`.
- **Workouts level filter** (`workout-list.tsx`): All levels / Beginner / Intermediate / Advanced chips; **defaults to the rider's capability** (verified Intermediate preselected). Filter shows the matching-level structured sessions + always shows unleveled ones. `CAPABILITY_TO_LEVEL` maps capability→Level.
- **Outdoor conversion:** the 3 long indoor rides (Endurance Ride, Long Base Builder, Alpine Simulator) got `environment: "outdoor"` + an "OUTDOOR" badge on the card and " · Outdoor ride" in detail (user chose "convert to outdoor").
- **Rider cutout cleanup:** removed the white background from `rider_male_rear_cut.png` & `rider_female_rear_cut.png` via edge flood-fill (PIL) → clean transparent cutouts (backups in /tmp). Virtual-route rider remains animated (bob + sway = in motion).
- **Live workout layout:** restructured the body into a top `innerRow` (Power/HR/Cadence | center | Terrain+Route) and a `bottomRow` (RideSummaryStrip | Wearable). ROUTE map height reduced (96px) so the ROUTE card lines up with the cadence card bottom; the WEARABLE card now sits in line with the SPEED/DISTANCE/ELEVATION strip. Verified via screenshot.
- STILL OPEN: connected-services real OAuth/health linking (needs keys + native build) — user hasn't finalised; service toggles remain local-state.

## Readiness + Rider Level engines, nav reorder (2026-06-23)
- **Rider Readiness engine** (`backend/readiness.py`, `POST /api/rider/readiness`): 0–100 score from available check-in/wearable/activity/previous-workout data with automatic re-weighting of missing categories (weights: feeling 20, sleep 15, load 15, pain 15, HR/HRV-vs-baseline 10, prev RPE/completion 10, wearable-combined 5, mood 5, other 5). Personal 7d/28d baselines for HR/HRV. Anti-double-counting down-weights the combined wearable score when raw sleep/HR/stress/load are present. Safety overrides (chest discomfort, fainting, dizziness, breathlessness, palpitations, fever, illness, severe/new/worsening pain, or pain≥7) → score 0 / "Do Not Train". Bands 85/70/55/40. Confidence high/medium/low. Returns readinessScore/status/safetyOverride/confidence/mainFactors/dataUsed. Auto-enriches training load from ride_history. Validated via curl (override, full, sparse).
- **Rider Level classifier** (`backend/rider_level.py`, `POST /api/rider/level`): Beginner/Intermediate/Advanced from ~6–8wk ability/consistency/completion/recovery/performance (never age). Re-weights available criteria. Promotion is one-step and sustained-weeks gated (B→I needs 4 wks meeting, I→A needs 6 wks); reduction only after 4 wks below or a prolonged break. Returns riderLevel/previousLevel/levelChanged/progressTowardNextLevel/confidence/mainReasons/nextLevelRequirements. Validated via curl (promote, held-back).
- NOTE: both engines are backend logic + endpoints; frontend wiring (a daily check-in form; auto-setting profile capability from /rider/level) is not yet built.
- **Side nav reorder:** Virtual Routes now sits above Training Plan (`src/data.ts` navItems).
- **Calendar access moved:** removed "Calendar" from the side nav; the Training Plan header now has a "Full calendar" pill (`testID=view-calendar`) that pushes `/calendar`. Verified via screenshot.

## Now-playing pill, profile region + avatar removal (2026-06-23 fork)
- **Now-playing pill:** tracks now carry cycling-themed names (`MUSIC_TRACKS` = `{name, uri}`); the hook returns `trackName` + `nextTrack`. A pill (♪ track name + skip-forward) shows in the live-workout media bar (`app/workout.tsx`, testID `now-playing`/`skip-track`) AND in the immersive full-screen HUD (`ImmersiveHud` gained `musicOn`/`trackName`/`onSkip`, top-right under ROUTES, testID `hud-now-playing`/`hud-skip-track`) so riders can see/skip without collapsing the ride. Verified via screenshots.
- **Profile regional details:** the Profile identity card now shows the rider's location (`city, region, country` from `rider/profile`) as a "📍 …" line under the org name. Verified ("Perh, Western Australia, Australia").
- **Profile top-right cleared:** `AppScaffold` now hides the entire `TopStatus` (flame, bell, avatar) on the Profile screen via `minimalStatus`. Verified via screenshot.

## Seated Mode, Native BLE sensors, polish (2026-06-24 fork)
- **`/training` header date hydrated:** the date row now shows the active workout's real calendar date (e.g. "Tuesday, 28 July 2026") by matching `useCalendarWeek()` day whose `cycling.workout_id === activeId`; falls back to the plan footer then generic. Replaces the old hardcoded "Wednesday, 12 May 2025".
- **CalendarCard key warning fixed:** home `TODAY'S PLAN` rows keyed by `${type}-${id ?? index}` (fb50/wellness sessions have no id → previously duplicate `undefined` keys).
- **Seated Mode (P2):** new persistent `settings.seatedMode` (AsyncStorage) with a toggle in the workout `SettingsPanel` ("Seated mode — Stay in the saddle · cues avoid standing efforts"). Passed as `seated` into coach cue context → backend `POST /api/coach/cue` `seated: bool` adds a directive so Alberto/Adriana never cue standing/out-of-the-saddle efforts and instead coach relaxed upper body / steady seated cadence. Local fallback `buildCue()` also has seated variants. A "SEATED" pill shows on the live coach cue (`AlbertoLiveCue seated`). Verified: backend seated cue returns no standing cue; toggle visible in settings.
- **Native BLE sensors (P2 — REQUIRES NATIVE BUILD):** `react-native-ble-plx@3.5.1` + config plugin (app.json: `neverForLocation`, iOS `NSBluetoothAlwaysUsageDescription`, Android `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`).
  - GATT parsers `src/lib/ble/parse.ts` (Heart Rate 0x2A37, Cycling Power 0x2A63 incl. crank cadence, CSC 0x2A5B, `cadenceFromCrank` with 1/1024s + rollover handling). 11/11 parser unit tests pass.
  - `src/hooks/useBleSensors.ts` (native, lazy-requires the native module inside try/catch so Expo Go/web never crash) — scan/connect/subscribe to Cycling Power / CSC / Heart Rate services, Android 12+ runtime permission flow (`BLUETOOTH_SCAN`/`CONNECT`, blocked → Open Settings), streams `{power,cadence,hr}`. `useBleSensors.web.ts` stub returns `supported:false`.
  - `src/components/BleSensorsPanel.tsx` opened from the workout three-dot menu ("Bluetooth Sensors"): scan list, connect/disconnect, live readings; on web/Expo Go shows a "needs a native build" notice (verified via screenshot). Connected BLE power/cadence → treated as trainer-on; HR strap → wearable-on.
  - **Feeds the WebSocket telemetry contract:** `useTelemetry.sendSensor({power,cadence,hr})` → backend WS `{type:'sensor'}` overrides `TrainerSim` output for 4s (source flips to `"sensor"`), then falls back to the sim. Verified via WS client (override applied + fallback).
  - **NOTE:** actual Bluetooth pairing CANNOT be tested in web preview / Expo Go — needs a published iOS/Android build.

## Training plans moved into MongoDB — portable admin API (2026-06-24 fork)
- **Plan definitions now live in the `plans` MongoDB collection** (previously couch-to-road was a code-generated JSON and build-and-climb a Python constant). New portable module `backend/plans_admin.py` (self-contained APIRouter `plans_router`, mounted at `/api/plans`) is designed to be lifted as-is into the separate admin project — it only needs a Motor `db` handle + optional `on_change(plan_id)` callback via `plans_admin.init(db, on_change)`.
- **Seed-on-startup is NON-DESTRUCTIVE:** `@app.on_event("startup")` seeds `couch-to-road` (from `couch_to_road_plan.json`) and `build-and-climb` (from `BUILD_AND_CLIMB`) only when missing, so live edits are never overwritten on restart. To force a pristine re-seed: `mongosh mongodb://localhost:27017/test_database --eval 'db.plans.deleteMany({})'` then restart backend.
- **Portable CRUD + structured editors:** `GET /api/plans`, `GET /api/plans/{id}`, `POST /api/plans` (409 on dup), `PUT /api/plans/{id}` (replace), `PATCH /api/plans/{id}` (merge top-level fields), `DELETE /api/plans/{id}`, `PUT /api/plans/{id}/weeks/{n}` (replace week), `PATCH /api/plans/{id}/weeks/{n}/days/{i}` (merge day fields), `POST /api/plans/{id}/adapt` (batch ops for the coaching companion / rider requests; records `edit_history` capped 30).
- **Edits reflect instantly, no rebuild:** `_on_plan_change` reloads the in-memory couch-to-road cache (`CTR_PLAN`/`CTR_WEEKS`/`_CTR_PLANNED_TSS`) after any edit, so `GET /api/plan` and `GET /api/calendar/week` immediately show changes.
- **Build & Climb** definition now sourced from `plans`; `get_plan` overlays app-mutable runtime (`goals`, `adaptation_history`, `zone_bias`, `zone_exec`, `adaptation_ai_*`) from the `training_plans` collection onto the DB definition.
- Verified: testing agent iteration_24 (10/10 pytest) — CRUD, edit propagation to /api/plan + /api/calendar/week without restart, adapt endpoint, and full regression of existing plan/calendar endpoints. Demo state restored.
- **Deferred (needs user go-ahead):** wiring the LLM coaching companion to *generate & apply* plan changes (via coach chat "make next week easier" or the adaptive engine) using the `/adapt` endpoint; and any in-app editor UI (the separate admin project will own the editor).

## Companion plan editing — Phase 2 (2026-06-24 fork)
- **Part A — chat-driven plan edits:** `POST /api/coach/chat` detects plan-edit intent (keyword gate), asks the LLM to emit sanitized structured ops (allowlist: day duration/zone/tss/title/rpe, week objective/title; max 8; validated against the plan's week+day range), applies them via `plans_admin.adapt_plan(source='rider-request')`, records an adaptation-history note ("At your request…"), and the coach reply confirms the change. Response adds `plan_updated` + `plan_change`. New module `backend/companion_plan.py`.
- **Part B — adaptive auto-ease:** after a ride debrief with `0 < compliance < 70` (couch-to-road only), `_refresh_adaptation_after_ride` eases the NEXT week's cycling days ~10% (floor 15 min) once per week (`plan_state.eased_weeks` guard); the coach adaptation note mentions the easing. Uses the active plan id via `_active_plan_id()`.
- **Frontend feedback loop:** `CoachChatModal` shows a green "Plan updated · …" pill (testID `chat-plan-notice`) and calls `onPlanUpdated`; `usePlan()` now exposes `refresh()`, and `/plan` refetches so the change appears immediately.
- Verified: testing agent iteration_25 — backend 3/3 pytest (chat edit applies + confirms; non-edit message does NOT edit; adaptive auto-ease eases week 2 25/30/35→22/27/32 and is idempotent) + frontend (pill shows, plan refetches). Demo state reset to pristine.
- **Companion editing safety:** ops are allowlisted + range-validated + capped; every companion/admin change is logged to the plan's `edit_history` (capped 30). Auto-ease currently hardcoded to `couch-to-road` (structured plan); generalising off plan metadata is a noted future tidy-up.

## Plan-switch endpoint, tidy-ups, API docs, live-workout card aspect (2026-06-24 fork)
- **Switch a rider's plan:** `GET /api/rider/plan` → `{active_plan_id, plans:[summaries]}`; `POST /api/rider/plan {plan_id, reset_progress?}` persists `assigned_plan_id` on the rider profile (404 if plan missing). `_active_plan_id()` now prefers `assigned_plan_id`, then falls back to the Green-Lantern→couch-to-road name heuristic. The `/api/plan` and `/api/calendar/week` gates were refactored to use `_active_plan_id()`. Verified: switch to build-and-climb → /api/plan returns Build & Climb; switch back → couch-to-road; calendar unbroken.
- **Tidy-ups (from review):** (1) intent matching now uses word boundaries (`_INTENT_RE`) so "added"/"harderned" no longer false-trigger while "rest day"/"harder" do; (2) adaptive auto-ease eligibility is driven by plan metadata (`plan_def.weeks` = structured) instead of a hardcoded `couch-to-road` id.
- **Admin porting doc:** `backend/PLANS_API.md` documents the full `/api/plans` contract (wiring, data model, every endpoint incl. `/adapt`, rider assignment, companion editing) for lifting `plans_admin.py` into the separate admin project.
- **Live-workout center card → 16:9:** the tablet fit-scaling scales X/Y non-uniformly, which stretched the 16:9 route video to ~2.72:1. Added an `aspectRatio` prop to `RouteVideo` and a stable, clamped compensation in `workout.tsx` (`videoAspect = clamp((16/9)/(scaleX/scaleY))`) plus slightly wider side columns (leftW 150→168, rightW 170→188). Result: the video now DISPLAYS at ~1.82:1 (≈16:9), taller and slightly narrower, and is stable across renders (verified via measured getBoundingClientRect).

## MULTI-USER — auth, per-user data, onboarding, account (2026-06-24 fork)
- **Authentication** (`backend/auth.py`): email/password (bcrypt) + Emergent-managed Google (`POST /api/auth/google {session_id}`) + Apple Sign In (`POST /api/auth/apple {identity_token}`, iOS-BUILD-ONLY). Endpoints: register/login/google/apple/me/logout. Bearer session tokens in `user_sessions` (7-day TTL, revocable on logout). `users` collection with unique email/user_id indexes.
- **Auth enforcement**: pure-ASGI `AuthMiddleware` resolves the Bearer token into a ContextVar; ALL `/api/*` require auth except `/api/auth/{register,login,google,apple}`. WS is exempt.
- **Per-user data**: `udb` scoped-DB proxy auto-injects `user_id` into 14 user-owned collections (rider_profile, plan_state, ride_history, supplementary_log, training_plans, coach_chats, connected_accounts, workout_sessions, scheduled_workouts, daily_checkins, workout_prefs, cycling_activities, settings, calendar_weeks) — 82 call sites migrated. Plan DEFINITIONS (`plans`) stay global (admin-owned).
- **Green Lantern migration**: on startup, pre-existing single-user data is claimed by demo account **greenlantern@roujaune.app / rideon9900** (`user_greenlantern`, onboarded, couch-to-road). Non-destructive (runs once).
- **Onboarding**: `POST /api/onboarding/recommend` classifies Beginner/Intermediate/Advanced from a 5-question questionnaire and recommends the level's plan (couch-to-road=Beginner, build-and-climb=Intermediate; advanced falls back with `authored:false`). `POST /api/rider/plan {plan_id}` assigns + marks user onboarded; `plan_id:'none'` = ride free (`/api/plan` returns a free-riding payload). New users routed through onboarding; picking a plan/ride-free lands home.
- **Frontend**: `AuthProvider`/`useAuth` (`src/lib/auth-context.tsx`) + token in expo-secure-store/localStorage + a global `fetch` patch (`src/lib/session.ts`) attaching the Bearer to all `/api/*` calls (zero churn on existing calls). `AuthGate` in `_layout.tsx` redirects: no user→/login, !onboarded→/onboarding, else app. Screens: `login.tsx` (email/pw + Google + Apple-on-iOS), `onboarding.tsx` (questionnaire→recommendation→pick/ride-free). Profile screen shows account email/provider + **Sign out**.
- **Live-workout layout fix**: raised the horizontal fit-scale cap (2.2→4.0) so the tablet layout always fills width (removes the right-side black gap on very wide windows); kept the video 16:9 aspect compensation.
- Verified: testing agent iteration_26 — backend 14/14 pytest (auth, migration, onboarding, data isolation) + frontend 6/6 (guard, login, register→onboarding, sign-out, social buttons, workout smoke). Test users pruned; Green Lantern preserved.
- **Apple Sign In is iOS-BUILD-ONLY** — cannot be tested in web/Expo Go; requires a published iOS build. Google works on web/native.

## Ride Stronger — Intermediate default plan (Phase 1, Weeks 1–4)
- **New plan** `ride-stronger` ("Ride Stronger"), level **Intermediate**, structured, 4 weeks / 12 rides (3/week). Source of truth: `frontend/src/lib/programs/ride-stronger.ts`; rides wired into Live Workout via `workout-catalog.ts` (`RS_WORKOUTS`, ids `rs-ride-1..12`, level "Development"). Backend def: `backend/ride_stronger_plan.json` (generated by `frontend/scripts/export-ride-stronger.js`), seeded in `server.py`.
- **Plan engine generalized**: `_ctr_state/_ctr_progress/_ctr_plan_response/_ctr_calendar_week` now take optional plan context (`_struct_ctx(plan_id)`) so any structured plan renders/progresses like couch-to-road. CTR output preserved via `is_ctr` fallbacks (no regression).
- **Level tags / recommender**: couch-to-road=Beginner, ride-stronger=Intermediate, build-and-climb level **unset** (Advanced → coming-soon). `LEVEL_PLAN.Intermediate='ride-stronger'`. Intermediate onboarding now recommends Ride Stronger with `authored:true`. New intermediate riders get it on accept; existing riders (Green Lantern/beginner) unchanged. `reset_progress` now resets ride-stronger week state too.
- Non-cycling days intentionally carry only title+duration (per user). Phases 2–3 to be authored later (plan shows as 4 weeks for now).
- Verified: testing agent iteration_27 — backend 5/5 (plans list, recommend, assign+GET /plan rich response, calendar/week, couch-to-road regression) + frontend (Change Plan→Ride Stronger→Training/Calendar render→launch Ride 1 timeline; Green Lantern restored to couch-to-road). Fixed hardcoded "Beginner" level chip on the training main-workout card → now derives Beginner/Intermediate/Advanced from `workout.level`.

## Auth/Onboarding branding
- Sign-in / create-account (`login.tsx`) + onboarding (`onboarding.tsx`) now use the sunset cyclist background (`assets/images/auth_bg_sunset.png`) with a dark scrim and the ROUJAUNE logo glyph + wordmark (`auth_logo_glyph.png`, `auth_wordmark.png`, clipped to transparent) placed above the form card.

## Live Workout responsive layout (rebuilt)
- Replaced the distorting non-uniform `scaleX/scaleY` transform with a responsive flexbox layout: the route video fills remaining vertical space (`flex:1`) and the tablet view uses a `flexGrow:1` ScrollView — fills large tablets without stretching and scrolls (no compression/overlap) on shorter tablets. Verified at 1400×1000 and 1024×700.

## Ride Stronger — COMPLETE (12 weeks / 3 phases / 36 rides)
- Phase 2 (Weeks 5–8 "Strength and Sustainable Power", rides 13–24) and Phase 3 (Weeks 9–12 "Goal Ready", rides 25–36 incl. the 150-min personalised Achievement Ride) appended to programs/ride-stronger.ts. `ride_stronger_plan.json` regenerated (12 weeks). All rs-ride-1..36 auto-wired into Live Workout via RS_WORKOUTS.
- Startup now force-refreshes the ride-stronger def from the shipped JSON on every boot. FIXED a bug where `_reload_rs_from_db()` ran before the force-update and clobbered the fresh JSON with a stale DB doc — force-update now uses the module-loaded JSON, then reloads.
- Verified (testing iter27/28/29): GET /api/plans week_count 12; GET /api/plan duration_weeks 12 with 3 phases (Foundation active, Strength + Goal Ready locked), progress "1 / 12"; /plan renders all 3 phase rows; Phase-2 & Phase-3 rides (e.g. rs-ride-30, rs-ride-36) launch in Live Workout with authored interval timelines; no regression to beginner couch-to-road (16 weeks). Green Lantern remains on couch-to-road.
- Known minor (non-blocking): Recharts logs a negative-width warning on /plan Weekly Load Overview when a phase pct is 0.

## Ride Beyond — Advanced default plan (Phase 1, Weeks 1–4) + Phase-celebration + SVG fix (2026-07-24 fork)
- **New plan `ride-beyond` ("Ride Beyond"), level Advanced, structured, 4 weeks / 12 rides (3/week).** Source of truth `frontend/src/lib/programs/ride-beyond.ts` (Phase 1 "Performance Foundation" authored; Phases 2–3 append later). Rides wired into Live Workout via `workout-catalog.ts` (`RB_WORKOUTS`, ids `rb-ride-1..12`, level "Performance"; threshold/climb/tempo/recovery typing). Backend def `backend/ride_beyond_plan.json` (generated by `frontend/scripts/export-ride-beyond.js`), seeded + force-refreshed each boot in `server.py` (mirrors the ride-stronger pipeline incl. the non-clobber startup fix). `_struct_ctx`/`_on_plan_change`/`get_plan`/`get_calendar_week`/`reset_progress` all extended for ride-beyond.
- **Advanced onboarding now recommends Ride Beyond**: `LEVEL_PLAN.Advanced='ride-beyond'`, tagged level "Advanced" (build-and-climb reverts to an untagged Advanced roadmap). Advanced profiles get `authored:true`.
- **Phase-completion celebration** (`src/components/PhaseCelebrationModal.tsx` + `src/lib/phase-complete.ts` `usePhaseCelebration`): fires ONCE per completed phase (persisted per plan in AsyncStorage `phaseCelebrated:<planId>`) when a phase reaches 100% pct, using the authored `phase.complete{heading,summary,coachMessage}` from the program catalog. Animated star badge + achievement checklist + coach message; wired into `app/plan.tsx` (testID `phase-celebration-continue`).
- **Negative-width SVG warning FIXED**: clamped bar width/height to `Math.max(0, …)` in `WeeklyLoadChart` + `WorkoutProfile` (`src/components/plan.tsx`). (Chart is react-native-svg, not Recharts.)
- **`level` now returned in GET /api/plans** (`plans_admin._summary`).
- Verified: testing agent iter30 — backend ride-beyond /plan+/calendar+no-regression PASS, frontend celebration modal + no SVG warning + rb-ride-1 Live HUD PASS. Green Lantern restored to couch-to-road.
- **Deferred (user go-ahead):** "Forgot password" + email verification (Emergent Resend) deferred by user.

## Ride Beyond — FULL 12-week / 3-phase / 36-ride plan + end-of-program screens (2026-07-24 fork)
- **Ride Beyond is now COMPLETE**: Advanced, 12 weeks, 3 phases, 36 rides (`rb-ride-1..36`). Phase 1 Performance Foundation (Wk1–4), Phase 2 Power and Durability (Wk5–8), Phase 3 Performance Ready (Wk9–12, incl. personalised Achievement Ride with Full/Supported/Foundation levels). Authored in `programs/ride-beyond.ts`, exported to `ride_beyond_plan.json`, force-seeded on boot. Verified: /api/plans (12 wk), /api/plan (3 phases), progress "1 / 12".
- **End-of-program messages** added to all three plans' program catalog via new optional `Program.endMessage`: couch-to-road (16wk), ride-stronger (12wk), ride-beyond (12wk).
- **Plan-completion celebration screen**: backend `_ctr_plan_response` now exposes `plan_complete` (computed by `_plan_done` = on final week AND final week fully complete) across all three structured plans + `normalize()` carries it. Frontend `usePlanCompletion` hook + `getPlanEnd()` (in `phase-complete.ts`) fire a grand trophy modal ONCE per plan (persisted `planCompleted:<planId>`), reusing `PhaseCelebrationModal` in a `isPlanEnd` variant (trophy badge, "PROGRAMME COMPLETE", final-phase summary recap + program `endMessage` in the coach card, "Celebrate" + "Talk to coach" buttons). Wired into `app/plan.tsx`. Visually verified via front-end fetch-patch screenshot.
- Note: the per-phase celebration fires when crossing phase boundaries (phases 1→2, 2→3); the final phase never hits 100% by design, so the dedicated plan-completion screen covers program completion.

## Shareable Achievement Card + Auth (forgot-password + soft email verification) (2026-07-24/25 fork)
- **Shareable Achievement Card**: new `AchievementCard` (branded sunset card: ROUJAUNE wordmark, trophy/star badge, title, weeks/rides/hours stats, coach signature) + `ShareCardModal` (captures via `react-native-view-shot`, shares via `expo-sharing`, saves via `expo-media-library` with permission handling + Open Settings fallback). A "Share" button was added to `PhaseCelebrationModal` (both phase-milestone and plan-completion variants) and wired in `app/plan.tsx` (`buildCard` uses live plan progress). iOS `NSPhotoLibraryAddUsageDescription` added. NATIVE: full share/save sheet only works on a device build; card renders + capture verified in preview. (`react-native-view-shot`, `expo-sharing`, `expo-media-library` added.)
- **Auth — Forgot password (email link) + soft email verification** (sender "ROUJAUNE", via Emergent-managed Resend):
  - Backend `emailer.py` (managed Resend at `integrations.emergentagent.com`, key read at call-time from `EMERGENT_EMAIL_KEY`; branded HTML emails). `auth.py`: users now carry `email_verified` (surfaced in `_public_user`; OAuth/demo = true). `register` sets `email_verified=false` + best-effort verification send. New endpoints: `POST /auth/resend-verification` (auth), `GET /auth/verify-email` (HTML confirm page, flips flag), `POST /auth/forgot-password` (no enumeration; stores 60-min `reset_token`), `GET /auth/reset-password` (HTML form page), `POST /auth/reset-password` (min-6, invalidates old sessions). All added to `_PUBLIC`.
  - Frontend: `login.tsx` inline "Forgot password?" panel; `auth-context` adds `forgotPassword`/`resendVerification` + `email_verified` on `AuthUser`; new `VerifyEmailBanner` (soft, dismissible) on the dashboard.
  - **IMPORTANT — email delivery is DEPLOY-time**: the Emergent email key is injected at deploy (like push). In preview, sends return 401/502 and no email arrives — EXPECTED. All token/flag/HTML-page logic works and is tested. Tested: backend 16/16 (`tests/test_auth_verify_reset.py`), frontend login forgot panel + verify banner. Demo `greenlantern@roujaune.app` = verified (banner hidden).
  - Env added (backend/.env): `EMERGENT_EMAIL_KEY` (placeholder → real at deploy), `EMAIL_FROM_NAME=ROUJAUNE`.
- Known minor (pre-existing, non-blocking): RN-web `"shadow*" style props are deprecated → boxShadow` console warning from the global theme helper.

## Live Workout screen redesign (COMPLETE — matches user reference image)
- Rewired `frontend/app/workout.tsx` presentation layer to new premium components in `frontend/src/components/workout-live.tsx` (LiveHeader, MetricCard, ConnectionsPanel, CoachBanner, TerrainCard, IntervalTimeline, LiveControlBar). All BLE/telemetry/coach-cue/route logic and modal panels preserved.
- Layout matches reference: header (menu, logo, route+workout name, ELAPSED, progress, %, EST FINISH, LIVE/DEMO pill, Audio, Settings) → 4 metric cards (Power/HR/Cadence/Interval with target sub + status pills) → left Connections panel → center Alberto coach banner + route video → right Terrain + Route map cards → bottom "Build the rhythm" interval timeline + Next Up → control bar (Audio, Mirror, ERG +/-, Controls, Pause, End Workout). Floating media buttons removed (now in control bar).
- Tone tokens: yellow=Power/accents, red=HR/End, green=connected/on-target. Metric values show "—" until trainer/wearable connected or DEMO enabled.
- Tested: frontend testing agent iteration_33.json — 13/13 checkpoints pass. Route video shows "Video unavailable" only in headless preview (expected). Non-blocking observations: pre-existing shadow* deprecation warning; ERG optimistic state; workout.tsx length.

## Live Workout — step timeline detail + tap popup + video fallback (COMPLETE)
- Bottom "Build the rhythm" now a horizontally-scrollable StepTimeline (workout-live.tsx): each segment = a chip (step #, intensity bar, label, duration, target W). Status styling: done=green check/dim, current=yellow border+dot, future=grey outline. Header shows STEP x/y + REMAINING. Replaces the old plain bar chart + single NEXT UP card.
- Tapping any step opens StepDetailModal (testID step-detail-modal): STEP x/y badge, zone dot + title, status pill (Completed/In progress/Upcoming), stat grid (ZONE, DURATION, TARGET W, %FTP, RPE) + zone coaching tip. Managed via `stepDetail` state in workout.tsx; `stepList` memo is the single source (segments + targetWatts + mmss).
- Route video graceful fallback: RouteVideo now takes an `onError` prop (fires on invalid id or player error); workout.tsx auto-switches to the Virtual route once (with toast) so riders don't get stuck on an error card. Note: YouTube's own in-iframe "Video unavailable" in the headless preview does NOT emit an API error, so it's a preview-only artifact — real load errors fall back correctly.
- Verified via screenshots (timeline + popup). Removed now-unused workoutProfile/segmentProfile/nextInterval/nextSeg. Lint clean (only pre-existing ble exhaustive-deps warning).

## Live Workout — Workout card (replaces Interval card) + auto-scroll (COMPLETE)
- Replaced the top-right INTERVAL metric card with a richer WorkoutCard (workout-live.tsx): shows plan name (from usePlan, e.g. "FROM COUCH TO ROAD"), Phase · Week · Day chips (phase=plan.phase.name, week parsed from plan.progress.weeks, day=today's weekday), workout name, and a scrollable list of ALL steps — each with a one-line zone description (ZONE_DESC map) + duration, colored zone bar, status (done=check / current=yellow highlight+dot / future=dim). Steps are tappable and open the existing StepDetailModal.
- Layout restructured: metric row now 3 cards (Power/HR/Cadence); right column = WorkoutCard (top) with Terrain + Route moved BELOW it (per user request). rightW widened to 312. New styles mainRow/leftCenter.
- Auto-scroll: bottom StepTimeline horizontally scrolls the active chip into view; WorkoutCard list vertically scrolls current step into view — both on activeIndex change.
- Verified via screenshots: correct plan/phase/week/day, all 13 steps with descriptions, tap→detail popup works.
- Tradeoff: the tablet screen now scrolls vertically (Workout card + Terrain + Route + bottom StepTimeline + control bar exceed an 800px preview height). Bottom StepTimeline is now somewhat redundant with the WorkoutCard list — can be removed to fit one screen if desired.

## Live Workout — fixes & refinements (COMPLETE)
- Right column width bug fixed: the rightCol had `tablet && flex1` which overrode its fixed `width`, stretching it to ~half screen. Removed flex1 so it respects rightW (236). Only Terrain/Route moved down under the Workout card, as intended.
- Bottom step graphic restored to the profile-bar style (workout-live.tsx StepTimeline): duration-proportional intensity bars, one row, tappable segments (testID step-seg-N) → StepDetailModal. Enhancements: a "NOW · <step> · <one-line desc>" summary line, and a dynamic per-step fill that advances with `stepProgress` (elapsedInSeg/durationSec) on the current segment. Removed the old horizontal chip scroller (StepChip) and its auto-scroll.
- Workout card Day chip: now shows the workout's sequential DAY NUMBER within its plan via new `planDayNumber(workoutId)` in workout-catalog.ts — counts EVERY session type (strength/cycling/recovery/balance/rest) in plan order across CTR/RS/RB programs. NOTE: Welcome Ride = "Day 2" because Week-1 Monday strength is Day 1. Not tied to the calendar date anymore. Chip hidden for catalog workouts not in a plan.

## Live Workout — card slimmed + step details moved to bottom (COMPLETE)
- WorkoutCard redesigned: workout title on TOP, then plan name, Phase/Week/Day chips, and a summary description (selected.description). Removed the in-card step list → card is compact (~188px).
- Bottom StepTimeline now shows the step DETAILS: below the duration-proportional profile bars (with dynamic current-step fill), a horizontally-scrollable strip of step chips — each with step name, one-line zone description, duration and target watts; current highlighted (yellow), done = green check, future = dim; tappable → StepDetailModal; auto-scrolls current chip into view. Removed the redundant NOW summary line.
- Height trimmed (route map fill 96→52, terrain minHeight 118→100) so the full dashboard incl. Pause/End control bar fits without scrolling on standard landscape tablets (content bottom ~873px at 900-tall viewport, was ~1055px).

## Live Workout — Speed + Interval cards & dynamic Route graphic (COMPLETE)
- Top metric row now 5 cards: Power, Heart Rate, Cadence, Speed (new: telemetry.speed km/h, cyan accent), Interval (re-added: value=remaining time, sub=STEP x/y, status=segment label).
- RouteMapCard is now dynamic: route drawn as a fixed switch-back climb polyline (ROUTE_PTS); traveled portion (yellow) + a white "you" marker move along the path via routeAt(progress) — mirrors the terrain ElevationProfile. Removed the horizontal progress bar (routeTrack/Fill/Dot) per request; kept the km/pct text line. Removed now-unused Path import. Day-numbering confirmed correct by user (counts all session types; Welcome Ride = Day 2).

## Live Workout — step text overlaid on the profile bars (COMPLETE)
- Removed the separate horizontal step-detail chip strip. Step details (step # + name, one-line zone description, duration · watts) are now overlaid directly onto each profile bar (ProfileSeg), bottom-aligned with a text shadow for contrast. Bars made taller (chart 104px, bar h 40–92) and min base opacity so text is readable. Current segment keeps yellow border + dynamic left-to-right fill; bars remain tappable (step-seg-N) → StepDetailModal. Narrow (short-duration) segments truncate with ellipsis. Removed unused ScrollView import + StepDetailChip.

## Live Workout — metric reorder, min-width bars, interval remaining (COMPLETE)
- Metric row order changed to: Heart Rate, Speed, Cadence, Power, Interval.
- Interval card confirmed to show the current step's time REMAINING (mmss(activeSeg.remaining)); counts down as the workout timer runs.
- Bottom profile bars: each bar now has a MIN width (104px) so overlaid step text (name/desc/duration·W) stays readable; bars remain duration-proportional above the floor. If total width exceeds the container it scrolls horizontally, with the current step auto-scrolled into view. Bars contiguous (gap 0). ProfileSeg now takes px width; chart measures its width via onLayout.

## Coach Cue step intros + Workout Complete popup with ride extension (2026-07-25 fork)
- **Coach Cue step introductions** (`app/workout.tsx`): the companion coach now (1) INTRODUCES the first step ~2.5s after the ride opens and introduces each subsequent step the moment the rider enters it, and (2) PREVIEWS the next step as the rider nears the end of the current one (last ~15% of the step, clamped 8–20s, fired once per step). Backend `POST /api/coach/cue` gained `cue_kind` (`live`|`intro`|`next_preview`|`extend_advice`) + `next_segment`/`next_zone`/`next_target`; the prompt switches instruction by kind. Frontend `generateCue(kind)` builds the context from live refs (activeSeg/ftp/zoneBias/coachCtx) with in-persona local fallbacks. The old fixed 2.5s "first cue" timer was replaced by the intro-on-segment-enter effect (covers step 0). Verified live: intro cue shown in the coach banner ("this warm-up is your foundation… 90 rpm…"); backend curl confirmed all 3 new kinds return in-persona text.
- **"Workout Complete" popup + ride extension** (`app/workout.tsx`): when the ride's elapsed reaches the sum of all segment durations, a popup fires ONCE (guard resets on extension). It shows a green checkmark, "Workout Complete", the coach's contextual advice on whether to extend (fetched via `cue_kind=extend_advice`, loading spinner until it arrives), EXTEND chips (+10 min / +20 min / +5 km), and a primary "Finish & Save" (→ `/summary`). Extending appends an easy Z2 endurance block (`workout-catalog.extensionSegment`) to `segments` (base = `buildSegments`, plus `extraSegments` state), so the timeline/target/progress continue seamlessly and `totalSec` now derives from the live segment total. Verified via forced-state screenshot (popup UI + advice + chips + finish button render correctly). NOTE: the natural time-based completion trigger can only be reached by riding the full session in real time (sim runs ~real-time), so it is best validated manually / on device; the popup UI, handlers, and advice fetch are verified.

## Smart ride extension + summary-as-popup + prominent Save & Exit (2026-07-25 fork)
- **Smart, coached extension**: new backend `POST /api/coach/extend-advice` returns `{advice, recommend: extend|finish, suggested: 10min|20min|5km|null}`. A rule engine (`_extend_decision`) weighs workout type (hard/steady/easy), HR vs effort (when a wearable is on), and time ridden to decide whether extending is wise and the best amount; the LLM advice text is generated ALIGNED to that decision (in-persona, with rule-based fallback). Frontend `fetchExtendPlan` (`src/lib/coach.ts`). In the Workout Complete popup: when `recommend==="finish"` the extend options are HIDDEN and a green "coach recommends finishing/recovering" note shows; when `extend`, the chips show and the coach's `suggested` option is highlighted with a "COACH PICK" badge + accent border (`ExtendChip`). Primary button relabeled from "Finish & Save" → **"OK"** (still → `/summary`). Verified via curl (vo2+HR170→finish; endurance+HR125→extend/5km).
- **Summary is now a pop-up modal** (`app/summary.tsx`): the `/summary` "Workout Complete" results page was converted from a full-screen (sidebar + header + content) layout into a centered modal card over a dimmed backdrop (`styles.backdrop`/`modalCard`) with a close ✕ (→ home), a scrollable body (sidebar removed), and the action bar pinned inside the card footer. All existing content (hero recap, metrics grid, compliance, interval targets, charts, sync/export, route/achievements/recovery, manual-entry fallback) preserved and responsive. Verified via screenshot.
- **Save & Exit prominent** (`src/components/summary.tsx` `BottomActionBar`): the SAVE & EXIT action is now the bright green primary button (larger flex 1.5, filled icon, bold, glow) instead of a dark secondary button. Verified via screenshot.

## Extension crediting + removed "Plan Tomorrow's Ride" (2026-07-25 fork)
- **Extension is credited automatically + coach is told**: `rideRecorder` now tracks `extendedMin`/`extensions` (`addExtension(min)`, reset on new ride); `app/workout.tsx` `onExtendRide` records each added block (toast: "…added to your ride"). Extra volume already flows into the summary aggregates (TSS/duration/distance are computed from telemetry recorded across the FULL ride incl. the extension, so weekly load in `ride_history` credits it). Now it's ALSO explicit to the coach: `useCoachDebrief` sends `extended_min`; backend `CoachDebriefRequest.extended_min` + prompt make the debrief acknowledge/credit the extra volume (verified: "sixty minutes was the plan but you gave me eighty… that extra volume is real").
- **Removed "Plan Tomorrow's Ride"** action from the summary `BottomActionBar` (`src/components/summary.tsx`) + its `onPlan` prop/caller. Action bar is now VIEW FULL ANALYSIS / SAVE & EXIT (prominent green) / SHARE RIDE. Verified via screenshot.

## Saved-state action bar, timeline timing relocation, Full Ride Analysis modal (2026-07-25 fork)
- **Saved-state action bar** (`useSummary` now returns `saved`, set true after any successful `/api/workouts/summarize`): the summary `BottomActionBar` shows a green "✓ WORKOUT SAVED" pill and the primary green button switches from "SAVE & EXIT" → **"EXIT"** (exit-outline icon) once the ride is saved (auto for telemetry rides, or after the manual "Save Workout"). Verified via screenshot.
- **Live-workout timing moved into the timeline header** (per user diagram): removed the standalone top `LiveHeader` bar from `app/workout.tsx`; `StepTimeline` (`workout-live.tsx`) now accepts `elapsed`/`progress`/`estFinish` and renders ELAPSED · N% complete · EST. FINISH right next to the workout title, with STEP x/N and the step "… REMAINING" pill on the right. Frees vertical space for the video. Verified via screenshot.
- **Full Ride Analysis modal** (`FullAnalysisModal` in `app/summary.tsx`): the summary's "VIEW FULL ANALYSIS" button now opens a centered modal (instead of a toast) with the full metrics grid, full-width Power curve (target vs actual) + Heart-Rate curve, Time-in-Zones, and the lap-by-lap `IntervalTargetsCard` — reusing existing summary components. Verified via screenshot.
- **Removed "Plan Tomorrow's Ride"** from `BottomActionBar` (earlier this fork).

## Prominent lap timer, lap-split table, "Workout Summary" rename (2026-07-25 fork)
- **Live-workout timeline header redesign** (`workout-live.tsx` StepTimeline): ELAPSED/EST-FINISH moved to the RIGHT (giving the workout title room on the left); the current step's countdown is now a large, prominent centered "lap timer" (32px yellow, "REMAINING · STEP x/N") filling the space between the title and the right-hand stats; a full-width overall PROGRESS bar (+%) sits beneath the header row. Verified via screenshot.
- **Lap Splits table** in the Full Ride Analysis modal (`app/summary.tsx`): beneath the power/HR curves, a per-interval table (LAP name+zone dot / TIME / AVG W / % FTP / AVG HR). `computeIntervals` (`summary.ts`) now also computes per-segment `avgHr` and returns `ftp`; %FTP = avgW/ftp. Verified via screenshot (13 laps listed; W/HR show "—" with no telemetry).
- **Renamed summary header "Workout Complete" → "Workout Summary"** (`src/components/summary.tsx` SummaryHeader). The end-of-ride celebration popup in `workout.tsx` keeps its "Workout Complete" title.

## Lap-split compliance indicator (2026-07-25 fork)
- Each Lap Splits row now shows a color-coded compliance indicator (a left accent bar + a TARGET pill with the interval's compliance %), coloured green ≥80% / amber 50–79% / red <50% (grey "—" when no telemetry for that lap). Added a legend ("On target / Slipping / Faded") and the header hint now reads "Where did you fade?". Uses the existing per-interval `compliance` from `computeIntervals`. Verified via screenshot (colors populate on rides with recorded telemetry; grey in the no-telemetry preview).

## Show/hide password on sign-in (2026-07-25 fork)
- Added an eye toggle inside the password field on the auth screen (`app/login.tsx`): tapping switches `secureTextEntry` and the eye/eye-off icon. Works for both sign-in and create-account modes. (Also fixed a pre-existing unescaped apostrophe in the reset-link hint.) Verified via screenshot.

## Active Controls panel + logo glyph, show/hide password (2026-07-25 fork)
- **Controls panel is now fully functional** (`app/workout.tsx` `onControlAction`, switched from label→key): Skip Interval (`sendInit` to end of current segment → next step), Extend Recovery (append 3-min easy `extensionSegment`), Reduce/Increase Intensity (`onErg` ∓5%), Toggle ERG Mode (new `ergMode` state/ref — when OFF the per-segment `sendTarget` is suppressed so the rider controls effort), Camera Selection (`setShowRoutes`), Mute Alberto (`toggleVoice`), Trainer Reconnect (`simulateDropout`), Touch Lock (`setLocked`), Peaceful Pause (pauses ride). Each shows a confirming toast. Verified via screenshot.
- **Logo glyph added above the ROUJAUNE wordmark** in the workout BrandCard (`workout-live.tsx`, `auth_logo_glyph.png` stacked over `auth_wordmark.png`). Verified via screenshot.

## Adjustments history strip (2026-07-25 fork)
- Added an `AdjustmentsStrip` (`workout-live.tsx`) shown between the timeline and the LiveControlBar on the workout screen: a horizontal, timestamped history of the last (up to 8) ride-affecting adjustments — ERG ±%, Skip interval, +3:00 recovery, ERG mode ON/OFF, Peaceful pause, Pause/Resume. `app/workout.tsx` keeps a `controlLog` state and a `logControl(label)` helper (timestamps via `mmss(telemetry.elapsed)`) called from `onErg`, `onPauseToggle` and `onControlAction`. Only renders once at least one adjustment is made. Verified via screenshot (Skip → STEP advanced; ERG changes logged with times).

## Time-based duration metrics + adjustments carried into summary (2026-07-25 fork)
- **Time-based ride metrics** (`app/workout.tsx`): when NO live sensors are connected (`timeBased = !trainerOn && !wearableOn`), the top metric row now shows duration metrics instead of blank telemetry cards — ELAPSED (value + "TOTAL SESSION mm:ss"), INTERVAL (current block remaining), CALORIES (kJ≈kcal estimate integrated from planned target watts over elapsed), WORKOUT STEP (Step X of N). When a trainer/wearable IS connected it shows the original HR/Speed/Cadence/Power cards. Matches the user's mockup. Verified via screenshot.
- **Adjustments carried into the ride summary**: `rideRecorder` now stores `adjustments[{t,label}]` (`addAdjustment`, reset-cleared); `logControl` records each mid-ride change there too. `useSummary` exposes `adjustments`; the Full Ride Analysis modal renders a "Ride Adjustments" list (time · action). The debrief request sends `adjustments` and the backend (`CoachDebriefRequest.adjustments` + prompt) references the most relevant one to explain data anomalies (verified via curl: coach mentioned the ERG bump). 

## Accurate power-based calories + carried into saved summary (2026-07-25 fork)
- **Calorie estimate now integrates ACTUAL watts when a power meter is streaming** (`app/workout.tsx`): a running energy accumulator (`energyRef`, joules) sums `power × dt` from trainer telemetry; the live `kcal` memo uses that when available, else integrates planned target watts (time-based). Both use `kJ × 0.7` (≈24% efficiency) — the SAME formula as the backend telemetry summary — so the live number matches the saved ride.
- **Carried into the saved summary**: `rideRecorder.estCalories` (`setEstCalories`, reset-cleared) holds the live figure; `useSummary` posts `est_calories`. Backend `SummarizeRequest.est_calories` overrides calories in the no-telemetry REFERENCE fallback and the manual RPE/empty paths, so a time-based ride's saved calories equal what was shown live (power rides keep their accurate sample-derived calories). Verified via curl (est_calories=145 → saved calories 145).

## Weekly energy rollup + streak on home dashboard (2026-07-25 fork)
- **Backend** `GET /api/stats/energy` aggregates `ride_history` calories → `{today_kcal, week_kcal (ISO week, Mon-start), week_rides, streak_days}`; streak = consecutive days (ending today, or yesterday if none yet today) with ≥1 ride. `_save_ride_history` now stores `calories` (from summary/est_calories) so the rollup has data. (Note: ride_history is not user-scoped, matching existing `/rides/history`.)
- **Frontend**: `MetricSummaryStrip` gained a `useEnergyRollup()` fetch + a "WEEKLY ENERGY" cell (flame icon) showing `week_kcal kcal` with a "{n}-day streak" / "{today} kcal today" subtitle. Global fetch auto-injects the bearer token, so the plain fetch is authenticated. Verified via curl (1,040 kcal, streak 1) and screenshot on the dashboard.

## Virtual Routes MVP (2026-07-25 fork)
- New immersive **Virtual Routes** feature at `app/virtual-route.tsx` (nav "Virtual Routes" now points here). Layered 2.5D approach: the supplied rear-view Roujaune rider plates are the hero focal point; the world is animated around/beneath via a rAF phase loop in `src/components/virtual-route/scene.tsx` (camera bob synced to cadence, bike lean from route curvature, effort scale from power, scrolling road markers + glowing edge streaks + rotating wheel/drivetrain SVG from speed, motion-blur haze, vignette, dynamic floor glow). Brand lockup (glyph+wordmark) preserved upper-left; no telemetry baked into artwork.
- 4 rider profiles (`src/lib/virtual-riders.ts` — vr_rider_male/female/mature_male/mature_female.png in assets/images) selectable in a setup overlay; selection persists through the ride.
- Route engine `src/lib/vroutes.ts` (Alpine Sunset Pass, 20km) with per-distance gradient/curvature/checkpoints; drives resistance, lean, progress. Distance integrated locally from speed.
- Telemetry reuses existing `useTelemetry` (live + backend simulation); EMA smoothing prevents jitter. Connection states derived (Connecting/Signal lost/Reconnecting/Disconnected/Simulated/Connected). Auto trainer resistance follows gradient (clamped 60–140% ERG), toggle + **Emergency Stop Resistance**. Sim presets (Recovery/Endurance/Tempo/Climb via sendTarget), signal-drop test (simulateDropout).
- Telemetry panels: **all / minimal / hidden** cycle; reduced-motion toggle; accessibility labels. Start/Pause/Resume/End controls. Widescreen landscape. Verified via screenshots (setup + riding).
- NOTE: photo plates are baked full scenes, so anatomical per-limb pedalling isn't rigged — motion is conveyed via camera bob/lean/world-scroll + a cadence/speed drivetrain indicator (honest MVP; per-part rider sprites would be a future enhancement). Web Bluetooth/native BLE per-sensor connect is stubbed through the existing modular telemetry layer.

## Virtual Routes — testing + fixes (2026-07-25 fork)
- Frontend testing agent (iteration_34): ALL 9 Virtual Routes flows PASS (rider select, start, full/min/hidden panel cycle, pause/resume, end→dashboard, sim presets, auto-resistance, emergency stop, reduced-motion, signal-drop). 
- Fixes applied: (1) End Ride now uses `router.canGoBack() ? back() : replace('/')` (safe nav). (2) In-scene brand lockup hidden during setup (avoids overlap with app-shell wordmark). (3) IMPORTANT — corrected rider id→image mapping (downloaded files were mislabeled). Verified via analyze_file_tool: male→vr_rider_mature_female.png (young male, black/burgundy), female→vr_rider_mature_male.png (young female, pink), mature_male→vr_rider_female.png (yellow), mature_female→vr_rider_male.png (light pink). Confirmed via screenshot: selecting Mature Male shows the correct yellow & burgundy rider.

## Configurable Rider, Bicycle & Clothing System — Phase 1 (2026-07-26 fork) — COMPLETE
- New dedicated **Rider Customisation screen** `frontend/app/rider-customise.tsx` (Expo Router route `/rider-customise`). Three pickers: Choose Your Rider (4 identities: younger_male/younger_female/mature_male/mature_female), Choose Your Bike (road/mountain/vintage), Choose Your Riding Style (pro/get_fit/casual). Live preview (rider sprite + bike/style chips), action bar Reset (to default) / Restore (last saved) / Save / Continue.
- Virtual Routes setup screen (`virtual-route.tsx`) now shows route selection + a **"YOUR RIDER" summary card** with a Customise button (testID `vr-customise-rider`) that opens the screen. Old inline 4-rider grid removed. Appearance loaded on mount + on focus via `useFocusEffect(loadAppearance)` so changes reflect immediately and **persist until the user changes them again**.
- Config lib `src/lib/rider-config.ts`: enums, DEFAULT_APPEARANCE (younger_male/road/get_fit), per-bike geometry (wheel circumference etc.), `loadAppearance`/`saveAppearance` (backend-first, AsyncStorage cache). `src/lib/virtual-riders.ts` adds `riderVisualFor(riderType)` mapping identity→artboard/sprite. `scene.tsx` accepts `appearance` and feeds riderType/bikeType/clothingStyle into `RiveRider` (native Rive data-binding; web/Expo Go = sprite fallback, expected).
- Backend `GET/PUT /api/rider/appearance` (collection `rider_appearance`, doc id "me") persists riderType/bikeType/clothingStyle; partial PUT updates only provided fields. Verified: backend 8/8 pytest (`backend/tests/test_rider_appearance.py`) + all 7 frontend acceptance flows (iteration_36) PASS. Small-button hitSlop added for mobile touch targets.
- NEXT: Phase 2 = actual Rive artboard/data-binding variants per bike + clothing (needs production `roujaune-riders.riv`; currently placeholder/fallback). Phase 3 = mountain/vintage variants + route compatibility rules/warnings.

## Live Workout — embedded immersive Virtual Ride player (2026-07-26 fork) — COMPLETE
- New shared component `frontend/src/components/virtual-route/VirtualRidePlayer.tsx` (modes `embedded` | `fullscreen`) composed from the existing `VirtualRouteScene` + `vroutes` (`routeStateAt`/`routeTerrainBias`) + `RouteProfile`. NOT a separate route implementation.
- Live Workout (`app/workout.tsx`) center now renders the immersive Virtual Ride (backdrop + rider) driven by the live workout telemetry + the selected scenic route's gradient at current progress. Old video player / old `VirtualRoute` / `ImmersiveHud` / video `RoutePicker` REMOVED. Route source switched from `routeVideos` → `VIRTUAL_ROUTES` (vroutes.ts). Auto-matches route to workout type (`vrouteIdForType`) with an in-screen picker (Auto-match / Shuffle / list; testIDs vroute-picker-*).
- Embedded overlay is MINIMAL only: route-name pill (opens picker), current gradient pill, camera/motion toggle, fullscreen button (testIDs vr-embed-route/view/fullscreen). Surrounding dashboard (metrics/coach/terrain/timeline/controls) unchanged.
- Fullscreen (`vr-fullscreen`): full HUD — 8 telemetry cells, elevation RouteProfile, progress, interval chip, coaching cue, Pause/Resume + Exit (vr-exit-fullscreen) + ERG presets (Recovery/Endurance/Tempo/Climb → sendTarget) + ERG toggle + sim reconnect. One shared state (props derived from the single ongoing workout) drives both views → perfectly synced. Verified elapsed/distance keep advancing across enter/exit fullscreen (no reset). Rider appearance loaded from /api/rider/appearance.
- Verified: testing_agent iteration_37 — all 6 flows PASS, state preservation confirmed, no runtime errors (only pre-existing RN-Web deprecation warnings). Rive native anim = native-build only (web = sprite fallback, expected).

## Live Workout / Virtual Ride UI polish (2026-07-26 fork) — COMPLETE
- Emergent-managed Google Sign-In: confirmed already fully integrated (frontend redirect + session_id parse, SecureStore/localStorage token, backend /api/auth/google session-data exchange). Added mobile cold-start deep-link fallback in auth-context.tsx (Linking.getInitialURL + addEventListener('url')). Web unchanged.
- Rider-on-road realism: added per-route `riderAlign` ({x,bottom,top,scale}) to VRoute (vroutes.ts), applied in `VirtualRouteScene` (left/right % offset + extra scale transform). Tuned coastal-sprint (x -0.035, scale 0.9) and forest-loop (x -0.03, scale 0.95); alpine/desert/city-night/rainy-cobbles use centered default (roads are centred). Passed through embedded+fullscreen player and standalone virtual-route screen.
- Fullscreen Virtual Ride HUD redesigned for small tablets: telemetry now a VERTICAL LEFT RAIL (ScrollView) stacking Power→Cadence→Speed→HR→Gradient→Load→Distance→Elapsed, with brand (glyph+wordmark) SCALED to rail width (responsive via useWindowDimensions; smaller on width<1000). Route header+progress+elevation at top of rail. Coaching cue + interval chip + camera/motion toggle moved to top-right.
- Fullscreen bottom control bar now MATCHES the Live Workout LiveControlBar style, pinned across the bottom: presets (Recovery/Endurance/Tempo/Climb) + ERG + Reconnect on left, Pause/Resume + Exit on right (testIDs vr-fs-pause, vr-exit-fullscreen). Scene brand disabled in fullscreen (rail brand used instead).
- Fixed LiveControlBar overflow on small tablets (workout-live.tsx): bar now wraps (flexWrap) with a flexible `group` (secondary controls, wraps) + fixed `primary` group (Pause/End) so Pause/End are always on-screen. Verified at 900x600: Pause 660-777, End 785-879.
- Verified visually at 900x600 + 1024x768: dashboard, wrapped control bar, fullscreen rail, route picker, and standalone virtual-route screen — no regressions.

## Unified fullscreen + STAGES + refactor + personalisation (2026-07-26 fork) — COMPLETE
- UNIFIED FULLSCREEN: standalone Virtual Routes ride (`app/virtual-route.tsx`) now renders the SAME `VirtualRidePlayer` (mode="fullscreen") as Live Workout — identical HUD/layout/controls across both. Player extended with context-aware optional controls (exitLabel/exitIcon, onSensors/sensorsOn, onEmergency, connLabel/connTone) so Virtual Routes keeps End Ride + Bluetooth Sensors + Emergency Stop + auto-resistance, while Live Workout shows Exit. Removed virtual-route's old TelemetryPanel/MiniStat/CtrlBtn/PRESETS/panel-cycle.
- VERTICAL STAGES: added a stacked "STAGES" progression section to the rail HUD (prop `stages: {label,sub,state}[]`). Live Workout passes workout intervals (buildSegments, active highlighted); Virtual Routes passes route checkpoints (done/active/upcoming by distance).
- REFACTOR: extracted `src/lib/workout-vroute.ts` (TYPE_VROUTE, vrouteIdForType, deriveVirtualRide) and `src/components/workout/VRoutePicker.tsx` from workout.tsx.
- PERSONALISATION: favourite scenic route pinned PER workout type (prefs.ts getFavoriteRoute/setFavoriteRoute, AsyncStorage) — pin star in VRoutePicker (vroute-pin-<id>); auto-loads on entering that workout type; embedded overlay shows an auto-match/"Your pick" badge (routeBadge prop).
- RN-WEB DEPRECATION CLEANUP: migrated all app-level raw shadow*/textShadow* to theme helpers (shadow.card / textShadow()) + Platform.select boxShadow (ShareCardModal); converted all `pointerEvents=` props to `style.pointerEvents` across scene/VirtualRidePlayer/workout-live/virtual-route. No raw deprecated props remain in app code.
- RIDER GROUNDING: per-route riderAlign {x,bottom,top,scale} + backdrop zoom (bgScale/bgShiftY) in VRoute. Coastal/Forest/Rainy-Cobbles dropped low (bottom -6) + bg zoom so wheels sit on road; Alpine/Desert/City-Night use centered default.
- Emergent Google Sign-In: confirmed integrated; added mobile cold-start deep-link fallback (auth-context.tsx).
- Verified: testing_agent iteration_38 — all 5 flows PASS at 1280x800 + 900x600, no runtime errors, state preserved on fullscreen enter/exit.

## Route PR tracker + control-bar scaling + exit button + workout.tsx modal split (2026-07-26 fork)
- **Best Time / PR tracker per scenic route (+ per-segment)** — user choice 1c (track BOTH fastest time = primary PR and highest avg power = secondary badge, plus per-checkpoint split PRs).
  - Backend: new user-scoped `rider_prs` collection (auth.py USER_SCOPED) + endpoints `POST /api/rider/prs` (compare {route_id,route_name,time_sec,avg_power,completed,splits[{label,km,time_sec}]} vs stored bests, update beaten, return `records:{route_time,route_power,segments[],first_time}`), `GET /api/rider/prs` (list), `GET /api/rider/prs/{route_id}` (single summary). First completion is NOT a beaten record (first_time=true); `completed=false` never sets route_time but still compares segment splits. Verified: testing_agent iteration_39 — 9/9 pytest pass incl. user-scoping isolation; GL rider_prs cleaned to empty.
  - Frontend: `src/lib/pr-tracker.ts` (module store: reset/mark/submit + fetchRoutePR + prToastMessages + fmtPRTime). Live Workout (`app/workout.tsx`) marks checkpoint splits from route progress, submits on natural completion AND manual Save Ride, shows staggered celebration toasts. Virtual Routes (`app/virtual-route.tsx`) resets on start, marks during ride, submits on End Ride, and shows a trophy PR banner (testID `vr-pr-banner`) in the Ride Summary card.
- **UI fix — LiveControlBar responsive scaling** (`src/components/workout-live.tsx`): the bottom control bar now scales down via `useWindowDimensions` (compact <1180, tiny <900) — smaller button heights/icons/fonts + reduced `group` minWidth — so on small screens it no longer overflows and Pause/End never wrap to a second line. Verified via screenshot at 860px (Pause/End same row).
- **UI fix — Virtual Routes fullscreen "End Ride" exit button doubled** (`VirtualRidePlayer.tsx` `exitBtn`/`exitIcon`): height 72→88, padding 26→44, icon 26→40, text 20→30. Verified via screenshot.
- **workout.tsx modal split**: extracted `Toast`, `CompletePrompt` (incl. ExtendChip) and `EndPrompt` (+ their styles) into `src/components/workout/WorkoutModals.tsx`. workout.tsx 1157→1060 lines; removed now-unused imports (Animated/Platform/ActivityIndicator/expo-image Image) and modal-only styles. Verified: EndPrompt renders from the extracted component via screenshot.
- DEFERRED (optional, higher-risk refactor): extracting a `useLiveTelemetry` hook from workout.tsx (ERG/sensor-bridge/energy). Left in place to avoid regressions on the working live screen.

## Home-screen revamp + one-line responsive control bars + VR End-Ride popup (2026-07-26 fork, batch 2)
- **Control bars now stay on a SINGLE line and scale down on small screens** (per product direction: small fonts/graphics are the accepted trade-off on small tablets, e.g. Galaxy Z Fold 6 landscape ~884dp → "tiny" tier).
  - `VirtualRidePlayer.tsx` fullscreen bar: secondary controls in a horizontal ScrollView (`barScroll`), Pause + End Ride pinned right (flexWrap:"nowrap"); responsive size tiers compactBar(<1180)/tinyBar(<900) for round/pause/exit heights, icons, fonts, padding. HUD rail metric value/icon + top-right toggle also scale via smallTablet. Exit "End Ride" button is large on big screens, scaled down on small.
  - `workout-live.tsx` LiveControlBar: same treatment — `group` is now a horizontal ScrollView, `primary` (Pause/End) pinned; bar flexWrap:"nowrap". Sizes scale via width<1180/<900.
- **Virtual Routes End Ride now uses the Live Workout confirm popup** (shared `EndPrompt` from WorkoutModals): tapping End Ride → "End this ride?" Save Ride / Abandon Ride / Resume. Save → persists to history + submits PR + shows a read-only "Ride Saved" results card (metrics + PR banner) with a single Done button (`vr-summary-done`). Abandon → back to setup. Resume → keep riding. (Removed the old auto-summary + Save/Discard buttons and the old `saveRide`/`endRide`.)
- **Virtual Routes route picker PR chip**: each route option shows a "🏆 m:ss" best-time chip (`route-pr-<id>`) from `fetchAllRoutePRs()` so riders see the record to chase.
- **Home screen (`app/index.tsx`)**:
  - Training Plan card (`TrainingPlanCard.tsx`): phase now prefixed with number → "Phase N · <name> · <weeks>" (from active `plan.phases`); "NEXT WORKOUTS" shows the primary next workout + up to 2 more upcoming (title · duration · TSS rows).
  - Today card (`CalendarCard.tsx`): renamed "TODAY'S PLAN" → "TODAY'S TRAINING"; adds a consolidated detail block for today's main cycling session (title, duration, zone, TSS, subtitle) + an "Open Today's Training" button (`open-today-training`) → `/training` (via new `onOpenToday` prop).
  - Removed `FeaturedRouteCard`; moved `ProgressCard` into the mid row in its place. ProgressCard enriched (Weeks/Workouts/Time/TSS stat grid + CTL/ATL/TSB + line chart) with a "View Progress" button (`view-progress-button`) → `/progress` (new optional `onPress` prop). Bottom row is now Community / Wellness / Achievement.
- Verified via screenshots at 884/1180/1280 widths: single-line scaled control bars (Fold 6), End Ride popup + Ride Saved results, home cards + links. Test rider_prs/VR history cleaned so Green Lantern stays pristine.

## Plan-screen layout polish + Today's Training steps + Training-plan next-workouts (2026-07-26 fork, batch 3)
- **Home Training Plan card** (`TrainingPlanCard.tsx`): "NEXT WORKOUTS" shows the primary next workout + up to 2 more upcoming (title · duration · TSS). Reported "rows appear then disappear on all devices" — INDEPENDENTLY verified by testing_agent (iteration N): rows render and PERSIST with zero console errors, not reproducible in web preview (data stable: couch-to-road = 3 workouts, none completed). Applied defensive React key `${w.id}-${idx}` in the map as hardening against any duplicate-id data.
- **Home Today's Training card** (`CalendarCard.tsx`): renamed TODAY'S PLAN → TODAY'S TRAINING; shows consolidated detail (title/duration/zone/TSS/subtitle) and now a **vertical textual WORKOUT STEPS list** (testID `today-training-steps`) — each step's name (e.g. "Settle In", "Build the Rhythm", "Easy Finish") + minutes, derived via `buildSegments(getWorkout(session.workout_id))` from the couch-to-road program intervals. (Replaced the earlier bar-graphic per user feedback; step names wrap to 2 lines so nothing truncates.) Plus "Open Today's Training" button → /training.
- **Plan screen** (`app/plan.tsx` + `src/components/plan.tsx`):
  - Plan Goals moved UNDER the Plan Hero card (left column `heroCol`), freeing width for the "This Week's Plan" calendar.
  - Plan Goals now render HORIZONTALLY as chips (dropped the bicycle icon; status tick sits on the same line as the title; full title on one line via adjustsFontSizeToFit). The "Edit Goals" button is now a compact inline **Edit** pill on the same row as the goal chips.
  - Rebalanced column widths (heroCol flex 1.4 / heroCalendar flex 0.82) so the calendar shrinks a bit and goals fit on one line.
  - Plan Progress strip moved ABOVE the Current Phase roadmap on the Overview tab.
- **Calendar date-alignment BUG FIX** (`CalendarCard.tsx` + `ui.tsx Touchable`): date numbers didn't line up with the MON–SUN day headers because `Touchable` applied `flex:1` to its INNER view while the grid's real children were the outer Pressables (no flex) → columns packed to content width. Fixed by passing the cell style via `containerStyle` so each day Pressable is `flex:1`, matching the header columns.

## Cold-start 401 fix + hero "today" logic + Today's Training dedupe (2026-07-26 fork, batch 4)
- **ROOT-CAUSE FIX for "next workouts show then disappear" (all devices)**: `src/lib/session.ts` fetch wrapper now AWAITS token hydration (`ensureToken()` + `_hydrated` flag; memoised) before firing any `/api/*` request. Previously authenticated calls could fire before the stored token loaded on cold-start → 401 → `usePlan` fell back to bundled static plan → upcoming-workouts list flipped/vanished. Verified via console logs (no post-login 401 on /api/plan) and rows persist.
- **Home hero (`AlbertoCoachCard.tsx`)** now reflects TODAY's actual scheduled activity from `useCalendarWeek` (any type): a ride shows "Today · <dur> · <zone>"; fb50 → "Strength"; wellness → "Recovery"; a rest day shows "Rest Day" + "Your next workout is <title> · <dur>". Button label is now "View Today's Workout" (or "View Today's Plan" on a rest day) instead of the static "Start Today's Ride". Still routes to /training.
- **Home Today's Training card (`CalendarCard.tsx`)**: removed the redundant cycling session row at the bottom (it duplicated the Welcome Ride title+duration already shown in the detail headline + WORKOUT STEPS). Today-scope now lists only supplementary sessions (fb50/wellness) and only renders that list when non-empty.

## Today-screen overhaul + Progress timeline + hero rework (2026-07-26 fork, batch 5)
### P0 — "Next Workouts" persistence (real root cause)
- The recurring "rows disappear" bug was NOT the auth race — the backend `_ctr_plan_response` only emitted the CURRENT week's rides (often 0–1 incomplete), so once live data replaced the 4-item bundled fallback the list collapsed. Fixed: the builder now emits ALL day types (cycling, recovery, rest, strength/mobility/balance) in chronological order across whole weeks until ≥5 incomplete entries exist. `TrainingPlanCard` shows the next + up to 4 upcoming rows, each with a type-aware icon/color and short meta label. Verified with Green Lantern (couch-to-road).
### Training Plan card shows all workout types
- Backend workout entries now carry `type`, `subtitle` (non-cycling), `icon`, `color`, plus `date`, `date_label` ("Mon 27 Jul") and `is_today`. Completion: cycling by ride_id, rest by past-date, recovery/supplementary by supp_dates.
### Progress timeline (NEW)
- Backend `GET /api/progress/timeline?range=week|month|3m|6m|1y&offset=N` (auth) buckets real `ride_history` (bucket counts 7/4/13/6/12), returns window_label, buckets[{label,tss,hours,rides}], summary{rides,hours,tss,distance_km,elevation_m,avg_power,tss_delta_pct}, has_next (offset>0). Frontend `src/lib/progress-timeline.ts` + a Training Timeline card on `/progress` (range chips, prev/next date scroll, TSS bar chart, summary, empty-state). Default range = 3 Months.
### Hero + Today layout rework
- Nav label "Home" → "Today".
- Hero coach card (`AlbertoCoachCard`) now uses the plan's first-incomplete workout of ANY type (so Green Lantern's next = "Beginner Cycling Strength · Mon 27 Jul · Strength", not the next ride), shows a phase/week kicker from a new backend `plan.hero` object ({week, phase_number, phase_name, week_in_phase, is_phase_start} → "START OF PHASE 1 · GET MOVING" or "PHASE n · <name> · WEEK w"), and gained Message + View Calendar buttons. Card is narrower.
- Hero weather is now LIVE & LOCAL: `src/lib/weather.ts` `useWeather()` uses expo-location GPS → Open-Meteo, falling back to a Settings home location (`homeCity/homeLat/homeLon`, default Nice, France) when GPS is unavailable (indoors), plus today's date. Location perms added to app.json.
- `TodayTrainingCard` extracted from CalendarCard and placed to the RIGHT of the hero (hero width ~0.64 of main). Calendar card REMOVED from the Today screen (still reachable via the hero "Calendar" button + nav). Achievements is a standalone card again next to Progress (undid the earlier merge).
### Coach persistence (server-side, per user)
- New user-scoped `rider_prefs` collection + `GET/PUT /api/rider/prefs` ({coach_id, coach_style, voice_guidance, speech_rate}). `coach-persona.ts` reconciles the local AsyncStorage cache with the server on load and PUTs on change, so the chosen companion coach is consistent across sessions/devices. Verified E2E.
### Misc
- Virtual Routes setup Exit button (`vr-exit`) enlarged into a prominent bordered pill.

### Phase 2 (Training Plan deep-dives) — DONE (2026-07-26):
- Phases: the Phases tab renders `PhasesDetailCard` (all phases with objectives + progress, current highlighted with a CURRENT chip); tapping a phase (roadmap card or detail row) opens `PhaseDetailModal`. Backend `plan.phases[]` now carry `objective`.
- Key Workouts: tapping a workout opens `KeyWorkoutDetailModal` (zone/duration/TSS + WorkoutProfile + WORKOUT STEPS via buildSegments + "Open in Training"), instead of navigating away. Grid filtered to cycling entries.
- Weekly Load: `WeeklyLoadCard` gained a stats row (This Week / Avg per Week / Peak week / Completed %).
- Adaptations: NEW `POST /api/coach/adaptation/detail` (Claude, cached per plan+coach, grounded in the computed plan for structured plans) → {summary, factors[], adjustments[]}. `AdaptationsModal` shows a "WHY <coach> ADJUSTED YOUR PLAN" reasoning block above the history timeline.
- Also: `rider_appearance` is now user-scoped (added to USER_SCOPED in auth.py).

## Full preference persistence + post-ride adaptation nudge (2026-07-26, batch 6)
- **All rider preferences persist server-side (cross-device)** with an AsyncStorage cache:
  - NEW user-scoped `GET/PUT /api/rider/settings` (`settings` collection) stores trainer/wearable/demo/HUD/FTP/ftpAuto/seatedMode + home location. `useSettings` loads local → merges remote → writes both on change (and still auto-syncs FTP from progress).
  - Coaching prefs (`coach_style`, `voice_guidance`, `speech_rate`) now write-through to `/api/rider/prefs`; hydrated on startup via `coach-persona.initCoach → hydrateRiderPrefs()`. (Coach persona already persisted from batch-5 Phase 3.)
- **Adaptation auto-refresh after each ride + nudge:**
  - `POST /api/coach/debrief` background task now regenerates BOTH the short adaptation note AND the detailed reasoning cache (`adaptation_detail_<coach>`).
  - `GET /api/plan` surfaces `adaptation_ai_<coach>_at` / `adaptation_detail_<coach>_at` (via `_with_adaptation_meta`) for structured plans.
  - `PlanUpdatedNudge` (top of Today screen) shows "<coach> updated your plan" when `usePlanBadge()` detects a fresh adaptation; tap/View → /plan (marks seen); X dismisses. `plan-badge` checks both coaches' timestamps.
- **Data hygiene:** deduped `training_plans` (had duplicate `id` docs) + added a UNIQUE index on `id` so adaptation caches read/write deterministically.
- KNOWN/optional (flagged by testing): Seated Mode / Smart Trainer toggles live on the pre-ride sheet & `/connections`, not `app/settings.tsx` — persistence layer is shared, so behaviour is equivalent; could surface them on Settings later.

## Today hero/card copy + Settings consolidation (2026-07-26, batch 7)
- Hero coach CTA now reads **"View Today's Workout"** only when the next activity `is_today`; otherwise **"View Next Scheduled Workout"** (rest → "View Today's Plan" / "View Next Scheduled Day").
- **View Calendar button moved** from the hero coach card to the **Today's Training card** (testID `today-view-calendar`); the hero card now shows only "Message <coach>".
- **`TodayTrainingCard`** reworked to reflect the SAME next-scheduled activity (any type) as the hero via `usePlan()`: its title flips between **"TODAY'S TRAINING"** and **"NEXT SCHEDULED WORKOUT"** (+ day label), shows the workout/steps for rides or a type note for strength/recovery, and buttons "Open Today's Training"/"Open Workout" + Calendar.
- **Settings screen**: added an **"EQUIPMENT & RIDE"** card (testIDs tg-hasTrainer/tg-hasWearable/tg-hudEnabled/tg-seatedMode/tg-demoMode) wired to `useSettings` (server-persisted) — a single source of truth alongside the pre-ride sheet & Connections.


## BLE speed + wheel calibration (2026-07-26, batch 8)
- BLE parser (`src/lib/ble/parse.ts`): added wheel-revolution parsing to `parseCsc`/`parseCyclingPower` + `speedFromWheel()` (handles 1/2048s CPM vs 1/1024s CSC, uint32 rollover). `useBleSensors(wheelCircumferenceMm)` now isolates crank/wheel state PER device id (`crankState[id]`,`wheelState[id]`) so a standalone cadence sensor no longer corrupts a power meter, and exposes `speed` + cumulative `wheelRevs`.
- `wheelCircumference` (mm) added to Settings (`settings.ts` DEFAULTS 2105) + backend `SETTINGS_DEFAULT`; Settings "WHEEL & SPEED" card = 7 tyre presets + custom mm stepper. Speed sent via telemetry `sensor` msg (backend `sensor_speed` override); BLE panel shows SPEED (km/h↔mph per units).
- GPS auto-calibration screen `app/wheel-calibration.tsx`: expo-location Haversine distance ÷ wheel turns → exact roll-out; saves to `wheelCircumference`; result banner shows nearest tyre preset + signed delta (`nearestWheelPreset()` in settings.ts). Contextual Location permission flow. NATIVE-ONLY (BLE+GPS) — shows build notice in web/Expo Go. Pure-fn tests in `frontend/scripts/*.test.mjs`.

## Benchmark Workouts — PHASE 1 (2026-07-26, batch 9)
- **Scope:** feature built in 5 sequential phases (1 nav+foundation ✅, 2 library+detail, 3 readiness+equipment+setup, 4 player+4 core tests, 5 calc+results+confidence+history+FTP review). Confirmed decisions: reuse existing telemetry sim + BLE for player; persist sessions/results in MongoDB via new `/api/benchmark/*`; accepting an FTP result updates app FTP + disables `ftpAuto`; extra 4 tests = Sprint Power/5-min MAP/1-min Anaerobic/Threshold Hold (coming_soon); result interpretation AI-written via Claude (Emergent key).
- **Foundation (reusable, data-driven):** `src/lib/benchmark/types.ts` (BenchmarkTest, TestInterval, CalculationConfig, ReadinessOutcome, EquipmentSetup, BenchmarkSession, BenchmarkResult, etc.), `catalog.ts` (8 tests; 4 `available` w/ full interval scripts + versioned calc config, 4 `coming_soon` metadata-only; `CATEGORY_META`, `getBenchmarkTest`, `AVAILABLE_TESTS`), `api.ts` (`useBenchmarkResults` — forward-compatible, resolves [] until `/api/benchmark/results` exists).
- **Nav + route:** added "Benchmark" side-nav item (`data.ts`, icon speedometer-outline) + `benchmark:"/benchmark"` in BOTH `app-scaffold.tsx` ROUTE map AND `app/index.tsx`'s local `routes` map (Today screen uses its OWN nav handler — MUST add new routes there too, else nav silently toasts instead of navigating).
- **Landing** `app/benchmark.tsx`: coach hero w/ "not pass-or-fail" core message, empty-history state (`bm-empty`), data-driven 8-test preview grid (category chips + SOON badges). "Browse the test library" CTA is a labelled dev-state (notice: library arrives next) — Phase 2 will make it live.
- **Tested:** desktop + mobile (390px: single column, Back btn, no horizontal overflow); nav-click navigation; empty state; CTA dev-notice. Lint clean.

## Benchmark Workouts — PHASE 2 (2026-07-26, batch 10)
- **Restructured** `/benchmark` into a route folder: `app/benchmark/index.tsx` (landing, was flat `benchmark.tsx` — DELETED), `app/benchmark/library.tsx`, `app/benchmark/[id].tsx`. URL `/benchmark` unchanged.
- **Reusable card** `src/components/benchmark/BenchmarkTestCard.tsx` (data-driven from BenchmarkTest; optional onPress → static preview vs accessible button; category chip / SOON badge / duration / intensity / measures). Used by landing preview + library.
- **Library** (`/benchmark/library`): full 8-test grid + horizontal category filters (all / ready-now / ftp / aerobic / anaerobic / neuromuscular / skill), empty-per-filter state. Reached via landing `bm-browse` / `bm-view-all` (now LIVE, replaced the Phase 1 dev-notice).
- **Test detail** (`/benchmark/[id]`, reusable for every test): quick facts (duration/intensity/frequency), what-it-measures + measure chips, numbered How-it-works, TEST STRUCTURE interval list w/ targets (`intervalTarget()` renders %FTP ranges / ramp steps / cadence), equipment chips (EQUIPMENT_META), maximal-effort safety note (`detail-safety`, shown when `requiresMaximalEffort`). Available tests → "Start test" (SOON tag, dev-notice — Phase 3 will wire readiness/setup); coming_soon → disabled "Coming soon" (`detail-start-disabled`). Unknown id → graceful not-found w/ back-to-library.
- **GOTCHA:** adding new route files to a running Metro needs `rm -rf .metro-cache/* /tmp/metro-file-map-*` + expo restart, else nav is stale.
- **Tested:** desktop + mobile (390px no overflow, single column); landing→library→detail nav; category filter (Ready-now shows 4); Start dev-notice; coming_soon locked state; safety note on maximal tests. Lint clean.


## Benchmark Workouts — PARTS 1–5 (2026-07-26, batch 11) — VERIFIED (iter46)
Re-specified per detailed 5-part brief; SUPERSEDES batch-9/10 "Phase 1/2".
- **Data model reworked** (`src/lib/benchmark/types.ts`, `catalog.ts`): 6 categories (threshold, aerobic_power, short_power, endurance, technique, recovery) + `CATEGORY_ORDER`. 8 tests ALL `available`: ramp, twenty_min_ftp, five_min_aerobic, one_min_power, sprint_power, aerobic_efficiency, cadence_control, recovery_response. Each has full Part-4 detail fields (purpose, whoFor, difficulty, effort maximal/submaximal, required+optional equipment, indoor/outdoor, warm/main/cool summaries, recovery, safety, intervals, versioned calc). `needsNoPower()` for the No-Power filter.
- **Part 1 nav** (`data.ts`): label "Benchmark Workouts", ordered Virtual Routes → **Benchmark** → Training Plan. (index.tsx local routes map + app-scaffold ROUTE both already have it.)
- **Part 2 landing** (`app/benchmark/index.tsx`): header + tagline "Test Your Fitness • Track Your Progress" + supporting line; CURRENT BENCHMARK PROFILE = 9 stats via `useBenchmarkProfile()` showing "Not yet tested" (never 0); RECOMMENDED NEXT BENCHMARK = Ramp (View/Schedule/Start); RECENT RESULTS empty state; BENCHMARK HISTORY range chips + dashed chart placeholder; embeds the Library.
- **Part 3 library** (`src/components/benchmark/BenchmarkLibrary.tsx`, reusable, `initialFilter` prop): grouped-by-category when All, flat when filtered; filters all/6 categories/maximal/submaximal/no_power. Reused on landing (`bm-library`) + `/benchmark/library` (reads `?filter=`). Reusable `BenchmarkTestCard` (View Details + Start Test, effort badge, equipment chips).
- **Part 4 detail** (`app/benchmark/[id].tsx`): reusable template, all fields; Start→readiness, Schedule→notice, Back→/benchmark; unknown id → graceful.
- **Part 5 readiness (SAFETY)** (`app/benchmark/readiness/[id].tsx` + `src/lib/benchmark/readiness.ts`): 7 Q's, progress bar, `rd-check` disabled until all answered. `evaluateReadiness`: danger Q (`symptoms`) bad → **do_not_start** (Continue button ABSENT; only Exit + View Submaximal Options→library?filter=submaximal); any other bad → **caution** (Continue + Choose Another + Reschedule); else **ready** (Continue). Answers cached in-memory (survive back-nav, wiped on full reload). On check → POST session.
- **Backend** (`server.py` ~L1183): GET /benchmark/profile (9 nulls), GET /benchmark/results ({results:[]}), POST/GET/PATCH /benchmark/sessions/{id}; user-scoped via auth.current_user_id(); collections benchmark_profile/results/sessions. `api.ts` hooks forward-compatible + auto-authed via installFetchAuth.
- **Tested (iter46):** 13/13 backend pytest (`/app/backend/tests/test_benchmark.py`) incl cross-user scoping; all FE Parts 1-5 incl safety gating. NO issues. Start-test/results are honest labelled dev-states (player=Part 6+, calcs=Part 10+).
- NEXT: Parts 6-10 (equipment setup, workout player + tests, calculations, results/confidence, history chart, FTP/zone review).


## Benchmark Workouts — PART 6 (2026-07-26, batch 12) — Equipment & Test Setup
- **Setup wizard** `app/benchmark/setup/[id].tsx` (single-route, 6 steps in state: Overview→Readiness→Equipment→Sensors→Environment→Summary, then Begin Test). Progress bar + "Step X of 6". Back preserves all state (single component). Cancel→/benchmark. Start-Test CTAs (landing rec-start, detail-start, library card) REPOINTED from /benchmark/readiness/[id] → /benchmark/setup/[id] (old readiness route still exists but is superseded).
- **Logic** `src/lib/benchmark/setup.ts`: SETUP_STEPS; EquipmentState (5 sensors + none); `computeSensorLevel` A/B/C/D; `checkCompatibility` (POWER_REQUIRED=ramp,twenty_min_ftp,five_min_aerobic,one_min_power,sprint_power block w/o power; POWER_PREFERRED=aerobic_efficiency,cadence_control warn; recovery_response HR/perceived ok); ENV_CHECKS indoor(8)/outdoor(7) differ; COACH_VOICES/DEPTHS; `simulateSensors` (all Connected, DEV-labelled).
- Readiness step reuses `readiness.ts` (evaluateReadiness/buildOutcome). Do-Not-Start blocks advance (Continue hidden; Exit + View Submaximal Options). Incompatible equipment → "requires equipment not currently available" + View Compatible/Update/Cancel, footer Continue hidden. Session created after readiness (createBenchmarkSession); Begin Test PATCHes /benchmark/sessions/{id} with equipment, sensorLevel, environment{mode,checks}, coaching{voice,depth}, status ready_to_start. Begin shows Part-7 placeholder (player not built).
- **Tested (screenshots):** full flow Overview→Begin (Level A shown, indoor/outdoor checklists differ, summary correct, session saved); Do-Not-Start gate (continue count 0, exit present); Caution restores Continue. Lint clean.
- NEXT: Part 7 (reusable Workout Player: states, pause/stop/recovery, sim data, coaching prompts), Part 8 (4 protocols), Part 9 (versioned calc service + result pages + confidence/quality + reflection + accept/exclude), Part 10 (profile/history charts + FTP/zone review, configurable zones). Player Begin currently a placeholder to wire in Part 7.


## Session update (Jun 2026 fork) — Today polish + Push (WP-F)
- **Today screen UI (P0, done + verified via screenshot):**
  - 7-day forecast now reliably tappable (added `hitSlop`), enlarged text (temp 22→27, place 16→18); hint is a highlighted yellow pill. Modal opens on tap.
  - User avatar: fixed missing `avatarImg` style (root cause photo not rendering); no-photo fallback now a **gold-ring circular initials placeholder** (rider initials, gold tint). `HeroRoute.tsx`.
  - Coach "Alberto" flash fixed: `coach-persona.ts` exposes `useCoachReady()`; `AlbertoCoachCard` masks portrait/name until hydrated. Weather location also gated on `settings.loaded`.
- **Benchmark Notifications WP-F (Emergent-managed push):**
  - Backend `push.py`: `POST /api/register-push` relay, `send_push()` helper, benchmark-week reminder loop (day-before + day-of), started on FastAPI startup. Confirmation push on `/benchmark/week/start`. All non-blocking. `EMERGENT_PUSH_KEY=placeholder` in backend/.env (deployer replaces).
  - Frontend `_layout.tsx`: module-scope handler + Android channel, tap listeners (warm + cold-start), denied-permission weekly nudge (Alert → openSettings), `registerForPush()` on sign-in. `src/lib/push.ts` uses `getDevicePushTokenAsync`.
  - `app.json`: `expo-notifications` plugin + `android.googleServicesFile: ./google-services.json` (user adds file at deploy).
  - **CANNOT be tested in Expo Go/web** — requires native build. In dev, register-push returns 500 (placeholder key) — EXPECTED; benchmark scheduling still succeeds (verified via curl).

## Session update (Jun 2026 fork) — Rive backlog + Today banners
- **Bike route-compatibility (was 🔴):** `rider-config.ts` now has `routeSuitability` per bike + `terrainKindForTag()` + `routeCompatibility(bike, tag)`. Virtual-route setup shows a warn/info banner (`testID=bike-route-compat`) when the selected bike isn't ideal for the route (e.g. Vintage + Big Climb → "Tough for big climbs"). Verified via screenshot.
- **Signal-loss behaviour (was 🟠):** `rider-animation.ts` adds a `degraded = signalLost && !frozen` state — pedalling stops, cadence/power/effort/bob ease to 0, speed spins down (tau 1.8s), posture/lean centre, coast→1. Distinct from the hard paused/emergency freeze.
- **Customise preview → live Rive rig (was 🟠):** `rider-customise.tsx` preview now renders `<RiveRider>` with idle sim telemetry (falls back to sprite on web / until real `.riv`).
- **Today plan-review banner:** `BenchmarkReminderBanner.tsx` now also surfaces a pending FTP plan-review (`useBenchmarkPlanReview`) as a green "FTP update ready NNNW→MMMW" row above the benchmark-test row; both tap to `/benchmark`. Verified: both render for demo (287→305W + Ramp Test tomorrow).

## Session update (Jun 2026 fork) — placeholder audit + local caching
- Root issue: several client hooks initialised with hardcoded defaults that flashed before their backend fetch resolved.
- `rider-profile.ts`: caches profile to AsyncStorage (`roujaune:riderProfile`), hydrates `_snap` + hook instantly; no "Rider One" flash. HeroRoute avatar initials gated on `profileLoaded`.
- `plan.ts` `usePlan`: caches per-id (`roujaune:plan:<id>`), paints last real plan instantly then reconciles; no bundled "Build & Climb" flash.
- Settings (home location) already AsyncStorage-cached + weather gated on `settingsLoaded`; coach gated on `useCoachReady()`. Verified Today shows "From Couch to Road" + "GL" with no placeholders.

## HWG admin console — full API contract implemented (2026-07-27 fork)
Completed the interrupted "before go-live" batch AND the remainder of `/app/memory/roujaune_admin_api_contract.md`:
- **Go-live:** `suspended` enforced on rider login/session (pre-existing), env-driven CORS allow-list `CORS_ORIGINS` (pre-existing), **`GET /api/openapi.json` now public** (ingress-reachable OpenAPI for contract import).
- **GDPR (F-07):** `POST /api/admin/users/{id}/export` + `DELETE /api/admin/users/{id}` (cascade over all user-scoped + benchmark collections + sessions), plus rider self-service `GET /api/auth/me/export` + `DELETE /api/auth/me`. Shared helpers `auth.export_user_data` / `auth.erase_user_data`.
- **Audit (§8.4):** every admin mutation now logs to `admin_audit` — plan CRUD (create/replace/patch/delete/week/day/adapt via `plans_admin.on_audit` → `_admin_audit_write`), benchmark/coach config, user patch/export/delete. `GET /api/admin/audit` + cursor pagination.
- **Config surface:** `GET/PUT /api/admin/benchmark/config` (retest windows/thresholds, persisted to `admin_config`, applied to live `BM_RETEST_DAYS`/`FTP_RETEST_DAYS` at startup + on PUT) and `GET/PUT /api/admin/coaches` (persona presentation only; `safety_policy` fixed/read-only, extra fields stripped).
- **Hardening:** admin user list/get strip `password_hash`/`reset_token`/`verify_token`; cursor pagination on users+audit; metrics carry no PII; `GET /api/admin/health` now `{status,db,version,uptime}`; admin self-delete blocked (400).
- **NOT built (per contract's own conditional language):** §5.4 server-managed workout catalog (catalog is client-side; "expose IF console must edit") and §8.8 production infra provisioning (deploy-time ops, not code).
- Verified: testing agent iter52 — 19/19 backend pass (`tests/test_iter52_admin_console.py`), incl. RBAC (403 rider / 401 anon), suspend-blocks-login, cursor pagination, secret stripping, audit, GDPR cascade, and rider-flow regression. Global benchmark config restored to defaults.

## HWG console — exact-path surface completed (2026-07-27 fork, iter53)
The console team reported only /login + /coaches were reachable; added the FULL set the console polls, under `/api/admin/*` (admin JWT):
- `GET /me` (admin identity — was the hard blocker), `GET /nav-badges`, `GET /dashboard` (rollup superset of /metrics), `GET /analytics/growth` (6-month new+cumulative rider series).
- Riders: `GET /riders` (alias of /users), `GET /riders/{id}`, `POST /riders/{id}/suspend|reactivate|reset-password` (reset returns a one-time temp password + force-logs-out), `DELETE /riders/{id}` (GDPR cascade), `GET /riders/export/csv` (declared before the path-param route so it isn't shadowed).
- Plans: `GET /plans` (with `status`), `POST /plans/{id}/publish|archive`, `DELETE /plans/{id}` (all reload the in-memory plan cache via `on_plan_change` + audited).
- `GET /integrations?health=1` (llm/push/email/weather/google_auth + a `database` entry when health=1), `GET /catalog` + `DELETE /catalog/{id}` (Mongo `workout_catalog`), `GET/PUT /config/benchmarks` (alias of /benchmark/config).
- Every mutation writes to `admin_audit`. RBAC enforced (rider→403, anon→401).
- Verified: testing agent iter53 — 22/22 backend pass (`tests/test_iter53_admin_console_paths.py`), incl. full rider lifecycle (suspend blocks login, reset temp pw logs in), plan publish/archive audit, CSV export not shadowed, 6-item growth series, and rider-flow regression. Global state restored (benchmark 56/56, couch-to-road published).

## Workout catalog → backend + copy-on-assign (2026-07-27 fork, iter54)
Migrated the workout catalog to the backend (server-managed, console-editable) with per-rider copies, per the user's decisions (assign from console/coach/rider = all; copy created on explicit assign; coach+rider edit via API, no in-app editor yet; also overrides individual plan workouts; app reads from backend with bundled fallback).
- **Seed:** `frontend/scripts/export-catalog.js` dumps `WORKOUTS` (171) → `backend/workout_catalog.json`; server seeds the global `workout_catalog` collection non-destructively on boot.
- **Rider endpoints:** `GET /api/catalog` (rider's effective catalog = global overlaid by copies), `GET /api/catalog/{id}`, `POST /api/catalog/{id}/assign` (copy-on-assign), `PUT /api/catalog/{id}` (edit own copy, auto-forks), `DELETE /api/catalog/{id}/reset` (revert to global).
- **Console/coach endpoints:** `GET/POST/PUT/DELETE /api/admin/catalog[/{id}]` (global CRUD) + `GET/POST/PUT/DELETE /api/admin/riders/{uid}/workouts[/{wid}][/assign]` (assign/edit/reset a rider's copy). All audited.
- **Copies live in `rider_workouts`** (user-scoped via udb; added to `USER_SCOPED` so GDPR export/delete cascade covers it). Resolution precedence: rider copy > global > bundled seed. Overriding an individual plan workout = a rider copy of that workout id (plans reference ids, so the Live HUD auto-resolves the copy) — unifies standalone-assign and plan-override.
- **Frontend:** `src/lib/catalog-cache.ts` (in-memory overlay) + `src/lib/catalog.ts` (`loadCatalog`/`assignWorkout`); `getWorkout()` checks the cache first then the bundled `WORKOUTS`; cache warmed at login in `app/_layout.tsx`. Works offline (bundled fallback) and picks up rider/coach edits once warm.
- Verified: testing agent iter54 — 9/9 backend pass (`tests/test_iter54_catalog_backend.py`: read, copy-on-assign+isolation, edit allowlist, admin CRUD, coach per-rider assign/edit visible to rider, GDPR cascade, regression) + frontend smoke (greenlantern → Workouts → FB50 → Start → Live HUD Step 1/3 with timeline). DB left clean (171 global, 0 rider copies).
- **Deferred:** in-app workout editor UI (user chose API-only for now); optional catalog admin UI lives in the external HWG console.

## Integrations cost rollup (2026-07-27 fork)
`GET /api/admin/integrations` now returns HWG spend data: each integration carries `est_cost_month_usd` + `budget_month_usd` + `over_budget` (flat estimates in `_INTEGRATION_COST`; swap for monthly_units×unit_price when usage tracking lands). Response adds top-level `totals {est_cost_month_usd, over_budget_count}`, `summary {day,week,month,year}`, `generated_at`, and returns both `integrations` and `items` keys. Default `health=1`. Verified: totals.est_cost_month_usd=70.0, over_budget_count=0.

## UX Remediation Phases 1–2 (2026-07-27 fork)
PHASE 1 (P0): Fixed broken READINESS metric label (single-line, removed squeezing mini-scale → "READINESS/84%/Good to go" horizontal); Benchmark "Review with <coach>" CTA + "reviewed" copy now use active coach via useCoach(); Daily Check-in subtitle uses active coach. Searched app for hard-coded coach names.
PHASE 2 (approved incl. #9 coach consistency): Renamed first nav 'Today'→'Home'; nav labels wrap to 2 lines so 'Benchmark Workouts' is fully readable (order unchanged: Home, Workouts, Virtual Routes, Benchmark Workouts, Training Plan, Progress, Community, Connections, Settings, Help). Made all remaining user-facing coach refs dynamic (calendar tip footer "<coach>'s Tip"+signature, "Mute Coach"). Removed residual Wellness content: 'Peaceful Pause' live-workout action, 'My Peaceful Companion' calendar filter + training MPCRecommendationCard, calendar 'RECOVERY & WELLNESS' row → 'RECOVERY', summary drawer 'My Peaceful Companion'/'My Wellness Journey'/'Sleep well'/'Hydrate well' lifestyle items → cycling recovery only. 'Harmony Wellness Group' parent brand retained. Verified: testing agent iter56 (nav/coach-switch/wellness/regression PASS) + 3 residual refs fixed & screenshot-confirmed. Lint clean.
Coach guardrails (prior): coach_system + coach_chat_system forbid wellness/medical/sleep/stress/lifestyle advice; wearable signals used only as cycling training-readiness inputs.
STILL PENDING (awaiting approval): Phase 3 (Home banners consolidation, dup CTAs, submax benchmark visibility, 45-min option, safe missed-workout handling); Phase 4 (accessibility/consistency). Also P1 backlog F-02/F-03 (benchmark safety triggers + BenchmarkDecisionService).

## Backend restructure — Rider + Benchmark domains extracted (2026-07-28 fork)
- Continued the P1 server.py modularization (3850 → 2610 lines). NEW: routes/rider.py (all /rider/* + /progress/summary), routes/benchmark.py (all /benchmark/* incl. sessions/results/zones/plan-gate/nudge/plan-review/trends/week/recommendation). Shared engine helpers moved to services/: rider_common.py (_rider_doc/_rider_line/_cal_status/RIDER_DEFAULT), coach_llm.py (coach_system/coach_chat_system/STYLE_TONE/coach_line), plan_common.py (STRUCTURED_PLAN_IDS/_RIDE_PREFIX/_active_plan_id/_plan_id_or_active).
- Admin benchmark-config router now mutates benchmark_routes.BM_RETEST_DAYS/FTP_RETEST_DAYS (module attribute) so cross-module admin edits stay in sync with the benchmark route.
- Verified: testing agent iter64 — 40/40 backend tests, ZERO behavioural regression (rider+benchmark endpoints, engine consumers of moved helpers, admin cross-module config sync).
- DEFERRED: coach + plan engines intentionally left in server.py (deeply intertwined via module-global mutation — CTR/RS/RB reload, adaptive targets); safe to extract together in a future pass.
- Rive avatar Phase 2: bindings (4 rider identities, 3 clothing styles, road/mtn/vintage bike variants) + customise UI + web/Expo-Go sprite fallback already complete in code; verified sprite→identity mapping is correct (the require() "scramble" intentionally compensates for mislabeled asset filenames). Real roujaune-riders.riv still MOCKED per user (integration blocked on the asset).
- Cleanup: fixed all React exhaustive-deps + duplicate-import lint warnings in app/workout.tsx and app/calendar.tsx (lint clean).

## Scenic Cycling — admin-managed POV catalog + player (2026-07-28 fork)
- Decoupled Scenic Cycling from the training Virtual Routes (`vroutes.ts`). Scenic now has its OWN admin-managed catalog of POV **YouTube** rides in a new Mongo collection `scenic_routes` (never reuses `VIRTUAL_ROUTES`).
- **Backend** `routes/scenic.py`: public rider feed `GET /api/scenic/routes` (published only) + `GET /api/scenic/routes/{id}` + `GET /api/scenic/last` (moved out of plan.py; scenic history tracked separately via `workout_id: scenic-<id>` vs training `virtual-`). Admin CRUD under `/api/admin/scenic-routes` (list/get/create/update/delete + publish/archive), gated by `require_admin`, audited to `admin_audit`, following the `admin_routes.py` pattern. youtube_id accepts a raw 11-char id OR any YouTube URL (normalised); id auto-slugs from name. Rich fields: place,country,duration_min,distance_km,elevation_m,tag,terrain,difficulty,surface,highlights[],description,thumbnail (falls back to YT hqdefault). Idempotent seed of 4 sample published routes when empty.
- **Frontend**: `src/lib/scenic-routes.ts` (types + `useScenicRoutes/useScenicRoute/useScenicLast/logScenicRide`); new `app/scenic-ride.tsx` relaxed YouTube player (no ERG/telemetry) with route detail + waypoint chips (foundation for future live-riding HUD); platform-split `src/components/YouTubePlayer.tsx` (native = react-native-youtube-iframe) + `.web.tsx` (DOM iframe, avoids web WebView shim). `ScenicCyclingTodayView.tsx` rewired to the API with empty-state; cards open `/scenic-ride`.
- Added real route **Lake Garda** (youtube lOkouNCWSqw, Bardolino/Lake Garda Italy, 39min/~15km/~150m, waypoints along the eastern shore) via the admin API, published as hero (sort=-1).
- Verified: testing agent iter67 — 12/12 backend PASS (auth gating, CRUD, URL/slug normalisation, 409/422, publish/archive, scenic/last separation, cleanup) + frontend player & Today scenic-mode flows PASS. Lint clean.
- DEFERRED (previous session, not started): "Reduce motion" a11y toggle; SVG Infinity warning on training Today chart. User will supply more real scenic YouTube routes later (managed via the HWG console admin API).

## Today Mode — per-rider persistence across sessions (2026-07-28 fork)
- User request: the chosen riding experience must persist across sessions until the rider explicitly changes it. `src/lib/today-mode.ts` now scopes storage per user (`roujaune:today-mode:<userId>`); `hydrateTodayModeForUser(userId)` is called from auth-context's user-change effect (login + session restore) to restore that rider's mode. `resetTodayMode()` (logout) now only resets IN-MEMORY state and KEEPS each rider's persisted choice; `clearTodayModeForUser()` wipes it on account deletion. Verified via screenshot: chose Scenic Cycling → full page reload → session restored with Scenic still active (sidebar + Lake Garda hero). Lint clean.

## Scenic catalog expansion + Welcome-back ribbon (2026-07-28 fork)
- Added 5 more admin-managed scenic POV YouTube routes (published) via the admin API with rich metadata (place/country/duration/terrain/difficulty/surface/waypoints): tyrol-three-lakes (Austria), adige-valley-to-garda (Italy), walchensee-loop (Germany), dolomites-cortina-calalzo (Italy), hells-gate-safari (Kenya). Feed now 10 published routes; lake-garda remains hero (sort=-1).
- WelcomeBackRibbon: subtle one-time home ribbon ("Welcome back — resuming your <mode>") shown when a NON-training experience is restored from the rider's saved preference on login/restart. Backed by `pendingRestore`/`consumeRestoreNote()`/`subscribeTodayMode()` in today-mode.ts (handles async hydration race); auto-dismisses after 7s, respects reduce-motion, dismissible. Rendered in app/index.tsx under HomeNotificationArea. Verified via screenshot. Lint clean.

## Scenic region filter (2026-07-28 fork)
- Added admin-managed `region` field to scenic routes (backend model + _public + create/update + seed). Tagged all 10 routes: Alps (adige-valley-to-garda, dolomites-cortina-calalzo), Lakes (lake-garda, lake-achensee, tyrol-three-lakes, walchensee-loop), Safari (hells-gate-safari), Countryside (dutch-countryside, german-country-roads, carolina-greenway).
- Scenic Today view: dynamic horizontal filter chip row (All + regions present in feed, ordered Alps/Lakes/Safari/Countryside) with per-region icons; selecting a region filters hero + all destination rows (Continue-journey stays unfiltered). testIDs: scenic-region-filter, scenic-region-<name>. Verified via screenshot (Alps → Adige Valley hero). Lint clean.

## Reduce motion + Explore Destinations search (2026-07-28 fork)
- REDUCE MOTION (P0, done): added `reduceMotion` to a11y store (a11y.ts) persisted per-rider via /api/rider/settings; toggle in Settings→Accessibility (tg-reduceMotion). `useReducedMotionSafe()` now returns OS setting OR app toggle, so all consumers honor it. Guarded continuous loops in VirtualRoute.tsx (road scroll + pedal bob) and PhaseCelebrationModal.tsx (Sparkle) to freeze when reduced. WelcomeBackRibbon already respects it. Verified: toggle renders/persists (testing agent iter68 backend 12/12 + reload).
- SVG INFINITY (P2): investigated via console-log capture on training home — NOT reproducible (RouteProfile divisions already guarded with span=max(1,..)/Math.max(0.0001,..)). Treated as resolved/non-reproducible.
- EXPLORE DESTINATIONS (new): app/scenic-destinations.tsx — full 22-route scenic catalogue with live search (name/place/country/tag; testID scenic-search) + region filter chips + responsive grid; opens /scenic-ride. Scenic side-nav "Explore Destinations" now routes here (was /routes). 
- BUG FIXED (iter68 critical): region chips weren't filtering — RN-web horizontal ScrollView stretched vertically and overlaid the grid, swallowing taps. Fixed by wrapping filter row in flexGrow:0 band, results ScrollView flex:1, keyboardShouldPersistTaps. Verified: Alps=6, Lakes=6, Safari=1, Countryside=9, All=22, Alps+'fedaia'=2.
- Remaining future/backlog: Rive rider variants (Phase 2/3, awaiting roujaune-riders.riv); activate more "coming soon" Today modes on request; favourite/save destinations (nav placeholder exists).

## Restore original training nav + rename mode (2026-07-28 fork)
- Per user: the Training experience's side navigation now matches the ORIGINAL app nav (from src/data.ts pre-Today-Mode): Home, Workouts, Virtual Routes, Benchmark Workouts, Training Plan, Progress, Community, Connections; footer Settings + Help. Each other experience (scenic/gravel/mtb/walking/etc.) keeps its own dedicated nav (unchanged).
- Renamed Today Mode label + shortLabel "Train today" → "Training" (selector button now shows "Training"). Verified via screenshot. Lint clean.

## Saved Destinations favourites + Coming-soon teasers + selector restyle (2026-07-28 fork)
- FAVOURITES: backend rider-scoped /api/scenic/favourites (GET list full published routes + ids, POST/{id} save w/404 guard, DELETE/{id}); frontend reactive store in scenic-routes.ts (useScenicFavourites/useSavedDestinations, optimistic toggle, hydrate on login / reset on logout via auth-context). Heart on shared DestinationCard (Explore + Saved) and scenic player. New /saved-destinations screen (empty-state + explore CTA). Scenic nav "Saved Destinations" now available → /saved-destinations. Verified iter69 (6/6 backend pytest + e2e).
- COMING-SOON TEASERS: backend /api/rider/interest (GET/POST/DELETE {mode}, 422 invalid; valid gravel/mountain-bike/walking/running/rowing/climbing); mode-interest.ts reactive store; new /coming-soon?mode=<id> teaser screen (hero + bullets from FutureActivityTodayView.PREVIEW + "Notify me when this launches" → notified/undo). TodayModeButton now routes coming-soon taps to the teaser (no experience switch). Verified iter69.
- SELECTOR RESTYLE (user): "TODAY I WANT TO" → "RIDING EXPERIENCE"; the selector is now a highlighted yellow button (wordmark yellow border/fill/icon/label/chevron); menu items ALL look active (removed coming-soon dimming + COMING SOON badge — teaser conveys it on tap). Training nav "Home" renamed → "Today". Verified via screenshot; lint clean.
- Known minor (non-blocking, web-only): DestinationCard heart Pressable nested in card Pressable → React web "button cannot descend button" hydration warning; functionality fine (stopPropagation).

## Selector polish + isolation fix + admin interest (2026-07-28 fork)
- DATA FIX: added scenic_favourites + mode_interest to auth.USER_SCOPED (were global; now per-rider). Verified isolation iter70 (rider A vs fresh rider B).
- HWG ADMIN API: GET /api/admin/interest → per-mode "Notify me" counts {items[6],total}, require_admin. Verified (401/403/200).
- DestinationCard: heart moved to be a SIBLING of the card Pressable (no nested <button> web warning). Verified.
- Coach avatar: added subtle white wash (rgba(255,255,255,0.14)) to lighten portrait.
- Riding-experience selector: solid yellow 3D button (borderBottom depth + shadow), no chevron; icon bubble now SOLID DARK (colors.bg) so the icon is visible; training mode icon changed "fitness"(heart-pulse) → "barbell"; label "EXPERIENCE" → "RIDE XP".
- Persistence re-confirmed per-rider across sessions (iter70 reload test).

## Standardised top-right header + sidebar cleanup + Surprise Me (2026-07-28 fork)
- **Reusable `HeaderStatus`** (`src/components/HeaderStatus.tsx`): standard top-right cluster = notification button (unread badge → NotificationsModal) + rider avatar (→ /profile). Self-manages its modal + benchmark nudge.
- Wired across every screen: home (HeroRoute training view + ScenicCyclingTodayView + FutureActivityTodayView), all AppScaffold screens (default `headerRight` = HeaderStatus: progress, community, connections, help, profile, routes, settings, benchmark), and custom-header screens (plan, training, workouts, workout-list, calendar).
- **Removed** the flame/streak "achievements" pill from the home top-right (HeroRoute StatusBar) + its now-dead "Your Progress" modal/state/imports in index.tsx.
- **Removed "Profile"** from the sidebar utility nav (`today-mode.ts` UTILITY) — profile is now reached via the avatar everywhere.
- **"Surprise me"** shuffle: scenic Today hero CTA (`testID surprise-me`) + Explore Destinations header — picks a random published scenic route and opens the player. Verified via screenshots (home training/scenic/future, progress, plan, workouts).

## Scenic Ride HUD wired to real data (2026-07-28 fork)
- **Points of Interest (LLM):** new `GET /api/scenic/routes/{id}/pois` generates route-specific POIs via emergentintegrations (openai gpt-5.4), cached in `scenic_poi`, with a highlights-derived fallback. HUD panel renamed COMING UP → **POINTS OF INTEREST**, auto-advances with ride progress; "Hear the story" narrates via companion TTS (expo-speech); save/skip/heard states; waveform animates only while narrating.
- **Progress/header/companion:** destination header + current-area + %/remaining derived from route metadata + ride position (elapsed≈video time; exact YouTube currentTime API still TODO). Companion (Alberto/Adriana) messages are contextual and respect Quiet/Discover/Guided (Guided auto-narrates); hiding a panel does not mute audio.
- **Ride info bar:** Time real; cadence/HR show "—" until BLE sensor (native build); distance estimated from progress; keeps recording when hidden.
- **Leave flow:** Close/Home/Journeys open a Save & leave / End without saving / Continue dialog; Explore saves then navigates.
- **Resume:** in-progress ride persisted to AsyncStorage (`scenic-resume.ts`: video position, elapsed, progress), cleared on logout/completion. Scenic Today hero becomes a **RESUME RIDE** card (progress bar + %) when a resumable ride exists, else the featured ride. Web player seeks via `&start=`.
- Video muted (web+native) + looping ambient CC0 track with in-HUD music on/off + 4-level volume.
- Deferred: exact video-currentTime sync, real BLE sensors, backend `discoveries` model + per-POI photos, cross-session persistence of heard/saved/hidden states, dedicated Scenic Audio/Settings/Companion sheets.

## Scenic Ride — real currentTime, discoveries, shareable Journeys recap, BLE HUD (2026-07-28 fork)
- **Pixel-accurate video sync**: both YouTube players now report real playback position. `YouTubePlayer.web.tsx` sends a `listening` handshake + parses YouTube `infoDelivery` postMessages for `currentTime`/`duration`; `YouTubePlayer.tsx` (native) polls `getCurrentTime()`/`getDuration()` every 500ms. Both call a new `onProgress(cur,dur)` prop. `scenic-ride.tsx` prefers the REAL video duration/position for the progress ring, "% explored", remaining-min, km and POI trigger timing (falls back to route `duration_min` + wall-clock until the player reports numbers).
- **Discoveries model** (backend `routes/scenic.py`, rider-scoped `scenic_discoveries` added to `auth.USER_SCOPED`): `POST /api/scenic/discoveries` (idempotent per route_id+poi_order), `GET /api/scenic/discoveries?route_id=`, `PATCH /api/scenic/discoveries/{id}`, `DELETE`. HUD "Save this discovery" bookmark (`poi-save`) now persists/removes a discovery and primes saved-state on ride entry via `fetchDiscoveries`.
- **Shareable Journeys recap**: `GET /api/scenic/journeys` joins completed scenic rides (ride_history `workout_id` `scenic-*`) with saved discoveries. `saved-destinations.tsx` retitled **"Journeys"** — renders per-ride recap cards (distance/ride-time/discoveries + Share/Ride-again) above the Saved Destinations grid. New `ScenicRecapCard` (branded, capturable) + `ScenicRecapShareModal` (`react-native-view-shot` capture → `expo-sharing`/`expo-media-library`, testIDs `recap-share`/`recap-save`/`recap-close`). `logScenicRide` now includes `youtube_id` so recaps show a thumbnail.
- **BLE telemetry in HUD**: `useBleSensors` wired into the HUD; cadence(rpm)/heart-rate(bpm) metrics are HIDDEN entirely when no sensor is connected (per user), shown only when a BLE sensor is connected (native build only — web/Expo Go always hides them).
- Verified: testing agent iteration_71 — backend 13/13 (`tests/test_iter71_scenic_discoveries.py`: CRUD, idempotency, cross-user isolation, journeys shape, regression) + frontend (Journeys recaps + share modal; HUD renders with rpm/bpm hidden; poi-save persists). NOTE: YouTube autoplay/currentTime cannot be validated in headless browser; verify video sync on a real device / Expo Go.

## Scenic follow-ups — POI landmark photos + richer recap card (2026-07-28 fork)
- **POI HUD card real photos**: `routes/scenic.py` POI generation now enriches each point of interest with a real landmark photo via keyless Wikipedia (`api.php` search+pageimages + REST `page/summary`, compliant User-Agent). The LLM prompt now also returns a `wiki` article title per POI for high relevance; a `_BAD_IMG` deny-list filters flags/coats-of-arms/locator+railway maps/logos/.svg. Images cached in `scenic_poi` (backfilled for old caches without re-running the LLM). HUD POI card + saved-discovery `photo` use `poi.image` (fallback route thumbnail). Best-effort, never 500s.
- **Recap card visuals** (`ScenicRecapCard.tsx` + `src/lib/scenic-recap.ts`): added (a) a deterministic decorative **elevation trace** (react-native-svg area+line, shaped by tag/elevation, seeded per route — not real GPS), (b) a stylized **location badge** (pin + place,country over a faint map grid) overlaid on the thumbnail, and (c) a **template coach caption** in the companion coach's voice (instant, free, varied by ride character — user skipped choosing so default = template, not LLM). `/api/scenic/journeys` payload gained `elevation_m` + `country`; `logScenicRide` now sends `country` + numeric `elevation_m`.
- Verified: testing agent iteration_72 — backend 15/15 (POI images present+relevant+cached+deny-list, journeys elevation_m/country, discoveries CRUD + scenic regression, no 500s) + recap card screenshot (elevation trace, location badge, "150 m climb", coach caption "…smooth and serene through Bardolino… — Alberto"). NOTE: old ride_history records logged before this show a blank thumbnail (no youtube_id); new scenic rides carry it.

## Journeys Discoveries gallery + side-nav label + coming-soon backgrounds (2026-07-28 fork)
- **Discoveries scrapbook**: Journeys screen (`saved-destinations.tsx`) now has segmented tabs **Rides | Discoveries**. The Discoveries tab is a photo grid (`DiscoveryTile`) of every saved POI across all rides (`useAllDiscoveries` → GET /api/scenic/discoveries). Tapping opens `DiscoveryDetailModal` (full landmark photo, description, editable "your note" via PATCH, "Ride this route", remove via DELETE). Verified via screenshot (2 Lake Garda discoveries with real photos + detail modal).
- **Side-nav "Ride Experience" button**: enlarged the label (`TodayModeButton.selLabel` 10→11px, lineHeight 14, tighter `wrap` padding 8→4) and capitalised to "Ride Experience"; wraps cleanly to two lines. Verified.
- **Coming-soon experience backgrounds**: `today-mode.ts` `modeMeta()` now attaches a fitting `heroBg` (keyless Wikimedia Commons photos) per coming-soon experience — gravel, mountain-bike, walking(hiking), running(trail), rowing(Henley crew), climbing(Haʻikū Stairs). `ExperienceHero` `source`/`imageTransform` wired in both `coming-soon.tsx` and `FutureActivityTodayView.tsx` (gentle 1.06 scale). Verified: climbing shows a famous mountain staircase, rowing shows a waterway crew.

## Recap cover photo picker (2026-07-28 fork)
- Riders can set any saved discovery photo as the cover of that ride's shareable recap card. Backend: `scenic_recap_covers` (rider-scoped, added to auth.USER_SCOPED) + `PUT /api/scenic/journeys/{ride_id}/cover {photo|null}`; `GET /api/scenic/journeys` now returns a `cover` field per ride. Frontend: `ScenicRecapShareModal` shows a "COVER PHOTO" thumbnail row (Default route thumbnail + each discovery photo) that updates the card instantly and persists via `setRecapCover`; `ScenicRecapCard` + the Journeys list card use `cover || thumbnail`; the Journeys screen reloads on modal close. Verified via curl (set/reset/persist) + screenshot (selecting a discovery updates the cover + highlight).

## Effortless one-tap "Save discovery" prompt during rides (2026-07-28 fork)
- In the Live Scenic Ride HUD (`scenic-ride.tsx`), when the rider reaches a point of interest a subtle top-center prompt pill auto-appears ("NEW DISCOVERY" + title + one-tap **Save** + dismiss ×), independent of the panels and visible even when the HUD is hidden. Tapping Save persists the discovery and shows a confirmation micro-toast; dismiss closes without saving. Each POI prompts at most once (skips already-saved/skipped). `toggleSave` now returns success + rolls back the optimistic add on failure; `acceptPrompt` shows an honest success/retry toast.
- Verified: testing agent iteration_73 — all 5 acceptance criteria PASS (prompt appears "Pertisau Bay", Save persists to /api/scenic/discoveries, dismiss doesn't persist, no re-prompt, HUD + poi-save regression OK). Note: YouTube doesn't autoplay headless so progress advances via wall-clock estimate.

## Discovery prompt chime + haptic (2026-07-28 fork)
- When the "Save discovery" prompt appears during a ride, a gentle bell chime plays and a success haptic fires (eyes-forward alert). Added a synthesized `assets/audio/discovery_chime.wav` (soft E6→B6 bell, ~1.1s), played via `expo-audio` at 0.55 volume; haptic via `expo-haptics` `notificationAsync(Success)` (guarded to non-web). Triggered from the same reached-POI effect validated in iter73. Audio/haptics are native — fully testable only on a device build. Verified: ride screen bundles + renders with the new asset.

## "Discoveries saved this ride" HUD counter (2026-07-28 fork)
- Tracks discoveries saved during the CURRENT ride via a `sessionSaved` set (distinct from previously-saved POIs primed from the backend on entry). Incremented on a successful new save (prompt or panel bookmark), decremented on un-save. A subtle yellow chip ("N discover(y/ies) saved this ride") appears in the left progress panel when count > 0. Verified via screenshot (counter text renders after saving on lake-achensee).

## End-of-ride recap + "Great explorer" celebration (2026-07-28 fork)
- When a scenic ride completes (pct>=0.98), a full "Ride Complete!" recap overlay auto-shows in `scenic-ride.tsx` with stats (Time/km/Saved) + CTAs "View my journey" (logs ride → Journeys) and "Back home". A one-time celebratory chime+haptic fires (respects Quiet). When `sessionSaved.size >= 3`, a gold "Great explorer! You saved N discoveries this ride" badge appears; 1-2 shows a scrapbook note; 0 shows a tip. Verified via screenshot (badge + recap render correctly). NOTE: completion isn't reachable in the headless preview (video doesn't autoplay); a temporary demo flag was used only to capture the screenshot and has been fully removed.

## "View my journey" → instant shareable recap (2026-07-28 fork)
- The end-of-ride recap's "View my journey" CTA now navigates to `/saved-destinations?justFinished=1`; the Journeys screen reads `justFinished` and auto-opens the shareable recap card (`ScenicRecapShareModal`) for the newest (just-completed) ride, with the cover-photo picker + Share/Save ready — one-tap sharing while the celebration is fresh. Verified via screenshot (modal auto-opens on the Lake Garda recap with cover picker).

## Landscape end-of-ride recap + animated route map (2026-07-28 fork)
- Redesigned the completion overlay (`scenic-ride.tsx`) from a plain portrait card into a wide LANDSCAPE recap: header (check + "Ride Complete!" + route/place) with an inline Time/KM/Saved stat block, a centerpiece animated route map, a footer with the "Great explorer!" badge (3+) / scrapbook note / tip + a coach caption (`recapCaption`), and CTAs "View & share" (→ /saved-destinations?justFinished=1 auto-opens the share card) + "Back home".
- New `src/components/RideRouteMap.tsx` (react-native-reanimated + react-native-svg): a stylised, deterministic trail that self-draws (animated strokeDashoffset) with a moving rider dot, over a faint terrain/contour backdrop; each saved discovery pops up (spring-scaled) as a photo bubble positioned at its at_pct along the trail. NOT real GPS (POV videos have no track) — a believable decorative map. Verified via screenshot: gold trail draws with 3 discovery bubbles (Pertisau Bay/Karwendel/Achenkirch), badge + caption + CTAs. Temporary demo flag used for the screenshot fully removed.

## ARCHITECTURE.md (2026-07-28 fork)
- Created `/app/ARCHITECTURE.md` — living overview of stack, backend routers, frontend routes/lib/components, user-scoped collections, the full Scenic Cycling data model + flow, key endpoints, integrations, native-only features, and conventions. **Keep it updated as features change.**

## Confetti celebration on 3+ recap (2026-07-29 fork)
- New `src/components/ConfettiBurst.tsx` (react-native-reanimated): one-shot confetti (gold/white/red/teal pieces spray + fall + rotate + fade). Rendered as the top-most, `pointerEvents="none"` layer of the landscape completion recap in `scenic-ride.tsx`, only when `sessionSaved.size >= 3` (pairs with the "Great explorer!" badge). Verified via screenshot. ARCHITECTURE.md updated.

## HuCentAI rebrand (2026-08 fork)
- Coach tagline changed to **"Your HuCentAI Training Companion"** (HuCentAI = Human-Centred Artificial Intelligence, "Hyoo-cent-eye") across `coach-persona.ts`, `data.ts`, plan/phase-celebration cards, Settings section header. One-time first-launch explainer `src/components/today/HuCentAIIntro.tsx` on Today; explainer subtitle under Settings coach section.

## In-app subscriptions (App Store / Google Play only) (2026-08 fork)
- Model: FREE = 3 rides total, each ≤30 min → paywall; PREMIUM (single auto-renewing sub, `premium_monthly`/`premium_yearly`, yearly default) unlocks everything. No free trial.
- Backend `routes/billing.py` is entitlement source of truth (user-scoped `billing`: premium_until, free_rides_used, consumed_keys). Endpoints: /billing/status, /products, /consume-ride, /validate (Apple/Google verify, IAP_DEV_TRUST fallback), /restore. Store creds placeholders in backend/.env; IAP_DEV_TRUST=0.
- Frontend: `react-native-iap` v16 via `src/hooks/useStore.native.ts` (web/Expo Go use `useStore.ts` stub); `src/lib/entitlement.ts` store; `PaywallModal.tsx`; `app/upgrade.tsx`; Settings membership card; gating + 30-min cap in `scenic-ride.tsx` & `workout.tsx`; subtle `RideStatusBanner.tsx` ("X free rides left" / Premium) on Today + scenic ride-picker. Purchase path is NATIVE-ONLY. Verified (iter 74/75).

## Ride ingestion + performance analysis — Phase 1+2 (2026-08 fork)
- Upload outdoor rides (.fit/.gpx/.tcx) → `routes/activities.py` + `activity_parse.py` (fitparse/gpxpy/XML) → TrainingPeaks metrics (NP/IF/TSS, power curve, power+HR zones) → existing `activity_sync.ingest_activities` (dedup/classify/mirror to ride_history, now with user_id). Unified indoor+outdoor list + detail + editable FTP endpoints.
- Frontend: `app/activities.tsx` (Rides & Analysis list + upload), `app/activity/[id].tsx` (Strava-style detail), `src/components/analysis/RideAnalysis.tsx` (interactive SVG route map + synced elevation/power/HR graph w/ drag scrubber + power curve + zone bars), `src/lib/activities.ts`. Nav item added to training + scenic modes. Verified iter 76 (8/8 backend + frontend).
- Roadmap: Phase 3 = aggregate PMC (CTL/ATL/TSB) + weekly TSS + all-time power curve; Phase 4 = Strava/Garmin OAuth auto-sync (infra exists in routes/connections.py + providers/). Native map later.

## Analysis Phase 3 + Strava (2026-08 fork)
- Fitness Trends: `routes/analysis.py /pmc` (CTL/ATL/TSB TrainingPeaks model) + `/records` (all-time best power 5s/1m/5m/20m). Screens `app/fitness.tsx`, components `FitnessCharts.tsx`, lib `analysis.ts`.
- Ride Compare: `app/compare.tsx` — pick 2 rides, overlay power vs progress + green/red delta table. "Compare" button on Rides list.
- Strava Sync: `providers/strava.py` (OAuth code flow, rides+streams→route_data samples/power_curve) registered via existing `routes/connections.py` generic pipeline; appears in Connections screen. Placeholder env STRAVA_CLIENT_ID/SECRET (user adds real keys at deploy). Custom-scheme OAuth needs native/published build. Verified: PMC/records/compare via screenshots+API; Strava provider registers + shows "Setup required".

## Bidirectional Health Sync — Apple Health + Health Connect (2026-08 fork)
- **Goal:** push completed Roujaune rides to, and import outdoor rides from, the rider's native health platform. iOS → Apple Health (`react-native-health` v1.19). Android → Health Connect (`react-native-health-connect` v4.1.2) — the OS hub that Samsung Health & Google Fit sync through, so one link covers both. User chose BOTH auto-push (on ride completion) + a manual "Sync now".
- **Frontend:** platform-split native wrapper `src/lib/health-native.native.ts` (real HealthKit/Health Connect read+write, lazy-require in try/catch) and `src/lib/health-native.ts` (web/Expo Go no-op stub) — Metro picks by extension. Orchestrator `src/lib/health.ts` (`useHealthSync` hook + `autoPushCompletedRide` helper + AsyncStorage prefs `{autoPush,importEnabled,linked,lastSync}`). UI `src/components/HealthSyncCard.tsx` rendered in `app/connections.tsx` as a "HEALTH SYNC" card (Connect → contextual permission request following the permissions contract: granted/denied/blocked→Open Settings; toggles for auto-push + import; Sync now; disconnect). Native providers filtered out of the OUTDOOR RIDE SYNC (OAuth) list to avoid duplication. Auto-push wired in `app/summary.tsx` (once per saved ride, best-effort/silent).
- **Backend:** `providers/native.py` registers `health_connect` alongside `apple_health` (both device_native). `routes/connections.py` new endpoints: `POST /connections/native/{id}/link` (mark connected after on-device permission), `POST /connections/native/{id}/import` (ingest device-read cycling workouts via `activity_sync.ingest_activities` → history/progress/coaching), `POST /connections/native/{id}/pushed` (record push status). Non-native ids → 404. NOTE: `udb` proxy injects user_id via `$setOnInsert`, so native docs must NOT include user_id in `$set`.
- **app.json:** iOS HealthKit entitlement + NSHealthShare/UpdateUsageDescription; `react-native-health` plugin health share/update strings; `react-native-health-connect` plugin; Android health READ/WRITE permissions (EXERCISE, DISTANCE, TOTAL_CALORIES_BURNED, HEART_RATE).
- **NATIVE-ONLY:** actual HealthKit/Health Connect I/O + permissions can ONLY be validated on a real device build (not Expo Go / web preview). Backend verified: testing agent iter77 14/14 pass. Web preview: Connections HEALTH SYNC card shows the "needs app build" notice (no crash).

## Performance Analysis — Form Forecast, Weekly Digest, Segment Compare (2026-08 fork)
- **Form Forecast:** `GET /api/analysis/pmc` now also returns `forecast[]` (next `forecast_days`, default 14) projected from the rider's recent 14-day average daily TSS, plus `forecast_fitness/forecast_form/forecast_state/projected_daily_tss`. `/fitness` PmcChart draws a dashed CTL continuation past a "today" divider + faint projected form bars; new `ForecastSummary` "In 2 weeks" card (projected fitness + form + state).
- **Weekly Digest:** `GET /api/analysis/weekly-digest` → `this_week{tss,hours,rides,distance_km}`, `deltas` vs last week, and `new_records[]` (power bests set in the last 7d beating prior all-time). New `WeeklyDigestCard` "This Week" recap at the top of `/fitness` (4 tiles + vs-last-week deltas + new-records list/empty state).
- **Segment Compare:** new `backend/segments.py` (GPS climb detection: valley→peak with dip tolerance, ≥30m gain / ≥3% / ≥400m; matched across rides by start-point haversine <300m + length within 45%; resampled base→summit by climb fraction with derived speed). `GET /api/analysis/segment-compare?a=&b=` returns matched climbs (aligned speed/elevation series + per-climb time/avg-speed + delta). `/compare` gained a "Whole ride" / "Same climb" toggle (testIDs `mode-ride`/`mode-climb`); climb mode shows an "X faster up this climb" banner (`segment-banner`) + aligned speed/elevation chart + per-climb stats. Graceful message when rides share no GPS climb.
- Verified: testing agent iter78 (backend 9/9 pytest + frontend both screens). DEMO ACCOUNT (demo@roujaune.app) has two uploaded GPX rides "Col de Test" (same ~171m climb, ids e3cb783f… slower / c95da076… faster) kept as a live Segment Compare example.

## Analysis Suite Round 2 — Climb Leaderboard, Form Target, Digest Share, Coach Weekly Note (2026-08 fork)
- **Climb Leaderboard:** `GET /api/analysis/climb-leaderboard` clusters every GPS climb (backend/segments.py detect+haversine) into repeated climbs (>=2 attempts), ranked fastest-first with `pr`/`gap_s`. New screen `app/climbs.tsx` (expandable per-climb cards, PR trophy, gap-to-PR, tap→/activity/[id]); linked from `/fitness` Personal Records header (testID `climbs-link`). Empty state when no repeated climbs.
- **Form Target:** `GET/PUT /api/analysis/event` (stores event_date/event_name in udb.settings id 'event'); `GET /api/analysis/form-target` projects Form/TSB onto the event morning (fresh if >5) using shared `_daily_tss`/`_ctl_atl_today` helpers. `/fitness` 'Form Target' card: event-name input + In 4/8/12 wks presets + Clear + verdict box (`form-target-result`).
- **Digest Share:** 'Share' button (`digest-share`) on the Weekly Digest card opens the existing `ShareCardModal` with a branded WEEKLY RECAP `AchievementCardData` (TSS/rides/hours/distance).
- **Coach Weekly Note:** `GET /api/coach/weekly-note?coach_name=&coach_gender=&refresh=` — LLM (claude via emergentintegrations, coach persona) returns `{note, focus, has_activity}`, cached per ISO-week+coach in udb.settings id 'weekly_note'. `/fitness` 'Coach's Weekly Note' card: note + 'This week:' focus + Listen TTS (useCoachSpeech, testID `weekly-note-play`) + Regenerate.
- Verified: testing agent iter79 (backend 7/7 pytest + frontend all checks). Demo account has event 'Gran Fondo' (~today+30) and the two 'Col de Test' climb rides as live examples.

## Analysis Suite Round 3 — Climb Map Preview, Record Celebration, Taper Plan, Share Streaks (2026-08 fork)
- **Climb Map Preview:** `climb-leaderboard` now returns per-climb `path` ([[lat,lng]] ~24 pts of the PR attempt via segments.climb_path); `/climbs` renders an SVG `ClimbMiniMap` thumbnail (green start / red end dots).
- **Record Celebration:** `climb-leaderboard` returns `new_pr` (fastest attempt == rider's most recent ride AND beats prior best) + `pr_improvement_s`; `/climbs` shows a celebration banner (testID pr-celebrate) + Share (pr-share) → ShareCardModal "NEW PERSONAL BEST", plus a 'PB' flame tag on the climb card.
- **Taper Plan:** `GET /api/coach/taper-note` — when form-target isn't fresh, LLM (claude) returns `{note, actions[]}` for the final-week taper, cached per event+coach in udb.settings 'taper_note'. Rendered inside FormTargetCard (testID taper-plan) with Listen TTS, only when `has_event && !past && fresh===false`.
- **Share Streaks:** `GET /api/analysis/streak` → consecutive ISO weeks with >=1 ride `{current_weeks,best_weeks,this_week_rides,active,weeks_ridden}`; `/fitness` 'Consistency Streak' card (flame count + progress) + 'Share streak' button (streak-share) → ShareCardModal. fitness.tsx share state refactored to hold AchievementCardData (digest + streak both use one ShareCardModal).
- Verified: testing agent iter80 (backend 4/4 pytest + frontend all checks). Demo event 'Gran Fondo' (~today+21, not fresh) kept so the taper plan shows.

## Analysis Suite Round 4 — Climb Detail, Taper Auto-Apply, Milestones, Streak Reminders (2026-08 fork)
- **Climb Detail Page:** `GET /api/analysis/climb-detail?id=<climb-id>` (shares `_cluster_climbs` helper w/ leaderboard) returns shared elevation `profile` + every attempt's speed `series` aligned base→summit. New screen `app/climb/[id].tsx` (elevation area + per-attempt speed polylines, PB highlighted + legend + attempt list). Reached from `/climbs` expanded 'View elevation & overlay' link.
- **Taper Auto-Apply:** `POST /api/coach/taper-apply {coach_name}` eases the plan week leading into the event via `companion_plan.auto_ease_ops` + `_apply_companion_ops` (reuses adaptive engine), records an adaptation note, tracks `plan_state.eased_weeks` (idempotent → already:true). Structured plans only (else {applied:false, reason:'unstructured'|'no_plan'}). 'Apply taper to my plan' button in FormTargetCard taper block (testID taper-apply-btn).
- **Milestones:** `GET /api/analysis/milestones` → lifetime totals + `recent` (round-number milestone crossed by the last ride, else null) + next ride/km targets w/ remaining. `/fitness` 'Milestones' card (tiles + progress-to-next; celebration banner + Share only when recent!=null). MILESTONE_RIDES/KM/HOURS constants in analysis.py.
- **Streak Reminders (in-app, NOT push):** `/api/analysis/streak` now returns `at_risk` (active streak && 0 rides this week && weekday>=Thu) + `days_left` + `weekday`; StreakCard shows an amber nudge (testID streak-at-risk) when at_risk.
- Verified: testing agent iter81 (backend 6/6 pytest + frontend all checks). NOTE: greenlantern plan_state.eased_weeks now has the taper-applied week (idempotent, harmless).

## Analysis Suite Round 5 — Milestone Ring, Climb Splits, Milestone Shout-out, Streak Freeze (2026-08 fork)
- **Milestone Progress Ring:** `/api/analysis/milestones` adds prev_rides/rides_progress + prev_km/km_progress (0..1). MilestonesCard renders an SVG `ProgressRing` toward the nearest next milestone (center 'X to N').
- **Climb Detail Splits:** `/api/analysis/climb-detail` adds `splits[]` (n=4 equal-distance segments; `_climb_splits` interpolates per-attempt time via series d/t; `fastest` flagged). `/climb/[id]` renders a 'Split times' table highlighting the fastest attempt per split.
- **Coach Milestone Shout-out:** `GET /api/coach/milestone-note` — when milestones.recent!=null, LLM (claude) returns `{has_milestone,note}`, cached per milestone in udb.settings 'milestone_note'. MilestonesCard celebration banner gets a Listen (TTS) button. {has_milestone:false} when nothing recent.
- **Streak Freeze:** `/api/analysis/streak` adds freeze_tokens (earned=min(3,1+weeks_ridden//4)-frozen), frozen_weeks, can_freeze, gap_week; `POST /api/analysis/streak-freeze` spends a token to bridge the breaking off-week (stored in udb.settings 'streak_freeze'.frozen; streak calc unions ride-weeks with frozen). StreakCard shows banked tokens + 'Use a freeze' button when can_freeze.
- BUGFIX: removed an accidental duplicate `@router.get("/streak")` stub that shadowed the real route and returned null.
- Verified: testing agent iter82 (backend 5/5 pytest + frontend all checks, no bugs).

## Round 5 (COMPLETE — iter 83 PASS)
- Season Recap card on /fitness (shareable) — GET /api/analysis/season-recap
- Milestone Wall screen /milestones (earned/locked badges) — GET /api/analysis/milestone-wall, linked from /fitness Milestones card
- Split PR Highlights in /climb/[id] — climb-detail returns recent_split_prs; amber banner + flame on split rows
- Freeze Auto-Suggest — StreakCard "Protect this week with a freeze" (suggest_freeze)
- GEMINI COACH VOICE: GET /api/coach/speak (Gemini TTS gemini-2.5-flash-preview-tts, GEMINI_API_KEY; Alberto=Charon, Adriana=Aoede). useCoachSpeech now streams this (web blob / native uri+bearer) via expo-audio with expo-speech fallback. Workout real-time cues (useWorkoutAudio) unchanged.

## Round 6 (COMPLETE — iter 84 PASS)
- Season Photo: AchievementCard 6-stat grid; season share -> saveable/shareable branded image
- Recap Reminder: year picker on Season Recap card (season-prev/season-next); refetches per year
- Badge Nudge: MilestoneNudgeBanner on Home (within 3 rides / 25 km of next badge) -> /milestones
- Voice Preview: Settings coach 'Hear a sample' now plays the real Gemini voice
- RESEND EMAIL (own account, Harmony Wellness Group <noreply@harmonywellnessgroup.com.au>, RESEND_API_KEY, domain verified): emailer.py -> direct Resend API. Welcome (on register), verification, password reset, weekly digest. GET/PUT /api/analysis/email-prefs, POST /api/analysis/email-digest, weekly_digest_loop (Mon ~08:00 UTC, opted-in verified riders). Settings EMAIL card + Weekly Digest 'Email me' button.

## Round 7 (COMPLETE — iter 85 PASS)
- Unsubscribe: one-tap link in weekly email footer -> public GET /api/analysis/unsubscribe?token= (branded confirmation page); per-rider unsub_token
- Send Day Choice: email_prefs.digest_weekday (0-6); Settings day picker; weekly loop sends on each rider's chosen day (daily 08:00 UTC tick)
- Season Photo Theme: AchievementCard variant='season' (year watermark + BIGGEST CLIMB hero band + 6-stat grid) for the shareable season image

## Round 8 (COMPLETE — iter 86 PASS)
- External HWG admin console access: static service token (ADMIN_API_TOKEN in backend/.env). Send `Authorization: Bearer <token>` to any /api/admin/* endpoint -> full admin (synthetic principal svc_admin_console). Added at top of auth._resolve_token (constant-time compare). Interactive admin login unchanged. CORS lockable via CORS_ORIGINS env (awaiting admin domain).
- PAUSED (to resume): Milestone Email, Recap Backdrop Photo, Email Preview.

## Round 8b (COMPLETE)
- HWG service token finalized: env HWG_SERVICE_TOKEN (fixed value from HWG), principal "HWG Console". CORS locked to the 4 HWG origins. Old ADMIN_API_TOKEN removed. Curl-verified (token 200, old 401, all 4 origins CORS pass, evil blocked, rider app unaffected).

## Round 9 (COMPLETE — iter 87 PASS)
- Milestone Email: celebratory email on crossing rides/km/hours milestones (baselines on first run, dedup). Hooked after ride save. emailer.milestone_email_html.
- Recap Backdrop Photo: riders add own photo behind Season share image (expo-image-picker, season-backdrop-pick) — AchievementCard bgUri.
- Email Preview: WeeklyEmailPreview in Settings EMAIL card (toggle-email-preview) shows a compact rendition of the weekly recap email.

## Round 10 (COMPLETE — iter 88 PASS)
- Discovery Backdrop: pick a Season-card backdrop from Discoveries scrapbook (season-discovery-picker) + camera roll.
- Send Digest Test: on-demand digest email now a clearly-labelled '[Preview]' with banner; buttons relabelled 'Send me a test now' / 'Send test'.
- Milestone Share Card: earned milestone badges on /milestones are shareable via ShareCardModal.
- Admin grant: roger.parenzee@gmail.com (user_f4b48fb94eff) given ANNUAL premium (plan yearly, product premium_yearly, premium_until 2027-08-11) via direct billing upsert (source admin_grant). Verified is_premium=True.

## Round 11 (COMPLETE — iter 89 PASS)
- Admin premium: POST /api/admin/riders/{id}/premium {action grant|revoke, plan yearly|monthly|gift_month}; user detail returns billing{premium,plan,plan_label,...}. Audit-logged. (For HWG console.)
- Plan Badge: profile shows 'Premium · Annual/Monthly' pill (premium-badge) via useEntitlement.
- Ride experience button: dropped biking icon; now shows current experience (meta.label/shortLabel) + chevron.
- Demo premium grant used for badge test then REVOKED (pristine).

## Automated Store Screen-Capture + Store-Listing system (admin-only) (2026-08 fork)
- New `backend/screen_capture.py` (self-contained `capture_router`, prefix `/api/admin/screen-captures`, gated by `auth.require_admin` — HWG service token or admin session). Playwright headless Chromium (installed) logs into the Expo WEB build as demo@roujaune.app, traverses 10 screens (home, plan, workouts, workout_list, scenic, progress, fitness, calendar, profile, wellness), captures raw viewport PNGs (1280x800 @2x), frames each on a branded 2048x1536 App-Store canvas via Pillow (caption headline + yellow accent line + device bezel + rounded screenshot + ROUJAUNE wordmark), and stores raw/framed/thumb base64 in Mongo `screen_captures`. App Store listing copy (title/subtitle/promotional_text/description/keywords) is generated via the Emergent LLM key (claude-sonnet-4-6) into `app_meta` key="store_listing". Job progress tracked in `capture_jobs` (_id="current").
- Endpoints: GET /screens, POST /refresh {screens?:[keys]} (async background job, 409 if running), GET /status, GET "" (list metadata+thumb only), GET /export (ZIP of raw+framed+listing txt/json), GET /store-listing, PUT /store-listing (partial), POST /store-listing/generate, GET /{key}?variant=framed|raw|thumb (PNG).
- Base URL read from `EXPO_PUBLIC_BACKEND_URL` (frontend/.env) so it tracks the current preview URL — never hardcoded. Demo account set `email_verified=true` for clean store shots.
- Verified: testing agent iter90 13/13 PASS (auth matrix 401/401/403/200, 10 screens, list excludes raw/framed, store-listing GET/partial-PUT, variant PNG + 422/404, ZIP export). Full 10-screen capture + LLM copy confirmed working end-to-end (framed images visually verified).
- `playwright==1.62.0` + `pillow==12.3.0` added to backend requirements.
- PAUSED (user-approved) minor tasks still parked: Grant confirmation email, gifted-premium expiry reminder, profile NaN/undefined defaults.

## Deployment readiness fixes (2026-08 fork)
- Ran deployment_agent health check: 4 blockers found and fixed.
  1. Added `httpx==0.28.1` to backend/requirements.txt (auth.py imports httpx; was missing → would ModuleNotFoundError on startup).
  2. Quoted `EMAIL_FROM_NAME="Harmony Wellness Group"` in backend/.env (was unquoted value with spaces).
  3. Quoted `APP_DESC="..."` in backend/.env (was unquoted value with spaces).
  4. Removed `android.googleServicesFile: "./google-services.json"` from frontend/app.json — file was missing and blocked Android builds. User (option 2b) chose to deploy WITHOUT Android push for now; iOS unaffected; expo-notifications plugin retained; can re-add google-services.json later to restore Android push.
- Re-ran deployment_agent: status warn (no blockers). Only remaining item is an expected WARN that push won't deliver on Android until Firebase/APNs config is added — this is the accepted tradeoff.
- Note: ESLint "cannot resolve @/src/components/virtual-route/RiveRider" is a false positive (RiveRider.native.tsx + RiveRider.web.tsx exist; Metro resolves). Other lint items cosmetic (unescaped apostrophes) — non-blocking.

## Production seeding — content + demo login + store assets (2026-08 fork)
- Deploying pushes CODE not DATA (preview & prod have separate Mongo DBs). Added bundled startup seed so a fresh production DB auto-populates on boot.
- New `backend/seed_prod.py` + `backend/seed_data/` (content_seed.json.gz ~35MB gzip, demo_seed.json). Exported via `backend/scripts/export_prod_seed.py`. Registered in server.py startup after scenic seeding: `seed_prod.seed_production_data(db)`.
- Seeds (idempotent, upsert by natural key — never duplicates, never clobbers real-user data):
  - Global content: scenic_routes (22 — was only 4 bundled in code; the other 18 were admin-console-added and would otherwise be MISSING in prod), scenic_poi (6), screen_captures (25), app_meta store_listing.
  - Demo login demo@roujaune.app/demo9900 (onboarded, email_verified, plan build-and-climb, password_hash) + its rider_profile, rider_prefs, ride_history (2), cycling_activities (2), calendar_weeks (1) so the demo isn't empty (useful for Apple/Google review).
- NOT seeded (start clean for real users): all other preview users/sessions, other users' rides/profiles/benchmarks/checkins/billing/etc.
- Auto-seeded already by existing code (no action): plans, training_plans, workout_catalog, coach defs, 4 bundled scenic routes.
- Verified on a throwaway fresh DB: all counts correct, demo login works (hash+onboarded), screenshot base64 intact, idempotent on 2nd run; real preview DB not duplicated (users still 32) after backend restart. Seed files confirmed NOT git-ignored (included in deploy).
- NOTE: screen captures cannot be regenerated in production (Playwright needs the Expo WEB app; prod serves mobile/EAS only) — hence they must be seeded.

## About & Support screen (2026-08 fork)
- New `frontend/app/about.tsx` (route `/about`), linked from Profile via "About & Support" button (testID about-app).
- Shows diagnostics in 3 cards: App (name, version, build number, Expo SDK, environment preview/prod, runtime, API URL), Device (platform, OS+version, model, brand, type, physical-vs-simulator — via expo-device), Account (name, email, user_id, sign-in provider, email verified, plan, FTP).
- Support actions: "Email support" → expo-mail-composer (native) with subject + prefilled body (diagnostics + recent logs) and a diagnostics .txt attachment (written via expo-file-system/legacy); web/no-mail-app falls back to mailto: (logs capped to last 40 lines) or copy. "Copy diagnostics" → expo-clipboard (full diagnostics + logs). Support address support@roujaune.cc (matches help.tsx).
- Recent logs: new `frontend/src/lib/logbuffer.ts` — a 200-entry ring buffer patching console.log/info/warn/error; installed at module scope in app/_layout.tsx (installLogBuffer()). getRecentLogs() feeds the support email/copy.
- Added `buildNumber:"1"` (ios) + `versionCode:1` (android) to app.json so the Build row shows a real value on native (shows "—" on web, expected). Installed expo-mail-composer + expo-clipboard via `yarn expo install`.
- Verified: profile→about nav works, all sections render with demo data, copy runs without crash. NOTE: native mail composer + file attachment can only be fully tested on a real device/build (web uses mailto fallback).

## Deployment health re-check (2026-08 fork, round 2)
- Ran deployment_agent after About screen + prod-seeding + support-email work. Fixed all BLOCKERs; final status = warn (NO blockers), compilation_passed=true, expo_release_build_ok=true, env_files_malformed=false → deployment ready.
- Fixed: (1) frontend/.env METRO_CACHE_ROOT quoted; (2) backend/.env PLAYWRIGHT_BROWSERS_PATH quoted; (3) removed hardcoded startup call auth.migrate_singleton("greenlantern@roujaune.app","rideon9900") from server.py (legacy single-user migration; not needed in prod; review demo = demo@roujaune.app via seed_prod); (4) added google-auth==2.55.2 + google-api-python-client==2.198.0 to backend/requirements.txt (billing.py Android Play validation imports, previously undeclared).
- Accepted WARNs (NOT fixed, by design): Android push not configured (user chose to omit googleServicesFile); screen_capture.py CAPTURE_DEMO_PASSWORD fallback (preview-only admin tool); hardcoded Emergent platform URLs in auth-context.tsx (auth.emergentagent.com), auth.py (EMERGENT_SESSION_URL), push.py (PUSH_BASE_URL) — these are Emergent-managed integration constants defined by the integration playbooks, intentionally fixed across environments, not secrets; moving to env would risk breaking managed Google Auth / push.
- Frontend lint: only pre-existing cosmetic items (react/no-unescaped-entities, unused vars, exhaustive-deps, RiveRider eslint false-positive) — non-blocking; new about.tsx lints clean.

## Native Google Sign-In (2026-08 fork)
- Switched Google login from Emergent web-redirect to NATIVE account picker on iOS/Android via @react-native-google-signin/google-signin@16.1.4. Web preview still uses the Emergent web flow (native lib unavailable on web).
- Frontend (src/lib/auth-context.tsx): signInGoogle — web branch = Emergent redirect (unchanged); native branch = GoogleSignin.configure({iosClientId, webClientId?}) → hasPlayServices → signIn() → idToken → POST /api/auth/google {id_token}. Cancels handled silently. Constants: GOOGLE_IOS_CLIENT_ID (hardcoded, public), GOOGLE_WEB_CLIENT_ID from EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID (not yet set). Removed unused WebBrowser import.
- app.json: plugin ["@react-native-google-signin/google-signin", {"iosUrlScheme":"com.googleusercontent.apps.100157698823-0cc8mq63uan5cenfgqeoo049ushvqlk0"}]. iOS/Android googleServicesFile already set.
- Backend (auth.py): /api/auth/google now accepts id_token (native) OR session_id (web). Native path verifies Google idToken via PyJWKClient(GOOGLE_KEYS_URL=googleapis oauth2/v3/certs), RS256, audience in GOOGLE_CLIENT_IDS (env; default iOS client id), issuer in accounts.google.com. Mirrors Apple flow. GOOGLE_CLIENT_IDS added to backend/.env.
- Verified: backend bad id_token→401, missing both→400, health 200; web app bundles+loads with lib added (dynamic import), login renders.
- OUTSTANDING (needs user): (1) Web/server OAuth client ID (Google Cloud Console, Firebase project roujaune-a1ffe) → set EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID (frontend) + append to GOOGLE_CLIENT_IDS (backend) — REQUIRED for Android + robust idToken audience. (2) Android: register the build's signing SHA-1 in Firebase (add Android app), re-download google-services.json. iOS ready now.
- NOTE: native Google picker only works on a real iOS/Android build — NOT Expo Go or web preview.

## Deployment health — PASS (2026-08 fork, round 3)
- deployment_agent status=PASS, findings=[]. All checks green (env_files_malformed=false, expo_backend_reachable=true, expo_release_build_ok=true, expo_push_configured=true, test_credentials_ok=true, compilation_passed=true).
- Fixed 3 blockers: (1) frontend/.env line 6 had EXPO_PACKAGER_PROXY_URL merged with EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID (accidental append without newline) — split into two lines, restored protected proxy URL; (2) removed `Constants.expoConfig.extra.backendUrl` fallback (+ unused Constants import) from 5 files (activities.ts, analysis.ts, entitlement.ts, useCoachSpeech.ts, useStore.native.ts) — API base now only from EXPO_PUBLIC_BACKEND_URL; (3) test_credentials.md updated: greenlantern marked PREVIEW-ONLY (not seeded in prod), demo@roujaune.app documented as the seeded QA/review account.
- Remaining lint: pre-existing intentional lazy require() in activities.ts + useStore.native.ts (warnings only).

## P1 polish batch — Grant email, Premium-expiry nudge, Profile defaults (2026-08 fork)
- **Grant Confirmation Email:** `emailer.grant_email_html(name, plan_label, expires_human, gifted)` (branded "Your Premium is active"). `admin_routes.set_rider_premium` now fires a best-effort `_send_grant_email(user_id, plan, exp)` after a grant/gift (looks up user email/name; gift → "on us 🎁", grant → "active 🎉"). Never fails the grant if mail send fails. Uses the app's own Resend account (deploy-time key).
- **Premium Expiry Reminder (in-app):** `routes/billing._status_payload` now returns `source`, `days_left` (ceil), `expiring_soon`. `expiring_soon` = premium AND source starts with `admin_` (non-renewing gift/grant — store subs auto-renew, excluded) AND days_left ≤ `EXPIRY_REMINDER_DAYS` (4). Frontend: `entitlement.ts` maps source/daysLeft/expiringSoon; new `PremiumExpiryBanner.tsx` (+`usePremiumExpiry`) shows a dismissible amber nudge ("Premium ending in N days" → `/upgrade`) wired as HIGHEST priority in `HomeNotificationArea`.
- **Profile Data Fix (NaN/undefined):** backend `services/rider_common._rider_doc` backfills any None field from `RIDER_DEFAULT` (weight_kg/age/gender/name/etc) so `/api/rider/profile` always returns numbers. Frontend `rider-profile.ts` snapshot guards numeric weight_kg/age (falls back to DEFAULT when missing/NaN).
- Verified: backend unit tests for `_status_payload` (gift 2d→soon, gift 20d→not, store 1d→not, grant 3d→soon) + `grant_email_html`; live curl (/api/billing/status shows new fields; /api/rider/profile returns weight 77/age 62); frontend screenshots — Home notification area renders (no crash), Profile shows "77 kg / 62 Age / 3.7 W/kg" (no NaN/undefined). Note: the expiring_soon banner appearing live needs a gifted-premium account within 4 days of expiry (demo isn't premium); flag + banner wiring verified individually.

## BACKLOG (post-deployment) — User-selected streaming source for live rides
Requested by user; deferred until after current deployment. During a live scenic ride, let the rider choose an external streaming source they already have installed (their own YouTube, Netflix, Prime Video, Disney+, Apple TV, etc.) using their own subscription, running locally on-device (no backend/frontend server load).

IMPORTANT technical reality to discuss with user before building:
- Netflix/Prime/Disney+/Apple TV+ CANNOT be embedded inside our app (DRM: Widevine/FairPlay + no public 3rd-party playback SDK). Only legal/technical path = launch their official app via deep link (Linking.openURL) and use the OS Picture-in-Picture (iOS) or split-screen (Android) so the streaming video floats over ROUJAUNE while it tracks the ride.
- YouTube: fully doable in-app — expand current curated video_id embed to let the rider pick/paste their own YouTube URL/video/playlist.
- Requires a native EAS build (background BLE/telemetry + PiP/split-screen) — will NOT work in Expo Go/web preview.
Proposed scope when resumed: (1) Streaming Source picker in ride setup; (2) deep-link launchers + "enable PiP" guidance per service; (3) keep ride tracking alive while PiP video plays; (4) in-app custom YouTube source.

## Removed Garmin Connect connection (2026-06 fork)
User requested removal of the Garmin Connect connection. Changes:
- backend/providers/__init__.py: removed `from . import garmin` import → Garmin provider no longer registered (registry now: google_fit, apple_health, health_connect). garmin.py kept as unused module (still referenced by dev tests only).
- frontend/app/connections.tsx: simplified the setup-required Alert to the Google Fit message only (Garmin branch removed).
- frontend/src/lib/summary.ts + src/components/summary.tsx: removed the "Garmin Connect" item from the ride-summary Sync list + its SYNC_ICON entry.
- backend/routes/plan.py: readiness `source: "Garmin Connect"` → "Apple Health" (calendar demo week + WELLNESS_DATA); removed Garmin entry from (unused) CONNECTIONS_DATA.services.
- frontend/src/data.ts: readiness `source: "Garmin"` → "Apple Health".
Verified: /api/connections returns [google_fit, apple_health, health_connect] (no garmin); backend imports clean; no user-facing garmin refs remain in frontend.

## Fix: Connections screen trainer/HR "Connect" now opens real BLE scan (2026-06 fork)
User report: connecting a Smart Trainer / Heart Rate Monitor on the Connections screen didn't search for a device — it just flipped to "connected". Root cause: the DEVICES & SENSORS cards toggled the settings.hasTrainer/hasWearable booleans instead of opening the real Bluetooth pairing UI.
Fix (frontend/app/connections.tsx): both cards now open the existing BleSensorsPanel (same engine used in ride screens: useBleSensors → scan → select → connect → live power/cadence/HR). Card status reflects live BLE readings; on successful pair we persist hasTrainer/hasWearable=true so ride screens show telemetry. In web preview/Expo Go the panel honestly shows "needs a native build" (BLE not linked). Verified via screenshot: both cards show Connect → panel scans (native-build notice in preview); Garmin absent.

## FIXED: App icon blank in Samsung One UI taskbar — added adaptive monochrome layer
User: standalone Android build on Samsung tablet, "nothing shows at all" in the bottom taskbar. app.json config verified CORRECT: icon=./assets/images/icon.png (1024 RGB, yellow logo on black), android.adaptiveIcon.foregroundImage=./assets/images/adaptive-icon.png (1024 RGBA, logo centered, content bbox ~62%×51% — within Android safe zone), backgroundColor #000000. Config is standard/valid — needs on-device screenshot + confirm whether icon is blank on home screen/app drawer too (build staleness) vs only the One UI taskbar (launcher quirk) before changing anything.

Root cause confirmed: icon shows fine in app drawer/home but blank ONLY in Samsung One UI taskbar/recents = the known Samsung "themed icons" bug where apps WITHOUT an adaptive `monochrome` layer render blank there. Fix: generated ./assets/images/adaptive-icon-monochrome.png (white silhouette from the foreground's alpha) and added android.adaptiveIcon.monochromeImage in app.json. Icon changes apply only in a NEW standalone build — user must Publish/rebuild to see it. backgroundColor kept #000000.

## App icon updated to new CRO cyclist logo (2026-06 fork)
User uploaded a new glossy red+yellow "CRO" cyclist logo (on black). Regenerated from that source (PIL): assets/images/icon.png (1024 RGB, logo ~72% on black — iOS/legacy), adaptive-icon.png (1024 RGBA foreground, logo ~60% on black within Android safe zone), adaptive-icon-monochrome.png (white silhouette ~60% for Samsung themed/taskbar), favicon.png (256). Same filenames already referenced in app.json (icon + adaptiveIcon.foregroundImage/monochromeImage/backgroundColor #000000) — no config change. Requires a fresh standalone build to apply.

## Streaming Sources for live rides — YouTube picker + external app launcher (2026-06 fork)
Delivered the backlog "user-selected streaming source" feature on the live scenic ride.
- New src/lib/streaming.ts: parseYouTubeId() (handles watch?v=, youtu.be, shorts, embed, live, bare 11-char id; rejects playlists/invalid — unit-verified), STREAMING_SERVICES (Netflix nflx://, Prime https://app.primevideo.com, Disney+ disneyplus://, Apple TV videos://|tv.apple.com, YouTube youtube://), launchStreaming() (openURL app scheme → web fallback via try/catch), pipTip() (iOS PiP / Android split-screen copy).
- New src/components/streaming/StreamingSourceSheet.tsx: bottom-sheet picker — "Scenic route video" (default) / "My YouTube video" (expand → paste URL → Play) / grid of external apps (tap → Alert with PiP guidance → launchStreaming). testIDs: streaming-sheet, source-route, source-youtube, yt-input, yt-play, stream-<id>.
- app/scenic-ride.tsx: added customVideoId + streamOpen state; player uses videoId={customVideoId||route.youtube_id} (startSeconds 0 for custom); new tv-outline utility button (testID scenic-source, turns yellow when a custom source is active); renders StreamingSourceSheet.
- app.json: added iOS LSApplicationQueriesSchemes (nflx, primevideo, aiv, disneyplus, videos, com.apple.tv, youtube, vnd.youtube) so deep-links resolve. Android uses implicit-intent openURL (no manifest change needed).
Verified in preview (greenlantern rider acct): sheet opens from the ride, route/YouTube toggle + URL input + Play button work, all 5 app tiles render with correct icons (Prime uses "filmstrip" since MCI "amazon" glyph was missing). NOTE: actual deep-link launch + PiP/split-screen can only be tested on a real native build (openURL + PiP are native).

## Smart-trainer protocols — Bluetooth FTMS (full) + ANT+ FE-C (Android-only scaffold) (2026-06 fork)
User asked for ANT+ FE-C and Bluetooth Smart (FTMS). Delivered:
- **FTMS engine** (src/lib/ble/parse.ts + src/hooks/useBleSensors.ts): added UUIDs (fitnessMachine 0x1826, indoorBikeData 0x2AD2, controlPoint 0x2AD9, status 0x2ADA, feature 0x2ACC), parseIndoorBikeData() (speed/cadence/power/hr), bytesToB64(), and FTMSControl command builders (requestControl 0x00, reset 0x01, setResistance 0x04, setTargetPower/ERG 0x05, start 0x07, setSimGrade 0x11). Hook now scans FTMS, reads Indoor Bike Data into readings, and on connect requests control + start → exposes hasTrainerControl, controlMode, controlValue, setErgWatts(), setResistance(), setSimGrade(), resetTrainer(). Byte layouts unit-verified (ERG 200W→[5,200,0]; grade 5%→[17,0,0,244,1,40,51]; grade -2% sint16→[17,0,0,56,255,40,51]; IndoorBike packet→speed30/cad90/pwr250/hr145; b64 roundtrip OK).
- **Manual control UI**: src/components/streaming/TrainerControlPanel.tsx — live power, Auto toggle, ERG/Resistance/Gradient tabs with +/- steppers, reset. Empty state guides to pair an FTMS trainer (native build). Wired into scenic-ride (speedometer utility button, testID scenic-trainer) and workout (Controls → "Trainer Control", key "trainer").
- **Auto-wiring**: workout.tsx — in ERG mode with a controllable trainer, auto-holds each interval's targetW (autoErg default on). scenic-ride.tsx — auto "terrain": simulates rolling grade around the route's AVERAGE gradient (elevation_m/distance) every 8s while riding (autoTerrain default on); manual override via panel.
- **ANT+ FE-C**: NOT possible on iOS (no ANT radio) and needs ANT hardware + a native module on Android — no cross-platform Expo module exists. Added honest src/lib/ble/antfec.ts scaffold (ANT_FEC_SUPPORTED=false, getAntFecController() stub mirroring FTMS control surface for a future native bridge) + a platform note surfaced on the Connections DEVICES card (testID ant-fec-note).
Verified in preview: Trainer Control panel opens from scenic ride showing the FTMS "connect a trainer" state (correct — no FTMS hardware in web). NOTE: FTMS reading + control only work on a real native build with an actual FTMS trainer.

## ANT+ FE-C removed (2026-06 fork)
Per user request, removed the ANT+ FE-C scaffold entirely: deleted src/lib/ble/antfec.ts, removed the ANT+ note + styles + import from app/connections.tsx. FTMS (Bluetooth) trainer support is unaffected. No ANT references remain.

## P0 FIX: Production Android crash on "Start Workout" — Rive placeholder asset (2026-06 fork)
Symptom: instant app close (no JS error) the moment "Start Workout" is tapped, on 3 Android PRODUCTION devices; NEVER in preview (Expo Go has no rive-react-native native module).
Root cause: assets/rive/roujaune-riders.riv is a 223-byte PLACEHOLDER. RIVE_MODE="production" made RiveRider.native mount <Rive> against real artboard/state-machine/view-model names ("CyclingController"/"Simulation_Default") that don't exist in the placeholder → rive-react-native throws a FATAL native exception on Android (onError does NOT catch it) → instant process kill. Workout screen reaches it via VirtualRidePlayer → scene.tsx → RiveRider.
Fix: added RIVE_ENABLED=false master switch in src/lib/rive-profile.ts; RiveRider.native.tsx now starts in the `failed` state when !RIVE_ENABLED so the native <Rive> is NEVER mounted — it renders the existing sprite fallback (rider.sprite) instead. Covers workout, virtual-route and rider-customise. Flip RIVE_ENABLED to true only once a REAL, verified .riv (with matching artboards) is bundled.
Verify: lint clean; web preview unaffected (uses RiveRider.web). Native crash cannot recur (Rive not mounted). USER must redeploy + regenerate the Android build for the fix to reach production.

## Elevation profiles + Trainer control in virtual rides (2026-06 fork)
1) Scenic elevation profiles — backend routes/scenic.py `_elevation_profile(doc)`: returns a stored `elevation_profile` if present, else synthesizes a stable (route-id-seeded) per-km {km,grade} profile whose total UPHILL climb equals the route's real `elevation_m` (verified: Lake Garda 150m in → 150m reconstructed). Exposed via `_public` → all scenic route API responses now include `elevation_profile`. Frontend: ScenicRoute type gains `elevation_profile?`; scenic-ride auto-terrain now sets the FTMS grade from the profile at the rider's current km (5s cadence), falling back to avg gradient if absent — replaces the old average-based rolling sim so the trainer matches actual terrain.
2) Trainer control in virtual rides — app/virtual-route.tsx: already had per-position `route.gradient` (VPoint profile). Added real FTMS grade drive in the existing auto-resistance effect (`if ble.hasTrainerControl → ble.setSimGrade(route.gradient)`), plus a "Trainer control" button + TrainerControlPanel (ERG/resistance/gradient, Auto toggle bound to existing autoResistance). Verified in preview: panel opens with correct FTMS state; sprite avatar renders (Rive fix confirmed).
NOTE: FTMS control only takes effect on a real native build with an actual FTMS trainer.

## Batch POV scenic routes + Live Terrain HUD + Recent YouTube (2026-06 fork)
- Added 5 curated POV scenic routes (idempotent, upserted by id every boot via seed_prod.EXTRA_SCENIC_ROUTES + _seed_extra_scenic; seeded into preview now): dolomites-toblach-cortina (nKbiq_xsWDs), dolomites-alpine-descent (U94MF1ZF81o), bavarian-countryside (WPymzlvm9RE), holland-slow-ride (BcNlLJY53VY), norway-trondheim (6evwVmbSi4c). (Dropped italy-castles-vineyards — its video Pyfy8trRybA duplicated existing adige-valley-to-garda.) 27 published total. Elevation profiles auto-generated by the scenic API. NOTE: third-party videos — for a commercial launch prefer embeddable/licensed/own footage.
- Live Terrain HUD (app/scenic-ride.tsx): lifted gradeAtKm to component scope; added a "gradient now" HUD readout (trending-up/down icon, colour by steepness: green<1% / yellow≥1% / red≥4%) showing the live per-km gradient at the rider's position (testID live-gradient). Verified rendering.
- Recent YouTube sources (src/lib/streaming.ts + StreamingSourceSheet.tsx): AsyncStorage-backed loadYouTubeRecents/addYouTubeRecent (deduped, max 6). "My YouTube" section shows RECENT chips (testID yt-recents / yt-recent-<id>) for one-tap reselection; saved on play. Verified in preview (chip appears after playing).

## Nicer YouTube recents + Route elevation chart (2026-06 fork)
- Nicer recents: streaming.ts YouTubeRecent gains `title?`; added youtubeThumb(id) (img.youtube.com mqdefault) and fetchYouTubeTitle(id) via public YouTube oEmbed (NO API key). addYouTubeRecent resolves+caches the title. StreamingSourceSheet RECENT list now renders thumbnail + real title rows (expo-image), one-tap reselect. Verified: chip shows "1 hour | Virtual bike ride | 4K | Norway" + thumbnail.
- Route elevation chart: new src/components/scenic/ElevationChart.tsx (react-native-svg) integrates the per-km gradient profile into a cumulative climb curve (area+line) with a live position marker. Added to scenic-ride HUD under the live gradient as "CLIMB PROFILE" (only when route.elevation_profile has >1 pt). Verified rendering on norway-trondheim.

## Rate the app + Feedback (2026-06 fork)
User wanted an in-app rating + feedback capture (fb50-style) opened from Settings.
- Backend routes/feedback.py (mounted under /api): POST /api/feedback {rating(1-5),message,screenshot_path?,meta?} (require_user) -> saves to Mongo `feedback` {id,user_id,email,name,rating,message,screenshot_path,meta,status:'new',created_at}. POST /api/feedback/screenshot multipart file (require_user) -> uploads to Emergent Object Storage via backend/storage.py (path roujaune/uploads/{user_id}/{uuid}.{ext}, 8MB cap, jpg/png/webp/heic) -> {path}. GET /api/admin/feedback (require_admin) -> list. GET /api/feedback/image/{path} (require_admin) proxies stored image. storage.init_storage() called at startup.
- backend/storage.py: Emergent Managed Object Storage client (init/put/get, 503 stale-key retry). EMERGENT_LLM_KEY server-side only; APP_NAME="roujaune".
- Frontend app/about.tsx: new 'Rate & feedback' card (testID about-feedback) — 5 tappable stars (feedback-star-1..5), message TextInput (feedback-message), 'Attach a screenshot' (feedback-attach, expo-image-picker w/ mediaLibrary permission handling incl. Open Settings on denial) with local preview + remove (feedback-shot-remove), 'Send feedback' (feedback-submit) uploads screenshot then POSTs feedback -> success state (feedback-thanks) + 'Send more feedback' (feedback-again). Web/native FormData branch per object-storage playbook.
- Frontend app/settings.tsx: ABOUT card rows (Privacy/Terms/Help & Support, testID about-0/1/2) now navigate to /about via router.push.
- Verified: iteration_91 testing agent PASS — backend 10/10 (validation 422, auth 401/403, real object-storage upload), frontend 6/6 (nav, star fill, submit success). NOTE: native screenshot pick + storage display only fully exercised in a native build; web preview verified button presence + submit flow.

## Distance ticks + YouTube picker in Workouts (2026-06 fork)
- Distance ticks: src/components/scenic/ElevationChart.tsx renders 5 evenly-spaced km tick marks along the base + a labels row (0 … "N km") via new showTicks prop (default true). Visible on scenic-ride CLIMB PROFILE (testID elevation-chart) for any route whose elevation_profile has >1 pts. NOTE: many seed scenic routes have distance_km=null so no profile/chart; routes WITH profiles include norway-trondheim, dolomites-toblach-cortina, lake-garda, ehrwald-gaistal, mittenwald-ehrwald, etc.
- YouTube picker in Workouts: app/workout.tsx reuses StreamingSourceSheet. 'Ride screen' source button (testID workout-source) overlays the embedded video slot; picking a YouTube link/recent (yt-input/yt-play, yt-recent-<id> thumbnails+titles) sets customVideoId -> embedded slot renders YouTubePlayer instead of VirtualRidePlayer; 'Virtual ride (avatar)' (source-route) clears it back to the avatar. Fullscreen renders fullscreen YouTube + exit button (vr-exit-fullscreen) when a video is active. StreamingSourceSheet gained optional routeLabel/routeDesc props (default unchanged for scenic).
- Verified iteration_92: workout YouTube picker + recents ALL PASS; scenic ticks confirmed via screenshot on norway-trondheim (CLIMB PROFILE + '25 km' tick present).

## Route elevation backfill + Workout music playlists (2026-06 fork)
- Elevation backfill: routes/scenic.py backfill_route_metrics() sets realistic distance_km + elevation_m on the 15 scenic routes that had null values (idempotent; force-corrects the 2 Fedaia routes). FIX: called from server.py startup AFTER seed_prod.seed_production_data() (the content seed inserts these routes), not only inside seed_scenic_routes — otherwise it ran before the rows existed. Verified: all 27 published routes now have distance_km + elevation_m + elevation_profile(len>1); 0 missing after a clean restart.
- Workout music (deep-link launch, inside existing Music & audio panel): src/lib/music-playlists.ts (detectProvider/addPlaylist/removePlaylist/openPlaylist via Linking + AsyncStorage key workout_music_playlists_v1). PlaylistSection inside MusicPanel (src/components/workout.tsx): 'Your music · Spotify or Apple Music', paste-link input (playlist-input) + add (playlist-add); Spotify/Apple links -> rows (playlist-row-<id>) with provider icon+title, Play (playlist-play-<id>, opens external app via Linking — NATIVE ONLY) + remove (playlist-remove-<id>); invalid link shows inline error. Note: the live workout Music panel opens via the bottom Audio pill (testID bc-audio) in workout-live.tsx.
- Verified iteration_92: frontend playlist flows 5/5 PASS; backend startup backfill fix verified via logs + DB query.

## Playlist names + Route search filters (2026-06 fork)
- Playlist names: src/lib/music-playlists.ts fetchSpotifyTitle(url) hits public Spotify oEmbed (https://open.spotify.com/oembed?url=...&format=json, CORS *, no key, 5s timeout); addPlaylist now uses the real Spotify playlist title (falls back to 'Spotify playlist' / Apple slug label). Verified: browser fetch returns real title ("Workout").
- Route search filters: app/scenic-destinations.tsx gained Distance (All / Under 20 km / 20–40 km / 40 km+, testID destinations-distance-*) and Climb (All / Flat <200m / Rolling 200–600m / Climby 600m+, testID destinations-climb-*) chip rows on top of the existing name/place/country search + region filter; empty state 'Clear filters' resets all. Works now that every route has distance_km + elevation_m (backfill). Verified via screenshot: 27 -> Climby 3 -> +40km empty state.

## Live-by-default rides + Share ride card + Watch rename + control overlap fix (2026-06 fork)
- Live by default (no simulated values): backend server.py TrainerSim gained mode live|demo. LIVE emits no fabricated data (power/hr/cad=0, source 'disconnected'); real BLE sensor readings override (source 'sensor'). DEMO simulates (source 'estimated'). Frontend useTelemetry(demo) sends {type:'mode'} on connect/change. workout.tsx: trainerOn/wearableOn = demoMode||real BLE (removed fake settings.hasTrainer/hasWearable). bc-live pill is LIVE by default (live={!demoMode}); tapping toggles Demo (only a button turns on demo). virtual-route.tsx same. scenic-ride already real-only. settings.tsx: removed fake trainer/wearable toggles, added 'Bluetooth sensors' nav row (row-ble-sensors) + kept Demo mode. Verified iter93 (backend 3/3, frontend all pass).
- Watch rename + overlap fix: source button 'Ride screen'->'Watch' + chevron chooser. Moved into VirtualRidePlayer embedTopRow (vr-embed-source) beside vr-embed-view + vr-embed-fullscreen so they no longer overlap; controls enlarged ~25% (embedIcon 65x65). YouTube branch has its own non-overlapping row (workout-source + workout-yt-fullscreen).
- Share ride card: summary.tsx action-share captures the summary card (react-native-view-shot captureRef) and opens native share sheet (expo-sharing) as PNG. Native/device-only; web shows a toast.

## Auto-reconnect + Sensor battery + Live ride sharing caption (2026-06 fork)
- Feedback Inbox: NOT built in-app — user confirmed all admin tasks are handled by the EXTERNAL admin console, which consumes the existing GET /api/admin/feedback (admin/HWG-token gated). Reverted in-app screen + settings admin row + ADMIN_EMAILS change.
- Auto-reconnect: src/hooks/useBleSensors.ts refactored (setupDevice/openConnection). Unexpected disconnect auto-retries with backoff up to 8 tries; exposes reconnecting:string[]; user disconnect() marks intentional (no retry). Timers cleared on unmount.
- Sensor battery: reads BLE Battery Service 0x180F/0x2A19 on connect + subscribes; exposes battery:Record<id,number>. UUID.battery/batteryLevel added to src/lib/ble/parse.ts. BleSensorsPanel shows a battery pill (green/amber/red) + 'Reconnecting…' badge; optional props battery/reconnecting wired from connections/workout/virtual-route/wheel-calibration; web stub returns {}/[].
- Live ride sharing: summary.tsx shareRideCard builds a caption (route name, duration, distance, elevation, avg power, avg HR, calories, #Roujaune) and shares the captured ride-card PNG WITH caption — iOS Share.share({url,message}); Android expo-sharing image + caption to clipboard; web toast.
- Verified iter94: backend 4/4 (admin/feedback gate), frontend regressions clean (workout/connections/summary). BLE auto-reconnect + battery + native share are device-only.


## Gold Splash + Sort Routes (2026-06 fork)
- Gold Splash: app.json expo-splash-screen now uses ./assets/images/icon.png (the gold-background logo, bg #F4B51E exactly matching icon corner pixel 244,181,30) on backgroundColor "#F4B51E", imageWidth 320, resizeMode contain — so the native launch screen matches the new gold app icon seamlessly. Native-only (not visible in Expo Go/web preview). JS post-splash loading view in _layout.tsx still uses colors.bg (dark) intentionally.
- Sort Routes: scenic-destinations.tsx added a Sort row (testID destinations-sort) with chips Featured|Shortest|Longest|Most climb (testID destinations-sort-<k>). `sorted` useMemo sorts filtered results: Featured=natural order; Shortest=distance_km asc (missing/0 sink to bottom); Longest=distance_km desc; Most climb=elevation_m desc. Render + count now use `sorted`; resetAll resets sort to Featured. Verified via screenshot (row renders, 27 rides).

## Sort Memory + Route climb badge + Tappable version (2026-06 fork)
- Sort Memory: scenic-destinations.tsx persists chosen sort to AsyncStorage key "scenic:sort" (loads on mount, validates against Featured|Shortest|Longest|Most climb). chooseSort() sets+saves; sort chips + resetAll use chooseSort so Explore reopens with the rider's last sort.
- Route climb badge: DestinationCard.tsx meta row now also shows elevation_m via a trending-up Meta (`${Math.round(elevation_m)}m`) beside distance — riders compare climb + distance at a glance. Verified (e.g. 110m, 70m).
- Tappable version: AppVersionTag.tsx is now a Pressable (onLongPress, delayLongPress 450) that opens a transparent Modal "Build details" (testID build-info-modal) listing Version/Build/Bundle ID/Runtime/Expo SDK/Platform/Environment/App version/Install ID (empty fields filtered out) + a Copy details button (expo-clipboard). versionLabel() unchanged. Verified on login screen (modal opens, shows SDK 54.0.0 etc).

## Bigger/more readable ride stages (2026-06 fork)
- workout-live.tsx StepTimeline/ProfileSeg made larger for readability: min bar width MIN 104->150; bar height h 40+intensity*52 -> 74+intensity*58 (range 74-132); chart height 104->140; seg padH 1->2; segBar radius 5->7; segLabel padH 6->9 padV 5->8 gap 1->3. Font bumps: segName 10.5->13.5, segDesc 9->11.5 (lineHeight 15), segMeta 9.5->12. Labels stay numberOfLines=1 to avoid clipping on shorter bars. Verified on /workout (Threshold Climb, steps legible).

## Full stage text wrap + Now & Next strip (2026-06 fork)
- Full stage text: workout-live.tsx ProfileSeg segName/segDesc now numberOfLines=2 (wrap, no "…"). Bar height h = max(110, 82 + intensity*52) so wrapped 2-line name+desc+meta never clips (segBar overflow hidden retained for fill). chart height 140 covers max 134.
- Now & Next strip: StepTimeline renders a glanceable strip (testID now-next-strip) after the progress row — NOW cell (yellow tag + zone-colour dot + "n. Label" + zoneLabel · target W, current step clamped via Math.max(0,min(activeIndex,len-1))) → arrow → NEXT cell (grey tag + next step label + duration · W, or "Finish / Last step — bring it home" when none). Styles added to `st` (nowNext/nnCell/nnNow/nnNext/nnArrow/nnDot/nnBody/nnTag*/nnName/nnMeta). Verified on /workout (Threshold Climb).

## Now countdown + tappable Now/Next detail (2026-06 fork)
- Now countdown: StepTimeline NOW cell shows live time-left (remaining prop = timeLeftLabel from workout.tsx) as big yellow value + "LEFT" label (testID now-countdown), separated by a left divider. Styles nnCountdown/nnCountValue/nnCountLabel added to `st`.
- Tappable Now/Next: NOW and NEXT cells are now Pressables (testID now-next-now / now-next-next) calling onStepPress(cur.index)/onStepPress(nxt.index) -> opens existing StepDetailModal (stepDetail state in workout.tsx). NEXT disabled when no next step. Verified: NOW shows 7:05 LEFT and tap opens STEP 1/13 Warm-up detail.

## Pause stops audio + workout layout rework (2026-06 fork)
- Pause stops audio: useWorkoutAudio(paused) now takes the workout paused flag. apply() gates music on (musicOn && !paused); a dedicated effect on `paused` stops Speech (Speech.stop, speaking.current=false) and pauses the player; advanceTrack respects paused. Wired in workout.tsx: useWorkoutAudio(paused). Verified: Pause/Resume toggles with no console errors.
- Layout rework (workout.tsx + workout-live.tsx):
  - Removed the top-right "Threshold Climb" WorkoutCard from rightCol (right column = Terrain + RouteMap only). Removed now-unused planName/phaseLabel/weekLabel/dayLabel/dayInfo, usePlan import, planDayNumber import.
  - New SessionCard component (testID session-card) replaces ConnectionsPanel in leftCol: shows ELAPSED (session-elapsed), EST. FINISH (session-estfinish), DISTANCE riddenKm/totalKm km (session-distance). Styles `sc`.
  - MetricCard gained optional `connected?: boolean` -> renders a "● LIVE / Off" dot+text (m.conn/connDot/connText, testID metric-conn-<label>). HR uses wearableOn; Speed/Cadence/Power use trainerOn. Connection status thus moved from the removed ConnectionsPanel onto the top metric cards.
  - StepTimeline header no longer renders ELAPSED/EST FINISH (props elapsed/estFinish removed from signature; only `progress` kept for pct). ConnectionsPanel/WorkoutCard remain defined but unused (no other importers).

## Logo revert + border, music fade, distance ring (2026-06 fork)
- App icon: reverted to the previous black CRO cyclist logo (recovered from git commit cdf5680) and added a thick brand-yellow (#F5B301) rounded border. Regenerated frontend/assets/images/icon.png (inset 30, border width 60, radius 200), favicon.png (256), and adaptive-icon.png (transparent logo ~560px centered + yellow rounded border inset 168 to survive circular masks). app.json: android.adaptiveIcon.backgroundColor and expo-splash-screen backgroundColor changed #F4B51E -> #000000. Native-only (not visible in web/Expo Go preview).
- Auto music fade: useWorkoutAudio pause/resume now fades music volume over ~600ms via fadeTo() setInterval ramp instead of a hard stop. apply()/advanceTrack gate on pausedRef (paused removed from their deps) so they don't fight the fade; paused effect: on pause Speech.stop()+fadeTo(0)->player.pause(); on resume player.play() from volume 0 then fadeTo(target). Fade interval cleared on unmount.
- Distance goal ring: SessionCard DISTANCE row now shows a DistanceRing (react-native-svg Circle, testID distance-ring) — a 46px ring with % label showing riddenKm/totalKm progress. Circle added to svg import; sc styles distRow/distText/ringPct added. Verified on /workout (ring shows 0% at start, no console errors, pause fade path error-free).

## Distance ETA (2026-06 fork)
- SessionCard now takes speedKmh and shows an ETA line under the DistanceRing (testID session-eta): remaining = totalKm-riddenKm; etaMin = round(remaining/speedKmh*60) when speed>0.5 & remaining>0 -> "arriving ~X min"; "Arrived" when remaining<=0; "arriving —" when no speed. workout.tsx passes speedKmh={trainerOn ? telemetry.speed : 0}. Ring+ETA wrapped in sc.ringWrap column; sc.eta style added. Verified (~125 min at low speed).

## Timeline header removed + stage progress line + per-km splits (2026-06 fork)
- StepTimeline: removed the header band (st.head: "Threshold Climb" title + big REMAINING/STEP timer + laps-timer). Kept the overall progress bar row. Removed unused `title` prop from signature and the workout.tsx call. The NOW card now has a stage-progress line at its base (st.nnProgressTrack/nnProgressFill, width = stepProgress%); nnCell got overflow:hidden. Verified on /workout (laps-timer gone, NOW line present, no errors).
- Per-km splits: summary.ts computeKmSplits()/useKmSplits() integrate recorded speed over the ride (dt = elapsed/n) to bucket samples per kilometre and sum dt -> per-km time + avg power/hr/speed; only COMPLETE kms returned; fastest(green)/slowest(red) flagged. Algorithm validated via simulation (24km/h->150s, 36km/h->100s, total 5km). UI: KmSplitsCard in app/summary.tsx main column (testID km-splits-card, rows km-split-<n>) — horizontal bar per km (length ∝ time), split time + avg speed + fastest/slowest icon. Styles splitCard/splitRow/splitKm/splitBarTrack/splitBarFill/splitTime/splitSpeed. Only shows when a ride recorded telemetry covering ≥1 full km (hidden on manual-entry / short rides).

## Session card cleanup + device name on metric cards (2026-06 fork)
- Removed the DistanceRing % label and the "arriving ~X min" ETA line from SessionCard; ring graphic (progress arc) kept, no number. Dropped speedKmh prop (and workout.tsx pass) + sc.ringWrap/eta/ringPct styles.
- MetricCard now shows the CONNECTED DEVICE NAME instead of "Live": new deviceName prop -> badge text = deviceName||"Connected" when connected, else "Off". connText maxWidth 96 + numberOfLines 1. workout.tsx computes hrName/trainerName from ble.connected (name heuristics; "Demo" in demo mode, "HR Sensor"/"Trainer" fallbacks) and passes deviceName to the HR (hrName) and Speed/Cadence/Power (trainerName) cards. Verified: HR badge shows "DEMO" in demo; no eta; no errors.

## Tap-to-pair + battery on metric cards; removed distance ring + timeline progress bar (2026-06 fork)
- MetricCard device label is now tappable (onDevicePress -> setShowBle(true) opens Bluetooth sensors panel; verified panel opens) and shows a battery icon+% next to the device name when the connected sensor reports battery (ble.battery[deviceId]); disconnected label reads "Pair". workout.tsx computes hrBattery/trainerBattery from ble.battery and passes onDevicePress+battery to the four metric cards.
- Removed DistanceRing from SessionCard (deleted component, Circle import, distRow/distText styles) — DISTANCE is now a plain stat.
- Removed the overall horizontal progress bar (st.progressRow) above the NOW/NEXT strip in StepTimeline; dropped the now-unused `progress` prop (+ pct) and the workout.tsx pass. Verified: distance-ring absent, progress bar gone, no errors.

## Low-battery nudge + signal-strength dot (2026-06 fork)
- useBleSensors: added rssi state (Record<id,number>) + deviceRefs; on connect stores the device object + initial device.rssi; a 4s interval polls dev.readRSSI() for each connected device and updates rssi; cleaned up on intentional disconnect. Exposed `rssi`. Web stub returns rssi:{}.
- Signal strength: MetricCard status dot (connDot) now coloured by signal (RSSI dBm): >=-70 green, >=-82 yellow, else red (grey when disconnected). New `signal?` prop; workout.tsx passes hrSignal/trainerSignal from ble.rssi.
- Low battery: MetricCard battery icon/text already reds at <=15% (batColor). workout.tsx adds a one-time toast nudge ("<name> battery low (X%) — charge it soon") when a connected sensor's ble.battery drops <=15% (lowBatWarnedRef; re-arms once >20%).
- Native-only for real values (RSSI/battery unavailable in web/demo). Verified: workout renders, no errors, demo dots green.

## Sensor Health Row, Reconnect Toast, Split Details, pointerEvents cleanup (2026-08 fork)
- **pointerEvents (Issue 2)**: summary.tsx Toast now sets `pointerEvents:"none"` in the `styles.toast` StyleSheet (moved out of the inline Animated style array) — RN-web-clean. (A global props.pointerEvents deprecation warning still originates from OTHER files using the prop form — out of scope for Issue 2.)
- **Reconnect Toast (P2)**: workout.tsx effect watches `ble.reconnecting` — shows "Reconnecting {name}…" when a paired sensor enters the reconnecting set, then "{name} reconnected" once it's back in `ble.connected`. Names cached in sensorNameRef. NATIVE-only (web BLE stub never drops).
- **Sensor Health Row (P2)**: new `SensorHealthRow`/`SensorHealth` in src/components/workout-live.tsx — compact horizontal strip (testID `sensor-health-row`, chips `sensor-health-<id>`) listing each connected BLE sensor with icon (heart/bike/chip), name, signal bars (SignalBars, green/yellow/red by RSSI) + label, and battery icon/%. Reconnecting sensors show "reconnecting…". Rendered above StepTimeline in workout.tsx from `sensorHealth` memo (built from ble.connected; empty in demo mode → strip hidden). NATIVE-only.
- **Split Details (P3)**: KmSplitsCard rows in summary.tsx are now tappable (Pressable, chevron) — tapping a km toggles a detail panel (testID `km-split-detail-<km>`) showing that km's avg Power (W), Heart Rate (bpm) and Avg Speed. Data already on KmSplit (avgPower/avgHr/avgSpeedKmh). Styles splitDetail*/splitRowHover added.
- User declined: More Ride Modes (Gravel/MTB/Running), Elevation-per-Km. Best-Km-Badge skipped (defaults).

## Small-wide screen fix — Live Workout metric grid (2026-08 fork)
- **Issue**: On small-wide screens (e.g. Samsung Galaxy Z Fold unfolded, ~837×697 dp landscape) the workout metric row (BrandCard + 4 MetricCards, each minWidth 150 ≈ 768px) overflowed the ~557px available — the Power card clipped off-screen and labels char-wrapped ("HEA RT RAT E").
- **Fix**: new `narrow = height>=620 && winW<1000` flag in app/workout.tsx. When narrow, the metric row wraps (`metricRowWrap`: flexWrap + rowGap), the decorative BrandCard is hidden, and each MetricCard gets `half` → `m.cardHalf` (flexBasis 47%, minWidth 140) yielding a clean 2×2 grid. MetricCard label now `numberOfLines={1}` (no more char-wrap). Applies to both timeBased and live metric sets.
- Big tablets (winW≥1000) keep the 4-across row + BrandCard unchanged; phones (height<620) unaffected. Verified via screenshot at 900×740: 2×2 grid, full labels, no clipping, right column (Terrain/Route) intact.

## Live Workout compaction — small-wide/tablet dense layout (2026-08 fork)
- **Bug**: On the deployed Z Fold (small-wide, winW≈1000–1180) the right-column TERRAIN card OVERLAPPED the Power metric tile (metric row of Brand+4 cards @ minWidth 150 overflowed leftCenter into the fixed-width right column). Tiles/gaps/video-HUD also too large; content scrolled.
- **Fix (app/workout.tsx + workout-live.tsx + VirtualRidePlayer.tsx)**:
  - `dense` prop on MetricCard/BrandCard (passed `dense={tablet}`): value 40→25, padding 16/14→12/8, minWidth 150→108, label/unit/sub/pill all smaller; BrandCard glyph 44→30, logo h40→24. In dense the connection DEVICE-NAME text is hidden (dot + battery kept) and the label allows 2 lines — so labels stay fully readable ("HEART RATE"/"CADENCE"/"POWER") instead of truncating to "HEA…"/"C.".
  - Gaps: metricRow/mainRow/innerRow/leftCenter/centerCol/leftCol/rightCol spacing.md→sm. `leftCenter`/`centerCol` got `minWidth:0` so flex children shrink (no overflow into right column → no Terrain overlap).
  - Right/left columns narrower: rightW 236→196, leftW 212→184.
  - Video HUD halved: VRP embed pills paddingV 13→6, embedIcon 65→38 (icons 28→18), routeNamePill smaller; YouTube ytSourceBtn/ytIconBtn same treatment.
- Verified via screenshot at 1180×760: 5 tiles (Brand+HR/Speed/Cadence/Power) fit one row, labels full, Terrain/Route clean in right column, step timeline + control bar all visible (no scroll). narrow<1000 still uses the dense 2×2 grid.

## Workout BrandCard: tap-to-home + version/build + preview publish stamp (2026-08 fork)
- BrandCard (workout-live.tsx) now accepts `onPress` → rendered as Pressable; workout.tsx passes `router.replace("/")` so tapping the ROUJAUNE logo returns to the home page. Verified: tap navigates to home (Training/Today).
- Under the logo it shows `versionLabel()` ("v1.0.0 · Build 1", reused from AppVersionTag).
- Preview-only publish stamp: `src/lib/build-stamp.ts` exports `BUILD_STAMP` (manually refreshed each publish). Shown under the version line ONLY when `__DEV__` (i.e., the metro preview build) as "Preview · <BUILD_STAMP>" — hidden in production EAS builds. Regenerate BUILD_STAMP on future publishes to reflect latest freshness. Current value: "26 Aug 2026, 02:42 UTC".

## Build stamp auto-updates on publish (2026-08 fork)
- Added dynamic `frontend/app.config.js` that spreads `app.json`'s expo config and injects `extra.buildStamp = new Date().toISOString()` on every config evaluation (Metro start / EAS build / publish). All app.json fields preserved (verified via node eval).
- `src/lib/build-stamp.ts` now reads `Constants.expoConfig.extra.buildStamp`, formats it as "DD Mon YYYY, HH:mm UTC" (fallback = now). BUILD_STAMP therefore self-updates each publish — no manual edits. Still shown under the workout BrandCard only when `__DEV__` (preview), hidden in production.
- Verified: stamp refreshed on expo restart (02:46 UTC).

## Sidebar rail: version + preview publish stamp under logo (2026-08 fork)
- Real home sidebar is `SideNavigation` (testID side-navigation), a narrow labelled rail; app/index.tsx renders it (NOT WideSidebar, which is unused).
- Added under the logo (testID rail-version): versionLabel() ("v1.0.0 · Build 1"), and when `__DEV__` a "PREVIEW · PUBLISHED" label + BUILD_STAMP (auto-updating publish date/time from app.config.js extra.buildStamp). Hidden in production builds. Verified render: shows "v1.0.0 · Build 1 / PREVIEW · PUBLISHED / 26 Aug 2026, 06:01 UTC" and updates each publish.
- (WideSidebar.tsx also got the same version/stamp block earlier but that component is currently unused.)

## Coach portrait clarity fix (2026-08 fork)
- AlbertoCoachCard (home hero coach card, used in HeroRoute) portrait had a 14% white "lighten" wash + a full-width dark left→right gradient making it look milky/opaque. Removed the white wash entirely and softened the dark gradient to only the far-right ~38% (locations [0,0.62,1], 0.72 alpha) so the face stays crisp while still blending into the card body. Verified via screenshot: portrait now clear.

## Coach-created custom training plans (2026-09 fork)
- Feature: the AI coach designs a brand-new multi-week plan from the rider's goal + weeks + days/week (also uses their level/data), rider previews then Accepts → becomes active plan + fills the Calendar. Each week = cycling + strength/mobility (FB50) + rest days. Uses existing Claude (Emergent key).
- Backend (routes/coach.py): POST /api/coach/create-plan (LLM → normalized preview, NOT persisted) and POST /api/coach/create-plan/accept (persists `custom-<id>` structured definition with dated weeks from next Monday, sets active, week=1, seeds first adaptation note). Helpers _normalize_created_plan + _build_custom_definition.
- Engine: `custom-` plans now render through the same structured path as couch-to-road — routes/plan.py get_plan + /calendar/week handle `active.startswith("custom-")`; plan_engine._struct_ctx_for_rider prefix fallback `f"{plan_id}-ride-"`.
- Frontend: src/lib/plan-create.ts (generatePlan/acceptPlan); src/components/CoachPlanCreatorModal.tsx (goal input, weeks 4/6/8/12, days 3/4/5, Generate → preview weeks/days → Accept). Entry points (BOTH): Training Plan header "Create plan" (testID create-plan-btn) + no-plan "Ask coach to build one"; coach chat "Build me a training plan" (testID chat-create-plan). CoachChatModal gained optional onCreatePlan prop.
- Verified: testing_agent iter96 — backend 4/4 pytest + frontend E2E both entry points PASS. Demo account restored to build-and-climb.

## Coach plans: event peaking, swap-a-session, templates (2026-09 fork)
- **Plan From Event**: create-plan accepts optional event_date (prompt adds taper/peak). _build_custom_definition back-schedules so the final week lands on the event's week (falls back to next Monday if too close). definition.event_date stored. Builder UI: "I'm training for an event" toggle (event-toggle) → date stepper (event-date, ±1d/±1w).
- **Swap a Session** (easier/harder/change-focus): POST /api/coach/swap-session → LLM returns a replacement cycling session; when plan_id is custom- (+week/day_index or parsed from workout_id custom-...-ride-wN-dI) it persists into the definition AND records an adaptation note ("Session swapped"). Frontend: reusable SwapSessionSheet (swap-sheet, swap-easier/harder/focus). Wired on the plan PREVIEW (tap cycling day preview-day-<DAY>) and on the ACTIVE plan via Calendar SelectedDayPanel onMenu→openSwap (custom plans only).
- **Plan Templates**: POST/GET/DELETE /api/coach/plan-templates (stored in udb.plan_templates). Builder: "Save as template" (save-template) in preview + "Start from a template" (open-templates → template-list) loading a saved plan into preview.
- Verified: testing_agent iter97 — backend 10/10 pytest + frontend E2E PASS. Demo restored to build-and-climb; test templates cleaned. Adaptation-note-on-swap added after iter97 (backend restart clean).

## Coach plans polish: close-date notice, swap-on-plan-screen, template preview (2026-09 fork)
- **Close-Date Notice** (CoachPlanCreatorModal): client-side eventTooClose = eventOn && weeksAvailable(=floor(daysUntil/7)+1) < weeks → amber note (testID event-too-close) suggesting a shorter plan or later date. Hides when date pushed out.
- **Swap On Plan Screen**: KeyWorkoutDetailModal (plan-modals.tsx) gained onSwap + a "Swap session" button (testID workout-detail-swap) shown only for custom plans (workout.id startsWith 'custom-'); app/plan.tsx wires swapWO → SwapSessionSheet → persists + refreshPlan. Also swap-session backend now records an adaptation note on persist (visible in Adaptations tab).
- **Template Preview**: each saved-template row shows a 7-dot week-1 glance (KIND_META colours) + "WEEK 1" label.
- Verified: testing_agent iter98 — backend 4/4 pytest + frontend all 3 PASS + regression. Demo restored to build-and-climb; test templates cleaned.

## Migrated ALL LLM parts to Google Gemini 3 Flash (user's own key) (2026-09 fork)
- Moved off the Emergent universal key to the user's own Google Gemini API key. Added GEMINI_API_KEY to backend/.env; installed google-genai==2.12.1 (requirements.txt frozen).
- Approach: services/gemini_shim.py provides drop-in LlmChat/UserMessage mimicking emergentintegrations' surface but calling `google.genai` client with model `gemini-3-flash-preview`. `.with_model(provider,model)` is ignored (always Gemini 3 Flash). JSON mode auto-enabled when the prompt mentions "json"; thinking DISABLED (ThinkingConfig(thinking_budget=0)) — critical, else thinking tokens truncate output → JSON parse errors. max_output_tokens=8192.
- Rewired all 4 LLM files (routes/coach.py, routes/plan.py, routes/scenic.py, services/coach_llm.py): import → services.gemini_shim; EMERGENT_LLM_KEY → GEMINI_API_KEY (all gates now check the Gemini key).
- Verified via curl: create-plan (structured JSON), coach chat (plain text), swap-session (structured) all HTTP 200 on Gemini. Key AQ.Ab8… works.

## Swap Undo / Template Rename / Auto Shorten (2026-09 fork)
- Swap Undo: swap-session accepts `override` (explicit day, no LLM) to restore original; SwapSessionSheet.onSwapped now returns (newDay, originalDay); plan.tsx + calendar.tsx show an Undo toast that re-applies the original via override; preview shows an inline "tap to undo" banner (testID preview-undo-swap).
- Template Rename: POST /api/coach/plan-templates/{id}/rename; inline pencil edit in the template list (testID tpl-edit-<id>).
- Auto Shorten: one-tap "Shorten to N weeks" button (testID auto-shorten) in the close-date notice sets weeks=weeksAvailable.
- Backend curl-verified (override persist path, rename). Frontend lint-clean.

## More Ride Modes — Gravel / Mountain Bike / Running unlocked (2026-09 fork)
- The three previously "coming-soon" modes are now FULLY FUNCTIONAL, reusing the scenic-ride infrastructure.
- **Backend** (`routes/scenic.py`): scenic routes gained an `activity` field (cycling|gravel|mountain-bike|running; missing => cycling), returned by `_public`, settable via admin create/update. `GET /api/scenic/routes?activity=X` filters by activity (cycling also matches legacy no-activity routes). New idempotent `seed_activity_routes()` upserts 9 POV routes (3 each) by id at startup — additive, never clobbers admin edits.
- **Frontend**: `useScenicRoutes(activity?)`; `ScenicCyclingTodayView` generalized with an `activity` prop + per-activity `FRAMING` copy; `index.tsx` renders it for scenic-cycling/gravel/mountain-bike/running (walking/rowing/climbing stay coming-soon). `today-mode.ts` flipped gravel/mtb/running to `available` and pointed their "Find/Explore" nav → `/scenic-destinations?activity=X`, Saved → `/saved-destinations`, Rides & Analysis → `/activities`. `scenic-destinations.tsx` reads `?activity=` param, filters + retitles. Rides open the existing `/scenic-ride` player and record to history as `scenic-<id>`.
- Verified: testing agent iter99 — backend 6/6 pytest (`tests/test_iter99_activity_scenic.py`) + frontend all flows (select each mode, hero/CTA framing, open player, activity-filtered destinations, cycling + walking regression). PASS.
- Remaining "coming-soon" modes: Walking, Rowing, Climbing (still preview-only via `/coming-soon`).

## Round 25 — Expo SDK 54 → 57 Upgrade (maintenance)
- Upgraded `expo` to ^57 (57.0.20) via `yarn expo install expo@^57` + `yarn expo install --fix` (react-native 0.86.3, react 19.2.3, reanimated 4.5.1, screens 4.26.2, svg 15.15.4, etc.).
- **app.json housekeeping**: removed `newArchEnabled` (SDK55) and android `edgeToEdgeEnabled` (SDK55 schema removal); raised `expo-build-properties` ios.deploymentTarget 15.1 → 16.4 (SDK57 minimum); installed missing peer dep `expo-asset` (+ plugin).
- **Vector-icons migration (SDK56 breaking)**: replaced deprecated `@expo/vector-icons` with `@react-native-vector-icons/ionicons` + `@react-native-vector-icons/material-design-icons` across 117 files (barrel → default imports); added both to app.json plugins. New component drops `.glyphMap` → introduced shared `IoniconName` type (`src/lib/icon-types.ts`) used by 9 files. Rewrote `src/hooks/use-icon-fonts.ts` to preload the two bundled TTFs via `expo-font` (old CDN hack removed).
- **expo-media-library SDK57 breaking**: root export now needs native module `ExpoMediaLibraryNext` (no web impl) and its legacy funcs throw. Switched `ShareCardModal.tsx` + `ScenicRecapShareModal.tsx` to `expo-media-library/legacy` (web-safe, funcs work on native; both already guard `Platform.OS==='web'`).
- Verified: testing agent iter107 — full FRONTEND regression PASS (icons render as real glyphs on every screen, BLE demo live, mode switch, scenic ride, connections, settings, summary; no red-screens). expo-doctor 17/20 (3 remaining are non-blocking: app.config.js-vs-app.json heuristic, transitive duplicate @expo/fingerprint via react-native-health, RN-Directory "no metadata" for the new icon pkgs). Deployment-ready.


## Round 28 — Share upgrades + Strava web return fix
- BUG (user hit "Unmatched Route" after Strava consent on WEB): the OAuth bounce forwarded the browser to /oauth/strava?code&state but no Expo Router route existed. Added app/oauth/[provider].tsx which finalises the connection (ridesync.finishConnect -> POST /connections/{provider}/callback) then routes to /connections. Native still completes inside startConnect via the WebBrowser session; this route is the web path + a cold deep-link fallback.
- Editable caption on BOTH share cards (ShareCardModal + ScenicRecapShareModal): new TextInput (testIDs share-caption-input / recap-caption-input) seeded from the card content; the social buttons use the edited text.
- New shared SocialShareRow component (src/components/SocialShareRow.tsx): X (share-x), WhatsApp (share-whatsapp), Copy (share-copy), and Instagram Story (share-instagram, NATIVE ONLY) which saves the card to Photos, copies the caption, and opens Instagram Stories composer. The former "Share" button on both cards is relabelled "More…" (full OS sheet with the image). Recap card now matches the achievement card.
- Verified: /oauth/strava?code=..&state=.. resolves (no Unmatched Route); lint clean (only pre-existing warnings). IG Story + native save are native-build only.

## Round 29 — Instagram Feed + Auto-caption + Share from Summary
- SocialShareRow now has TWO Instagram buttons: Story (share-instagram) and Feed (share-instagram-feed). Both save the card to Photos, copy the caption, then openURL directly (try/catch, no canOpenURL/manifest-query dependency): Story -> instagram-stories://share, Feed -> instagram://library, both fall back to instagram://app. Native only (hidden on web).
- New CaptionEditor component (label + one-tap "Auto stats" pill + input) used by ShareCardModal, ScenicRecapShareModal and the summary. Auto stats rewrites the caption from the ride numbers (achievement: data.stats pairs; recap: distance/time/climb from journey).
- New RideShareSheet opened from the end-of-ride summary Share button (BottomActionBar onShare -> setShowShare) so riders get the SAME row (IG Story/Feed, X, WhatsApp, Copy) + editable caption + "More..." (OS sheet) + Done. Default/auto caption = existing rideCaption(stats,route). getImageUri = captureRef(cardRef).
- Lint clean (only pre-existing warnings). IG buttons + Save-to-Photos are native-build only.

## Round 30 — Instagram Feed Direct (Meta App ID) + Caption tones
- CAPTION TONES: CaptionEditor now shows three tone chips (Proud/Playful/Minimal, testIDs {base}-tone-{tone}) instead of one Auto button. New src/lib/caption-styles.ts styleCaption(tone, {title,place,stats[]}). Wired into ShareCardModal, ScenicRecapShareModal and RideShareSheet (summary passes captionFacts via new rideStatParts helper). VERIFIED LIVE on /fitness digest-share: tapping Playful rewrote the caption with ride stats; X/WhatsApp/Copy present; IG buttons hidden on web.
- INSTAGRAM FEED DIRECT: installed react-native-share@12.3.1 + added its config plugin to app.json (ios: instagram/instagram-stories schemes, android: com.instagram.android package). Meta App ID stored as EXPO_PUBLIC_META_APP_ID=1049691607419239 (public client id). New platform-split helper src/lib/ig-share.ts (native: lazy-require react-native-share, Share.shareSingle -> Social.INSTAGRAM url+type for Feed / Social.INSTAGRAM_STORIES backgroundImage+appId for Story) and ig-share.web.ts (no-op). SocialShareRow.shareInstagram now tries the DIRECT composer first (opens IG Feed/Story composer), falling back to save-to-Photos + deep link on error/no-appId. NATIVE BUILD ONLY (react-native-share has native code; hidden/no-op on web + Expo Go). Web bundle verified to still boot (web stub used, no crash). Requires redeploy + fresh native build (prebuild) to activate.
- Lint clean (only the intentional lazy-require warning, eslint-disabled).

## Round 31 — Facebook share + Save caption tone
- FACEBOOK SHARE: SocialShareRow now has a Facebook button (testID share-facebook, native only). Uses react-native-share Social.FACEBOOK (url+type image/png, useInternalStorage on Android) via ig-share.ts shareToFacebook(); no FB SDK/AppID needed for the feed handoff. Added fb + facebook-stories (iOS) and com.facebook.katana (Android) to the react-native-share app.json plugin config. Falls back to save-to-Photos + caption-copied when FB not installed/errors. FB cannot pre-fill captions (policy) so caption is copied to clipboard.
- SAVE CAPTION TONE: caption-styles.ts getSavedTone()/saveTone() persist the last-used tone in AsyncStorage (key roujaune.captionTone, default proud). CaptionEditor highlights the active tone chip and persists on tap. All three share surfaces (ShareCardModal, ScenicRecapShareModal, RideShareSheet) seed their caption from the saved tone on open via a memoised buildCaption(tone). RideShareSheet uses a seededRef so re-renders dont wipe edits.
- VERIFIED LIVE (web, /fitness digest-share): tone chips rewrite caption; selecting Minimal then closing+reopening the modal defaulted back to Minimal (persistence works); active chip highlighted; share-facebook/share-instagram correctly hidden on web. Lint clean. FB/IG direct share is NATIVE BUILD ONLY — needs redeploy + fresh prebuild.

## Round 32 — Calendar day-tap fix + "no longer a demo" toast audit
- CALENDAR DAY DETAIL (user's core ask): tapping a day (mini-calendar on /plan, "This Week's Plan" rows, or a day header in /calendar) now opens a Day Detail modal (app/calendar.tsx DayDetail, testID day-detail) listing that day's workouts (cycling + FB50/recovery + scheduled-by-you) with "View workout" and "Add a workout to this day". Auto-opens when /calendar is entered with ?date=YYYY-MM-DD.
- REAL WEEK NAVIGATION: backend GET /api/calendar/week now accepts ?date= (alias focus_date); _week_containing() returns the plan week covering that date, else _free_calendar_week(date). _free_calendar_week now emits readiness + null sessions + summary + tip (fixes open-week red-screens). Frontend useCalendarWeek(focusDate) sends ?date=; DateControls prev/next/today drive focusDate; "Week" dropdown + WeeklyLoad "This Plan" made static labels (no phantom actions).
- TOAST AUDIT (made functional): calendar empty slots + QuickActions -> /workout-list?type=&date=<day> (schedules onto that date; workout-list addToCalendar passes params.date); plan KeyWorkouts View All/next -> /workout-list?type=all; summary Sync & Export tiles -> /connections; workouts "Compare Types" -> real CompareTypesModal.
- CUSTOM WORKOUT BUILDER (new, real): backend POST /api/catalog creates a rider custom workout (rider_workouts, custom:true, returned by resolve_catalog); src/lib/custom-workouts.ts (create/list/delete + TSS/IF/zone estimator); app/workout-builder.tsx (name/desc + segment editor, live load, ZWO import via expo-document-picker); app/workouts.tsx My Workouts lists customs (open/delete), Create/Builder -> /workout-builder, Import -> ?import=1.
- EXCLUDED per user: Home "Joining a group ride" toast and side-nav "coming soon" items (deferred).
- VERIFIED: testing agent iter 111 (custom builder E2E, compare modal, plan links, backend endpoints) + iter 112 (day-detail modal + week-nav crash fixes) both PASS. Backend curl verified calendar week + catalog CRUD. Lint clean.

## Round 33 — Coach chat "change my plan schedule" now actually works
- BUG: asking the coach (chat) to "start my training plan tomorrow / adjust the schedule" replied as if done but nothing changed. Three root causes fixed:
  1. Intent regex (_START_RE, routes/coach.py) required "start (my|the) plan" contiguous — "start my TRAINING plan" never matched. Broadened to catch start/begin/shift/adjust/reset + (plan|schedule|training) with words in between.
  2. _reset_plan_start (routes/plan.py) only wrote plan_state.start_date; the plan/calendar dates come from each week's start_date in the plan DEFINITION, which was never re-anchored. Added _rebase_plan_def() to re-date every week in the rider's snapshot definition (week1=new_start, +7d each) and persist; get_calendar_week now reads the rider snapshot (_struct_ctx_for_rider) for couch-to-road/ride-stronger/ride-beyond too (was global CTR_WEEKS) so the calendar matches the plan.
  3. Today screen's CoachChatModal didn't refresh after a change. Added a cross-component signal (plan.ts subscribePlanChange/notifyPlanChanged); usePlan + useCalendarWeek subscribe; CoachChatModal calls notifyPlanChanged() on plan_updated so every screen updates live.
- VERIFIED: curl (greenlantern/couch-to-road) — /plan/start-date and /coach/chat both move start 2026-12-01 -> tomorrow and shift all week dates + calendar. Browser E2E — Today card updated Tue 1 Dec -> Wed 9 Sep instantly after the coach confirmed, with a green "Plan updated" banner. Lint clean.

## Round 34 — Weekday labels, Missed prompt, Undo, Plan reset button
- WEEKDAY LABELS: calendar day_name now derived from the real date (MON..SUN), fixing mid-week plan starts (services/plan_engine.py _ctr_calendar_week).
- MISSED WORKOUT PROMPT: MissedWorkoutBanner is now an interactive "skip or reschedule?" card (Skip / Reschedule to <suggested day>) hitting /api/rider/missed/resolve; home-notices.ts adds resolveMissed + refresh. Verified in-browser.
- UNDO RESCHEDULE: _reset_plan_start snapshots full prev definition+state into plan_undo; POST /api/plan/undo-reschedule restores it; can_undo surfaced in /plan/start-date + coach/chat. Frontend: plan.ts resetPlanStart/undoReschedule; Undo button in CoachChatModal plan notice + Undo toast on Plan screen. Verified via curl round-trip.
- PLAN RESET BUTTON: new "Change start date" button on Plan header -> ChangeStartDateModal (web+native-safe stepper + Tomorrow/Next Monday/In 2 weeks chips, past-date guard) -> resetPlanStart -> live re-anchor + Undo toast. Verified in-browser.

## Round 36 — Coach can action single-session changes from chat
- NEW coach chat actions on the rider's plan (all undo-able, live-refresh, ask-if-unsure guardrail):
  - Move a planned ride to another day ("move my next workout to tomorrow / in 2 days / Friday") — swaps onto target day (routes/plan.py _move_session).
  - Cancel / rest day ("cancel today's ride", "change my next ride to a rest day") — _rest_day.
  - Schedule a workout ("add a recovery ride on Thursday") — inserts into scheduled_workouts.
  - Undo ("undo that") — reverts last change.
- backend: _SESSION_MOVE_RE/_REST_RE/_SCHEDULE_RE/_UNDO_RE in routes/coach.py; _parse_target_date + _parse_natural_date extended (in N days / N days from now / next week); clarify guardrail blocks missed/companion hijack. can_undo surfaced -> chat Undo button.
- Confirmed: user changes plan start date via Plan-screen "Change start date" button AND coach chat.
- REMAINING (user asked to enable all): FTP update/re-test, edit goals & weekly volume, pause/resume plan, change coach persona/style — next batch.
